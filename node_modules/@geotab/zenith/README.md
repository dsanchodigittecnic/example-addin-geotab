<!-- ABOUT THE PROJECT -->
## Introduction

Zenith is a library of UI components on React that's essential for making Geotab's user interfaces consistent and speeding up development. It provides a full set of building blocks for creating UIs easily, and it's built on React, the default frontend library in Geotab. 
Zenith is a “view” layer - it utilizes React components to render a user interface. Typically your application has a state, user filters, states of popups,  Zenith doesn’t request data, doesn’t handle errors, doesn’t check clearances. It only renders components on the page and presents data.

<!-- GETTING STARTED -->
## Getting Started

### Project installation and Setup

Zenith is distributed as an npm package on [https://www.npmjs.com/](https://www.npmjs.com/).

Go to the root of your project and run the following command:
```
npm install @geotab/zenith --save
```

#### Import styles
Styles in Zenith library are located in the css file `@geotab/zenith/dist/index.css` which should be included in your project. Here is an example how to include them if your builder handles css imports:

```
import "@geotab/zenith/dist/index.css";
```

It can be also imported in your less/sass file.

<!-- USAGE -->
### Usage
If installed correctly you will be able to import components from the package:

```
import { Menu, Button, IconSettings3 } from "@geotab/zenith";
```

Now components are ready for usage.

### Available components
Zenith library provides components defined in Zenith Design System. It includes very basic blocks like colors, icons, font presets, atom components like buttons, checkboxes etc and high order components - header, table, filters bar etc. List of existing components can be found in [Storybook](https://developers.geotab.com/zenith-storybook/index.html).

## Change log

### 2.0.0

We're excited to announce that **Zenith now supports EcmaScript Modules** (ESM)! This major release brings modern JavaScript module support to help you build better, more efficient applications.

**Why ESM Support Matters**

ESM is the modern standard for JavaScript modules in frontend development, bringing significant advantages:
 * Superior tree-shaking: ESM's static structure enables bundlers to analyze your imports at build time and eliminate unused code more effectively, resulting in smaller bundle sizes
 * Future-proof: ESM is the JavaScript standard moving forward, ensuring long-term compatibility

#### Important migration notes

While this major release does not include any API changes, there's a critical configuration requirement:

**Avoid Mixing Module Systems**

Your bundler must import all JavaScript files from either dist (CommonJS) or esm (ESM) — never both. Mixing imports can cause issues with React context and other shared state. Problematic example:
```
import { Button } from '@geotab/zenith';           // May use ESM
import { Card } from '@geotab/zenith/dist/Card';   // Uses CommonJS
// ❌ This mixing can break React context!
```

Good example:
```
import { Button } from '@geotab/zenith';           // May use ESM
import { Card } from '@geotab/zenith/esm/Card';    // Uses ESM as well
```

#### Recommended Approach

Since many modern bundlers prioritize ESM by default, we recommend one of these strategies:

 * **Use ESM** (recommended): ensure your bundler configuration prioritizes the esm directory
 * **Use CommonJS** (for compatibility): configure your bundler to prioritize CommonJS and ensure all imports use consistent paths

Upgrade today to take advantage of modern JavaScript module support and improved tree-shaking capabilities!

### 1.26.6

 * Improve `Chart` accessibility
 * Fix accessibility issues in `FiltersBar`
 * Fix jumping focus in `FiltersBar`
 * Fix loading spinner direction in `Waiting`
 * Improve animation of scroll buttons in `FiltersBar`
 * Fix handling errors in `LazyContent`
 * Add aria-label to the component `ColumnsSelector`
 * Add property `target` to list item links
 * Add option to hide counter number in `PillExpandable`

### 1.26.5

 * Fix min width of side panel on mobile
 * Fix updating title of pills in `PillBox`
 * Fix top border appearance of columns dropdown in `Table`

### 1.26.4

 * Improve accessibility of `Chart`
 * Delayed loading of `Table` actions on mobile
 * Improve positioning of `Popup` in edge cases

### 1.26.3

 * Update the title of the dropdown trigger button
 * Fix bar chart without tooltip
 * Fix using tooltip with menu items
 * Add note in the line chart tooltip
 * Improve footer buttons in side panel for languages with long words
 * Don't show changed filters in bar if they appear only in side panel
 * Add full width Banner size XXL
 * Add zenith translation files for new languages (da-DK, fi-FI, hu-HU, nb-NO, sk-SK)
 * Minor style improvements of `Toggle`
 * Allow icons in context-menu actions in `ActionsColumn`

### 1.26.1

 * Allow disabling next/submit buttons in `FormStepperButtons`
 * Fix scrolling dropdown on mobile
 * Make title optional in `Card`
 * Fix point hover size in `Chart`
 * Update the logic of selecting rows in `Table`
 * Allow custom icon in `PillExpandable`
 * Header toolbar updates (make "More" button to be an icon button, support icon buttons in Header)

### 1.26.0

 * Nested rows in `Table`
 * Add translations
 * Fix action link misalignment in `Table`
 * Allow to define point colors in `Chart`
 * Fix `PillExpandable` appearance in `Table`

### 1.25.3

 * Update translations in `Chart`
 * ColumnsSelector is right-aligned by default
 * Fix width of visible on hover actions in `Table`
 * Promise-based actions in `Table`

### 1.25.2

 * `FiltersBar.DropdownWithCheckbox` has been added
 * `FormStepper` has been added
 * Add tooltip for a long title in `Card`
 * Add property to configure `SidePanel` width
 * Add translations for tranditional chinese language
 * Fix `Chart` tooltip in `Modal`
 * Description can be a `ReactNode` in `PillExpandable`
 * Allow buttons in `Header` to be in context menu by default
 * Fix visual appearance of `PillExpandable`
 * Fix loading option names in pills under the `Dropdown` 
 * Add `aria-label` to single-select dropdown
 * Add the property `error` to `TimePicker`

### 1.24.2

 * Add Summary component in Charts
 * Fix Dropdown search in mobile view
 * Add initial sorting order for table
 * Fix Stepper issue with decimals
 * Fix max banner width for XL size
 * Fix sideEffect setting in package.json

### 1.24.1

* Create `Summary` component
* Create `Mobile banner` and fix DriveApp banner paddings
* Create context provider for buttons in `Header`
* Allow `Pill` to be used without text
* Fix `DataGrid` padding in `Layout`
* Update `DateRange` with stepper: step button can be in a disabled state
* Improve outline state in components
* Align image in `ListItem` to the center when it's small
* Set a height limit for the `ExpandablePill`
* Update `Alert` to accommodate 4 lines of text
* Update loading state `Dropdown` and `GroupsFilter`
* Improve `Range` and `FiltersBarRange` components appearance 
* Limit `SidePanel` width
* Resolve React library vulnerability issue
* Fix `DateRange` issue: Cannot clear date range back to initial or default value when only 'Custom' date range option is used
* Fix `Dropdown` issue:  wrong colors on active state
* Fix `Action columns` issue: failing to load in Mobile view
* Fix `Groups filter` issue: losing focus after removing all symbols
* Fix `FiltersBar` issue: mobile `SidePanel` state reset

### 1.23.0

 * Add the property `target` to `Modal` buttons
 * Add possibility to use separator in `Menu` and `Table` actions
 * Make specific `Table` columns non-draggable
 * Fix truncation in lists
 * Allow tooltip in `LineChart` to appear only on data points
 * Add `BannerUpsell`
 * Audit `SidePanel`
 * Add the property to configure `DateRange` trigger text
 * Add `SortControl` default value translation
 * Add ability to disable `Pagination`

### 1.22.4

 * Prevent `ListItem` to follow link on action click
 * Fix long title in `Card`
 * Fix long title in `Banner`
 * Add the property to configure initial focus in `Modal`

### 1.22.3

 * Update `Card` appearance
 * Trim whitespaces in `Dropdown` search with dataItems

### 1.22.2

 * Add component `List`
 * Reset 'site-wide` in `GroupsFilter` on reset
 * `DateInput` associates id with html element
 * Fix the `Dropdown` width constraint

### 1.22.1

 * Add Korean translation

### 1.22.0

 * DataGrid accessibility fixes
 * Paddings fixes for DataGrid
 * Audit button fixes
 * Update styles for Waiting component

### 1.21.1

 * Fix banner multiline issue without header

### 1.21.0

 * Implement `DateRange` with stepper
 * Implement GroupsFilter selection placeholder for advanced groups filter case
 * Fix table scroll position with pagination
 * Fix incorrect clear button disabled state
 * Update Header's title max-width

### 1.20.0

 * New component `Chart`
 * Add feature to detect range value in DateRange
 * Add possibility to prevent unchecking selection in SelectField
 * Add icon IconLaptop and deprecated duplicated
 * Audit: Feedback (Modal and Banner)
 * Fix GroupsButton positioning
 * Fix sort indicator in Table
 * Collect all Zenith documentation in one file

### 1.19.1

 * Fix stepper issue with decimal values

### 1.19.0

 * New interface for dropdown component. For flat data displayed in dropdowns, you can now specify that all items in the dataset are considered selected
 * New properties available in textarea component: 'resize' and 'readOnly'
 * Show selection in GroupsFilter
 * Audit: iconography 

### 1.18.0

 * New component `GridLayout`
 * Fix date formatting and parsing in DateRange
 * Fix z-index of the `Select` absolute container
 * Update CSS to use `flex-start` and `flex-end` instead of `start` and `end`
 * Fix validating value in Stepper
 * Fix using Card with buttons in forms
 * Fix positioning in input adornment styles
 * Prevent empty custom popups in Header
 * Fix opening secondary action link in `PillExpandable` in new tab
 * Add icon `IconHistory`
 * Update styles of `Tabs` in `Header`

### 1.17.0

 * BetaPill is updated to collect feedback on new pages/views
 * Add the ability to use customFiltersBar component in the header
 * Add debouncing for the getData function in the dropdown component
 * Add property `target` to Button to allow opening links in new tab
 * Add isLoading property to FiltersBar.Dropdown
 * Improve skeleton component
 * Improve data rendering in Dropdown
 * Improve Card title
 * Fix SelectField is out of viewport in some cases
 * Fix Extra symbols on SortControl in mobile view
 * Fix for Textarea overflow

### 1.16.1

 * Improve dateRange component
 * Fix unreliable test
 * Audit: table, pills, text-secondary for placeholder color, specs for the banner component
 * Improve Header: Make Menu.Item to be bottom-right aligned in Header
 * Improve FiltersBar: Show selected values as pills in the sidePanel dropdown when getSelectedItem is passed

### 1.15.4

 * Fix unreliable tests
 * Fix for Input's placeholder full width
 * Table Improvements
 * Improve Card accessibility
 * Improve Footer button stories
 * Fix styles for Header with Tabbar

### 1.15.2

 * Improve banner accessibility
 * Add attribute `autoFocus` to `TextInput` and `SearchInput`
 * Add default placeholder for `GroupsFilter`
 * Fix mode detection in `FeedbackProvider`

### 1.15.1

 * Improve FeedbackProvider in add-ins
 * Reduce size of npm package
 * Add property getSelectedItem to Dropdown
 * Add option to hide "Select all" in Table
 * Link ToggleButton with the label
 * Make iconPosition optional in TextIconButton
 * Fix spacing between radio and label in reverse mode
 * Fix Card autoHeight when use it inside Cards
 * Fix appearance of Alerts and Toasts
 * Fix issue with body scroll in Dialog

### 1.15.0

 * `Alerts` and `Toasts` are added
 * Fix unreliable test
 * Fix calculation of the Layout width
 * Apply changes in `FiltersBar` right away when user clicks the "Clear" button
 * Change the accessibility role for `Banner`
 * Improve bookmark icons
 * Disable dates in `DateInput`

### 1.14.1

 * Update structure of Zenith Storybook
 * Fix ARIA role for Banner
 * Improve accessibility of radio buttons in side panel
 * Improve accessibility of links
 * Improve sizing of the title in Header
 * Add the property `triggerAriaLabel` to `Dropdown`

## Feedback

For any questions or feedback please email zenith@geotab.com.
