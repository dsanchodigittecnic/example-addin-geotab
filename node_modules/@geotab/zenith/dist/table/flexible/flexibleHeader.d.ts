import { ReactNode } from "react";
interface IFlexibleColumnProps {
    draggable: boolean;
    resizable: boolean;
    title: ReactNode;
}
export declare const FlexibleHeader: ({ draggable, resizable, title }: IFlexibleColumnProps) => import("react/jsx-runtime").JSX.Element;
export {};
