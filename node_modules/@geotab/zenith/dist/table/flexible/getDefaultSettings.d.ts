import { IEntityWithId } from "../../commonHelpers/entity";
import { IRowEntity } from "../../dataGrid/row/row";
import { IListColumn } from "../table";
export declare const getDefaultSettings: <T extends IRowEntity<N>, N extends IEntityWithId>(columns: IListColumn<T, N>[]) => {
    name: string;
    visible: boolean;
    width: number | undefined;
}[];
