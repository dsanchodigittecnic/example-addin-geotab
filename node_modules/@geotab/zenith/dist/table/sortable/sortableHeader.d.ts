import { ReactNode } from "react";
import { ColumnSortDirection, ISortableValue } from "./interfaces";
import { ColumnSortDirection as ColumnSortDirectionTable } from "../table";
interface ISortableHeaderCellProps {
    columnName: string;
    isSortedBy: boolean;
    sortDirection: ColumnSortDirection | undefined;
    initialSortDirection?: ColumnSortDirectionTable;
    title: ReactNode;
    tooltip: string;
    onSortChange: (state: ISortableValue) => void;
    isMobile?: boolean;
}
export declare const SortableHeaderCell: ({ columnName, isSortedBy, sortDirection, title, tooltip, onSortChange, isMobile, initialSortDirection }: ISortableHeaderCellProps) => import("react/jsx-runtime").JSX.Element;
export {};
