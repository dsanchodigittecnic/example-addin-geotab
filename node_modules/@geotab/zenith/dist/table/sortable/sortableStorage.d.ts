import { ISortableStorage, ISortableValue } from "./interfaces";
export declare class SortableColumnsStorage implements ISortableStorage {
    private readonly defaultSortState?;
    private readonly storeKey;
    private readonly storage;
    constructor(pageName: string, defaultSortState?: ISortableValue | undefined);
    getItem(): ISortableValue | undefined;
    setItem(s: ISortableValue): void;
    clear(): void;
}
