export declare enum SelectionState {
    All = "all",
    CurrentPage = "page",
    Partial = "partial",
    None = "none"
}
export interface ISelectionValue {
    all: boolean;
    selected: string[];
}
