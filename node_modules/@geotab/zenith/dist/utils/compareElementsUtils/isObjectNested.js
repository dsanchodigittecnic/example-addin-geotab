"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isObjectNested = void 0;
const isArrayNested_1 = require("./isArrayNested");
const isObject_1 = require("./isObject");
const isPrimitive_1 = require("./isPrimitive");
const isObjectNested = (object) => {
    if (Array.isArray(object)) {
        return (0, isArrayNested_1.isArrayNested)(object);
    }
    if (!(0, isObject_1.isObject)(object)) {
        return false;
    }
    const keys = Object.keys(object);
    for (const key of keys) {
        if (!(0, isPrimitive_1.isPrimitive)((object)[key])) {
            return true;
        }
    }
    return false;
};
exports.isObjectNested = isObjectNested;
