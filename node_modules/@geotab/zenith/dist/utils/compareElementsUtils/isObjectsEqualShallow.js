"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isObjectsEqualShallow = void 0;
const isPrimitive_1 = require("./isPrimitive");
const isObjectsEqualShallow = (elem1, elem2) => {
    const aProps = Object.keys(elem1);
    const bProps = Object.keys(elem2);
    if (aProps.length !== bProps.length) {
        return false;
    }
    for (const propName of aProps) {
        if (!(0, isPrimitive_1.isPrimitive)(elem1[propName]) || !(0, isPrimitive_1.isPrimitive)(elem2[propName])) {
            return false;
        }
        if (elem1[propName] !== elem2[propName]) {
            return false;
        }
    }
    return true;
};
exports.isObjectsEqualShallow = isObjectsEqualShallow;
