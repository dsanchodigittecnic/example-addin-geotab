import { ReactNode } from "react";
import { IZenComponentProps } from "../../../commonHelpers/zenComponent";
import "./timeSelect.less";
export interface ITimeSelectItem {
    id: string;
    children: ReactNode;
}
export interface ITimeSelect extends IZenComponentProps {
    selected: string;
    items: ITimeSelectItem[];
    onSelect: (selected: string) => void;
    title: string;
}
export declare const TimeSelect: React.FC<ITimeSelect>;
