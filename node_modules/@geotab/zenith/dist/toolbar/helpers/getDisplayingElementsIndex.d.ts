import { ReactElement } from "react";
import { IToolbarContent } from "../toolbarContent";
import { IZenComponentProps } from "../../commonHelpers/zenComponent";
export type TChildren = ReactElement<IToolbarContent | IZenComponentProps>;
export interface IWidthProps {
    width: number;
    requiredWidth: number;
}
export interface IDisplayingElements {
    [key: string]: number[];
}
export declare const getDisplayingElementsIndex: (width: number, children: NodeListOf<HTMLElement>) => {
    displayingElements: {
        [x: string]: number[];
    };
    customizableMore: {
        [x: string]: number[];
    };
};
