"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDefaultProviderMode = void 0;
const getParentWindow_1 = require("../../utils/getParentWindow");
const getDefaultProviderMode = (mode) => {
    if (mode) {
        return mode;
    }
    const parent = (0, getParentWindow_1.getParentWindow)(window);
    const isTopWindow = window === parent;
    // if initialized in iframe
    if (!isTopWindow) {
        return "api";
    }
    // if "default" Provided already initialized on the same page
    if (parent.document.querySelector(".zen-alerts") || parent.document.querySelector(".zen-toasts")) {
        return "api";
    }
    return "default";
};
exports.getDefaultProviderMode = getDefaultProviderMode;
