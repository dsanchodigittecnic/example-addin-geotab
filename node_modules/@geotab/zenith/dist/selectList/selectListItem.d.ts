import * as React from "react";
import { CheckboxState } from "../checkbox/checkboxState";
import "./selectList.less";
import { IZenIdComponentProps } from "../commonHelpers/zenComponent";
export interface ISelectListItem extends IZenIdComponentProps {
    state?: CheckboxState;
    disabled?: boolean;
    showCheckbox?: boolean;
    focused?: boolean;
    onChange?: (id: string, state: CheckboxState) => void;
    onClick?: (id: string) => void;
    multiLevel?: boolean;
}
export declare const SelectListItem: React.FC<ISelectListItem>;
