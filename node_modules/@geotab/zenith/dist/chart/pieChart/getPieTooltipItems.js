"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPieTooltipItems = void 0;
const formatValue_1 = require("../plugins/legend/formatValue");
const getPieTooltipItems = (data, datasetIndex, index, options) => {
    if (data.datasets.length === 0) {
        return [];
    }
    if (datasetIndex === undefined || index === undefined) {
        return [];
    }
    const dataset = data.datasets[datasetIndex];
    // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
    if (!dataset) {
        return [];
    }
    const dataItem = dataset.data[index];
    if (!dataItem) {
        return [];
    }
    const labels = (data.labels || []);
    const colors = dataset.backgroundColor || [];
    return [{
            color: colors[index] && typeof colors[index] === "string" ? colors[index] : "",
            label: labels[index] && typeof labels[index] === "string" ? labels[index] : "",
            value: dataItem ? (0, formatValue_1.formatValue)(dataItem.toFixed(2), index, options === null || options === void 0 ? void 0 : options.unit) : undefined
        }];
};
exports.getPieTooltipItems = getPieTooltipItems;
