"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculatePositions = exports.positionOrder = void 0;
const arrowHeight = 12;
exports.positionOrder = {
    "left": ["top", "right", "bottom"],
    "top": ["left", "right", "bottom"],
    "right": ["top", "left", "bottom"],
    "bottom": ["left", "top", "right"]
};
exports.calculatePositions = {
    "top": {
        "className": "zen-chart-tooltip__arrow--bottom",
        calculate: (x, y, tooltipRect) => {
            const top = y - tooltipRect.height - arrowHeight;
            const left = x - tooltipRect.width / 2 - 1;
            return { top, left };
        }
    },
    "bottom": {
        "className": "zen-chart-tooltip__arrow--top",
        calculate: (x, y, tooltipRect) => {
            const top = y + arrowHeight;
            const left = x - tooltipRect.width / 2 - 1;
            return { top, left };
        }
    },
    "left": {
        "className": "zen-chart-tooltip__arrow--right",
        calculate: (x, y, tooltipRect) => {
            const top = y - tooltipRect.height / 2;
            const left = x - tooltipRect.width - arrowHeight;
            return { top, left };
        }
    },
    "right": {
        "className": "zen-chart-tooltip__arrow--left",
        calculate: (x, y, tooltipRect) => {
            const top = y - tooltipRect.height / 2;
            const left = x + arrowHeight;
            return { top, left };
        }
    }
};
