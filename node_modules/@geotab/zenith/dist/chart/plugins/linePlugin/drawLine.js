"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.drawLine = void 0;
const drawLine = (ctx, x, topY, bottomY) => {
    // Create a gradient
    const gradient = ctx.createLinearGradient(x, topY, x, bottomY);
    gradient.addColorStop(0, "rgba(116, 143, 170, 0.5)"); // #748FAA 50% opacity
    gradient.addColorStop(0.5, "#04355A"); // #04355A
    gradient.addColorStop(1, "rgba(116, 143, 170, 0.5)"); // #748FAA 50% opacity
    // Draw the vertical line with the gradient
    ctx.save();
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, bottomY);
    ctx.strokeStyle = gradient;
    ctx.lineWidth = 1;
    ctx.stroke();
    ctx.restore();
};
exports.drawLine = drawLine;
