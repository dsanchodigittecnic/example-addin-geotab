"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeRange = void 0;
const normalizeRange = (range) => {
    let min = range.min;
    let max = range.max;
    min = Math.floor(min);
    max = Math.ceil(max);
    if (min === max) {
        max = min + 1;
    }
    if (min > 0 && max > 0) {
        if (min / max < 0.3) {
            min = 0;
        }
    }
    return { min: min, max: max };
};
exports.normalizeRange = normalizeRange;
