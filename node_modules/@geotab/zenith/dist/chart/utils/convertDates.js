"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertDates = void 0;
const isDateString_1 = require("./isDateString");
const convertDates = (data, toLocalDateTime) => {
    for (let i = 0; i < data.datasets.length; i++) {
        const ds = data.datasets[i].data;
        for (let j = 0; j < ds.length; j++) {
            const point = ds[j];
            if (typeof point === "object" && typeof point.x === "string" && (0, isDateString_1.isDateString)(point.x)) {
                ds[j] = Object.assign(Object.assign({}, point), { x: toLocalDateTime(point.x) });
                continue;
            }
        }
    }
    return data;
};
exports.convertDates = convertDates;
