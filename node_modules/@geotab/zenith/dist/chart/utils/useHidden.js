"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useHidden = void 0;
const react_1 = require("react");
const useHidden = () => {
    const [hiddenDatasets, setHiddenDatasets] = (0, react_1.useState)(new Set());
    const isHidden = (0, react_1.useCallback)((index) => hiddenDatasets.has(index), [hiddenDatasets]);
    const toggleHidden = (0, react_1.useCallback)((index) => {
        const hidden = new Set(hiddenDatasets);
        if (hiddenDatasets.has(index)) {
            hidden.delete(index);
        }
        else {
            hidden.add(index);
        }
        setHiddenDatasets(hidden);
    }, [hiddenDatasets, setHiddenDatasets]);
    return { isHidden, toggleHidden };
};
exports.useHidden = useHidden;
