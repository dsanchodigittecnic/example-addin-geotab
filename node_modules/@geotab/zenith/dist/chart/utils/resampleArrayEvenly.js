"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resampleArrayEvenly = void 0;
function resampleArrayEvenly(originalArray, newLength) {
    const originalLength = originalArray.length;
    if (newLength <= 0 || originalLength === 0) {
        return [];
    }
    if (newLength >= originalLength) {
        return [...originalArray];
    }
    if (newLength === 1) {
        return [originalArray[0]];
    }
    const resampledArray = [];
    const step = (originalLength - 1) / (newLength - 1);
    for (let i = 0; i < newLength; i++) {
        const originalIndex = Math.round(i * step);
        resampledArray.push(originalArray[originalIndex]);
    }
    return resampledArray;
}
exports.resampleArrayEvenly = resampleArrayEvenly;
