"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getXAxisMeta = void 0;
const getMinDate = () => new Date(0);
const getMaxDate = () => new Date(8640000000000000);
const getXAxisMeta = (data, isTimedData) => {
    let minDate = getMaxDate();
    let maxDate = getMinDate();
    if (!isTimedData) {
        return { minDate: minDate.toISOString(), maxDate: maxDate.toISOString() };
    }
    for (let i = 0; i < data.datasets.length; i++) {
        const dataset = data.datasets[i];
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        if (dataset.data && dataset.data.length > 0) {
            for (let j = 0; j < dataset.data.length; j++) {
                const point = dataset.data[j];
                // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                if (point && typeof point === "object" && point.x) {
                    const date = point.x;
                    if (date.getTime() < new Date(minDate).getTime()) {
                        minDate = new Date(date.valueOf());
                    }
                    if (date.getTime() > new Date(maxDate).getTime()) {
                        maxDate = new Date(date.valueOf());
                    }
                }
            }
        }
    }
    return { minDate: minDate.toISOString(), maxDate: maxDate.toISOString() };
};
exports.getXAxisMeta = getXAxisMeta;
