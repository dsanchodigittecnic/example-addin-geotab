"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useChartTicks = void 0;
const react_1 = require("react");
const useChartTicks = () => {
    const chartRef = (0, react_1.useRef)(null);
    const [chartWidth, setChartWidth] = (0, react_1.useState)(718);
    (0, react_1.useEffect)(() => {
        if (!chartRef.current) {
            return () => { };
        }
        const applyWidth = () => {
            if (!chartRef.current) {
                return;
            }
            const { width } = chartRef.current.getBoundingClientRect();
            setChartWidth(width);
        };
        applyWidth();
        const handleResize = () => {
            applyWidth();
        };
        window.addEventListener("resize", handleResize);
        return () => {
            window.removeEventListener("resize", handleResize);
        };
    }, []);
    const ticks = (0, react_1.useMemo)(() => {
        const xlSize = 1102;
        const lSize = 718;
        const mSize = 526;
        if (chartWidth < mSize) {
            return 4;
        }
        if (chartWidth < lSize) {
            return 6;
        }
        if (chartWidth < xlSize) {
            return 8;
        }
        return 12;
    }, [chartWidth]);
    return { chartRef, ticks };
};
exports.useChartTicks = useChartTicks;
