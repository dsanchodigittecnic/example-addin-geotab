"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getGridClasses = void 0;
const constants_1 = require("./constants");
const gridClassPrefixes = [
    "zen-grid",
    constants_1.DESKTOP_PREFIX,
    constants_1.TABLET_PREFIX,
    constants_1.MOBILE_PREFIX,
    constants_1.GRID_COL_PREFIX
];
const getGridClasses = (className) => {
    const gridClasses = [];
    const restClasses = [];
    if (!className) {
        return {
            gridClasses,
            restClasses
        };
    }
    const classList = className.split(" ");
    classList.forEach((classItem) => {
        if (gridClassPrefixes.some((prefix) => classItem.indexOf(prefix) === 0)) {
            gridClasses.push(classItem);
        }
        else {
            restClasses.push(classItem);
        }
    });
    return {
        gridClasses,
        restClasses
    };
};
exports.getGridClasses = getGridClasses;
