"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.adjustGridState = void 0;
const constants_1 = require("./constants");
// eslint-disable-next-line complexity
const adjustGridState = (gridState) => {
    if (gridState.lg !== undefined && (gridState.lg < 1 || gridState.lg > constants_1.DESKTOP_COLS_NUMBER)) {
        gridState.lg = gridState.lg > constants_1.DESKTOP_COLS_NUMBER ? constants_1.DESKTOP_COLS_NUMBER : gridState.lg < 1 ? undefined : gridState.lg;
    }
    if (gridState.md !== undefined && (gridState.md < 1 || gridState.md > constants_1.TABLET_COLS_NUMBER)) {
        gridState.md = gridState.md > constants_1.TABLET_COLS_NUMBER ? constants_1.TABLET_COLS_NUMBER : gridState.md < 1 ? undefined : gridState.md;
    }
    if (gridState.sm !== undefined && (gridState.sm < 1 || gridState.sm > constants_1.MOBILE_COLS_NUMBER)) {
        gridState.sm = gridState.sm > constants_1.MOBILE_COLS_NUMBER ? constants_1.MOBILE_COLS_NUMBER : gridState.sm < 1 ? undefined : gridState.sm;
    }
    if (gridState.lgStart !== undefined && (gridState.lgStart < 1 || gridState.lgStart > constants_1.DESKTOP_COLS_NUMBER)) {
        gridState.lgStart = gridState.lgStart > constants_1.DESKTOP_COLS_NUMBER ? constants_1.DESKTOP_COLS_NUMBER : gridState.lgStart < 1 ? undefined : gridState.lgStart;
    }
    if (gridState.mdStart !== undefined && (gridState.mdStart < 1 || gridState.mdStart > constants_1.TABLET_COLS_NUMBER)) {
        gridState.mdStart = gridState.mdStart > constants_1.TABLET_COLS_NUMBER ? constants_1.TABLET_COLS_NUMBER : gridState.mdStart < 1 ? undefined : gridState.mdStart;
    }
    if (gridState.smStart !== undefined && (gridState.smStart < 1 || gridState.smStart > constants_1.MOBILE_COLS_NUMBER)) {
        gridState.smStart = gridState.smStart > constants_1.MOBILE_COLS_NUMBER ? constants_1.MOBILE_COLS_NUMBER : gridState.smStart < 1 ? undefined : gridState.smStart;
    }
};
exports.adjustGridState = adjustGridState;
