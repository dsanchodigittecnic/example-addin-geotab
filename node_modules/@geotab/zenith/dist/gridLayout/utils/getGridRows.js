"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getGridRows = void 0;
const getGridRows = (topValueArr, heightArr) => {
    const grouped = [];
    if (topValueArr.length === 0) {
        return grouped;
    }
    let currentGroup = [{
            index: 0,
            height: heightArr[0]
        }];
    for (let i = 1; i < topValueArr.length; i++) {
        if (topValueArr[i] === topValueArr[i - 1]) {
            currentGroup.push({
                index: i,
                height: heightArr[i]
            });
        }
        else {
            grouped.push(currentGroup);
            currentGroup = [{
                    index: i,
                    height: heightArr[i]
                }];
        }
    }
    grouped.push(currentGroup);
    return grouped;
};
exports.getGridRows = getGridRows;
