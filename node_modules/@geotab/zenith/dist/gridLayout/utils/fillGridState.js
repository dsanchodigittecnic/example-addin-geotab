"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fillGridState = void 0;
const constants_1 = require("./constants");
// eslint-disable-next-line complexity
const fillGridState = (gridState) => {
    const { lg, md, sm } = gridState;
    // If no values provided
    if (!lg && !md && !sm) {
        return;
    }
    // If all values provided
    if (lg && md && sm) {
        return;
    }
    // Only one value provided
    if (lg && !md && !sm) {
        gridState.md = lg > constants_1.TABLET_COLS_NUMBER ? constants_1.TABLET_COLS_NUMBER : lg;
        gridState.sm = lg > constants_1.MOBILE_COLS_NUMBER ? constants_1.MOBILE_COLS_NUMBER : lg;
        return;
    }
    if (md && !lg && !sm) {
        gridState.lg = md;
        gridState.sm = md > constants_1.MOBILE_COLS_NUMBER ? constants_1.MOBILE_COLS_NUMBER : md;
        return;
    }
    if (sm && !lg && !md) {
        gridState.lg = sm;
        gridState.md = sm;
        return;
    }
    // If two values provided
    if (lg && md && !sm) {
        gridState.sm = md > constants_1.MOBILE_COLS_NUMBER ? constants_1.MOBILE_COLS_NUMBER : md;
        return;
    }
    if (lg && sm && !md) {
        gridState.md = lg > constants_1.TABLET_COLS_NUMBER ? constants_1.TABLET_COLS_NUMBER : lg;
        return;
    }
    if (md && sm && !lg) {
        gridState.lg = md;
        return;
    }
};
exports.fillGridState = fillGridState;
