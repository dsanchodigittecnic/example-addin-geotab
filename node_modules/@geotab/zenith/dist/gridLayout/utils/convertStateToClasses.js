"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertStateToClasses = void 0;
const constants_1 = require("./constants");
const convertStateToClasses = (gridState) => {
    const { lg, md, sm, lgStart, mdStart, smStart } = gridState;
    const classes = [];
    if (lg) {
        classes.push(`${constants_1.DESKTOP_PREFIX}${lg}`);
    }
    if (md) {
        classes.push(`${constants_1.TABLET_PREFIX}${md}`);
    }
    if (sm) {
        classes.push(`${constants_1.MOBILE_PREFIX}${sm}`);
    }
    if (lgStart) {
        classes.push(`${constants_1.DESKTOP_START_PREFIX}${lgStart}`);
    }
    if (mdStart) {
        classes.push(`${constants_1.TABLET_START_PREFIX}${mdStart}`);
    }
    if (smStart) {
        classes.push(`${constants_1.MOBILE_START_PREFIX}${smStart}`);
    }
    return classes;
};
exports.convertStateToClasses = convertStateToClasses;
