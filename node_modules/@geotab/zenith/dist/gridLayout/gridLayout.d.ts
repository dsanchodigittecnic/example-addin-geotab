import { PropsWithChildren } from "react";
import { IZenComponentProps } from "../commonHelpers/zenComponent";
import "./gridLayout.less";
export interface IGridLayout extends PropsWithChildren, IZenComponentProps {
    /**
     * @property if true, the grid items will stretch to fill the available space (height).
     */
    stretch?: boolean;
}
export declare const GridLayout: React.FC<IGridLayout>;
