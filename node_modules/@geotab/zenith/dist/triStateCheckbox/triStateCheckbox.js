"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TriStateCheckbox = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = require("react");
const checkboxState_1 = require("../checkbox/checkboxState");
const checkboxUtils_1 = require("../checkbox/checkboxUtils");
const TriStateCheckbox = (_a) => {
    var { checked, indeterminate } = _a, rest = __rest(_a, ["checked", "indeterminate"]);
    const ref = (0, react_1.useRef)(null);
    let checkedValue = false;
    let indeterminateValue = false;
    const state = (0, checkboxUtils_1.getCheckboxState)(checked, indeterminate);
    if (state === checkboxState_1.CheckboxState.On) {
        checkedValue = true;
        indeterminateValue = false;
    }
    else if (state === checkboxState_1.CheckboxState.Indeterminate) {
        checkedValue = false;
        indeterminateValue = true;
    }
    else {
        checkedValue = false;
        indeterminateValue = false;
    }
    (0, react_1.useEffect)(() => {
        if (!ref.current) {
            return;
        }
        ref.current.indeterminate = indeterminateValue;
    }, [indeterminateValue]);
    return (0, jsx_runtime_1.jsx)("input", Object.assign({ ref: ref, checked: checkedValue, type: "checkbox" }, rest));
};
exports.TriStateCheckbox = TriStateCheckbox;
