"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findNextFocusable = void 0;
const focusableSelector_1 = require("../../utils/focusableSelector");
const findNextFocusable = (element) => {
    if (!element) {
        return undefined;
    }
    const listItemEl = element.closest("li");
    let nextElement = listItemEl.nextElementSibling;
    while (nextElement) {
        if (nextElement.querySelector(focusableSelector_1.FOCUSABLE_SELECTOR)) {
            return nextElement.querySelector(focusableSelector_1.FOCUSABLE_SELECTOR);
        }
        nextElement = nextElement.nextElementSibling;
    }
    return listItemEl.parentNode.querySelector(focusableSelector_1.FOCUSABLE_SELECTOR);
};
exports.findNextFocusable = findNextFocusable;
