"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findPrevFocusable = void 0;
const focusableSelector_1 = require("../../utils/focusableSelector");
function findPrevFocusable(element) {
    if (!element) {
        return undefined;
    }
    const listItemEl = element.closest("li");
    let prevElement = listItemEl.previousElementSibling;
    while (prevElement) {
        if (prevElement.querySelector(focusableSelector_1.FOCUSABLE_SELECTOR)) {
            return prevElement.querySelector(focusableSelector_1.FOCUSABLE_SELECTOR);
        }
        prevElement = prevElement.previousElementSibling;
    }
    const focusable = listItemEl.parentNode.querySelectorAll(focusableSelector_1.FOCUSABLE_SELECTOR);
    return focusable[focusable.length - 1];
}
exports.findPrevFocusable = findPrevFocusable;
