"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TRANSLATIONS = exports.Combobox = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = __importStar(require("react"));
const searchableField_1 = require("../searchableField/searchableField");
const searchList_1 = __importStar(require("../searchList/searchList"));
const absolute_1 = require("../absolute/absolute");
const comboboxNavigation_1 = require("./comboboxNavigation");
const classNames_1 = require("../commonHelpers/classNames/classNames");
/**
 * @deprecated - will be removed in future releases
*/
const Combobox = ({ getData, inputId, placeholder, dialogAriaLabel, title, disabled, onSelect, width, className, selection, inline = true, fullWidth }) => {
    const ariaComboboxId = "listbox";
    const ariaControlId = react_1.default.useId();
    const listId = react_1.default.useId();
    const [newListData, setNewListData] = (0, react_1.useState)([]);
    const [showWaiting, setShowWaiting] = (0, react_1.useState)(false);
    const [showList, setShowList] = (0, react_1.useState)(() => false);
    const [showEmptyList, setShowEmptyList] = (0, react_1.useState)(false);
    const [value, setInputValue] = (0, react_1.useState)("");
    const abortToken = (0, react_1.useRef)(new AbortController());
    const triggerRef = (0, react_1.useRef)(null);
    const comboboxRef = (0, react_1.useRef)(null);
    const inputRef = (0, react_1.useRef)(null);
    const searchListParentRef = (0, react_1.useRef)(null);
    const handleHighLevelKeyboardNavigation = (0, react_1.useCallback)((key, e) => {
        if (key === "Escape") {
            !showList && setInputValue("");
            setShowList(false);
            setShowWaiting(false);
            setShowEmptyList(false);
            return;
        }
        if (key === "Tab") {
            setShowList(false);
            setShowWaiting(false);
            setShowEmptyList(false);
            return;
        }
        if (key === "ArrowDown" && showList && !showWaiting && !showEmptyList) {
            e.preventDefault();
            return;
        }
        if (key === "ArrowUp" && showList && !showWaiting && !showEmptyList) {
            e.preventDefault();
            return;
        }
    }, [showList, showWaiting, showEmptyList]);
    const showListPopup = (newVal) => {
        setShowList(newVal);
    };
    const showWaitingPopup = (newVal) => {
        setShowWaiting(newVal);
    };
    const showEmptyPopup = (newVal) => {
        setShowEmptyList(newVal);
    };
    const controlData = (0, react_1.useCallback)((signal, searchValue) => {
        showWaitingPopup(true);
        showEmptyPopup(false);
        showListPopup(false);
        getData(signal, searchValue).then(data => {
            setNewListData(data);
            showWaitingPopup(false);
            data.length ? showListPopup(true) : showEmptyPopup(true);
        }).catch(() => {
            showListPopup(false);
            showEmptyPopup(false);
            showWaitingPopup(false);
        });
    }, [getData]);
    const onOpenSearch = (0, react_1.useCallback)(() => {
        const currentAbort = abortToken.current;
        if (showList) {
            showListPopup(false);
            return;
        }
        controlData(currentAbort.signal, value);
    }, [controlData, showList, value]);
    const onSingleChange = (0, react_1.useCallback)((selected, unselect) => {
        var _a;
        const newSetSelection = new Set(selection);
        const action = newSetSelection.has(selected) ? "delete" : "add";
        unselect && unselect.forEach(item => newSetSelection.delete(item));
        newSetSelection[action](selected);
        onSelect([...newSetSelection]);
        (_a = inputRef.current) === null || _a === void 0 ? void 0 : _a.focus();
    }, [onSelect, selection]);
    const subscriptionCondition = (trigger) => trigger.current !== null;
    const onFocus = () => {
        !showWaiting && !showList && !showEmptyList && controlData(undefined, value);
    };
    const onInput = (val) => {
        abortToken.current.abort();
        abortToken.current = new AbortController();
        controlData(abortToken.current.signal, val);
        setInputValue(val);
    };
    const containerClassName = (0, classNames_1.classNames)([
        "zen-combobox",
        "zen-combobox__container",
        className || ""
    ]);
    (0, comboboxNavigation_1.useComboboxNavigation)(handleHighLevelKeyboardNavigation, comboboxRef, subscriptionCondition);
    (0, comboboxNavigation_1.useClickOutside)(comboboxRef, () => {
        if (inline) {
            showListPopup(false);
        }
    });
    (0, comboboxNavigation_1.useClickOutside)(searchListParentRef, () => {
        if (!inline) {
            showListPopup(false);
        }
    });
    (0, react_1.useEffect)(() => {
        const disableKeyCode = ["ArrowDown", "ArrowUp"];
        const input = inputRef.current;
        const handler = (e) => {
            if (disableKeyCode.some(key => e.key === key)) {
                e.preventDefault();
            }
        };
        input === null || input === void 0 ? void 0 : input.addEventListener("keydown", handler);
        return () => {
            input === null || input === void 0 ? void 0 : input.removeEventListener("keydown", handler);
        };
    }, []);
    (0, react_1.useEffect)(() => {
        var _a;
        if (showList) {
            (_a = inputRef.current) === null || _a === void 0 ? void 0 : _a.focus();
        }
    }, [showList]);
    const popupAlignment = inline ? undefined : "bottom-right";
    const openChange = (0, react_1.useCallback)(() => {
        if (showWaiting) {
            return setShowWaiting;
        }
        if (showEmptyList) {
            return setShowEmptyList;
        }
        if (showList) {
            return setShowList;
        }
        return () => { };
    }, [showWaiting, showEmptyList, showList]);
    const getPopupContent = (0, react_1.useMemo)(() => {
        if (showWaiting) {
            return (0, jsx_runtime_1.jsx)(searchList_1.WaitingList, {});
        }
        if (showEmptyList) {
            return (0, jsx_runtime_1.jsx)(searchList_1.EmptyList, {});
        }
        if (showList) {
            return (0, jsx_runtime_1.jsx)("div", { ref: searchListParentRef, children: (0, jsx_runtime_1.jsx)(searchList_1.default, { selected: selection, parentRef: comboboxRef, listId: listId, listData: newListData, onChange: onSingleChange }) });
        }
        return null;
    }, [showWaiting, showEmptyList, showList, selection, newListData, onSingleChange, listId]);
    return (0, jsx_runtime_1.jsxs)("div", { className: containerClassName, ref: comboboxRef, children: [(0, jsx_runtime_1.jsx)(searchableField_1.SearchableField, { width: width, inputId: inputId, inputRef: inputRef, title: title, placeholder: placeholder, onChange: onInput, onOpenSearch: onOpenSearch, onFocus: onFocus, openSearch: showList || showWaiting || showEmptyList, disabled: disabled, ariaAutocomplete: "list", ariaControls: ariaControlId, ariaOwns: ariaComboboxId, triggerRef: inline ? undefined : triggerRef, fullWidth: fullWidth, value: value }), (0, jsx_runtime_1.jsx)(absolute_1.Absolute, { alignment: popupAlignment, onOpenChange: openChange, className: "zen-combobox-popup zen-shadow-dropdown-default", stateFullChilds: showList, inline: inline, isOpen: true, id: ariaControlId, triggerRef: triggerRef, ariaLabel: dialogAriaLabel || placeholder, role: "region", children: getPopupContent })] });
};
exports.Combobox = Combobox;
exports.TRANSLATIONS = [
    ...searchableField_1.TRANSLATIONS,
    ...searchList_1.TRANSLATIONS
];
