import { FunctionComponent } from "react";
import { IIcon } from "../icons/icon";
export type TListType = "standard" | "data" | "compact";
interface IListItemBasicAction {
    name: string;
    icon?: FunctionComponent<IIcon>;
}
export interface IListItemButton extends IListItemBasicAction {
    onClick: () => void;
}
export interface IListItemLink extends IListItemBasicAction {
    link: string;
}
export type TListItemAction = IListItemButton | IListItemLink;
export declare const isButtonAction: (action: TListItemAction) => action is IListItemButton;
export {};
