import * as React from "react";
import { IButton } from "../button/button";
import { TSvgIconSize } from "../svgIcon/svgIconSize";
import "./textIconButton.less";
import { IIcon } from "../icons/icon";
export declare const ButtonIconPosition: {
    readonly Start: "start";
    readonly End: "end";
};
export type TButtonIconPosition = typeof ButtonIconPosition[keyof typeof ButtonIconPosition];
export interface ITextIconButton extends IButton {
    icon: React.FC<IIcon>;
    iconPosition?: TButtonIconPosition;
    iconClasses?: string;
    iconSize?: TSvgIconSize;
    contentClasses?: string;
    tabIndex?: number;
    role?: string;
}
export declare const TextIconButton: React.ForwardRefExoticComponent<ITextIconButton & React.RefAttributes<HTMLButtonElement>>;
