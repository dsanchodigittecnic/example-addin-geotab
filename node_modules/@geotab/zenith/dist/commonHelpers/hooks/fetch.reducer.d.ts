import { Reducer } from "react";
import { TFetchAction } from "./fetch.action";
import { IFetchState } from "./useFetch";
export interface IFetchReducer<T> extends Reducer<IFetchState<T>, TFetchAction<T>> {
}
export declare function fetchReducer<T>(state: IFetchState<T>, action: TFetchAction<T>): {
    isLoading: boolean;
    data?: T | undefined;
    error?: Error | undefined;
};
