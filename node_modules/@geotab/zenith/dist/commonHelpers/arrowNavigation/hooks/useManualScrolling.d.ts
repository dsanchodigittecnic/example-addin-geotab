import { RefObject, MouseEvent } from "react";
export declare const useManualScrolling: <E extends HTMLElement>({ elementRef }: {
    elementRef: RefObject<E>;
}) => {
    scroll: (event: MouseEvent<HTMLElement>) => void;
};
