"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isFunction = exports.getComponentText = void 0;
const react_1 = require("react");
const getComponentText = (comp) => {
    const buffer = [];
    if (comp) {
        if (typeof comp === "string" || typeof comp === "number") {
            buffer.push(comp.toString());
        }
        else if (typeof comp === "object") {
            const children = comp.props.children;
            buffer.push(...react_1.Children.toArray(children).map(child => (0, exports.getComponentText)(child)));
        }
    }
    return buffer.join(" ");
};
exports.getComponentText = getComponentText;
const isFunction = (obj) => typeof obj === "function";
exports.isFunction = isFunction;
