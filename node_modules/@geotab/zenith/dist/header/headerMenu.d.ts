import { FC } from "react";
import { IButton } from "../button/button";
import { TSvgIconSize } from "../svgIcon/svgIconSize";
import { IIcon } from "../icons/icon";
import "./headerButton.less";
import { TAlignment } from "../absolute/absolute";
export interface IHeaderMenu extends Omit<IButton, "onClick"> {
    id: string;
    name: string;
    important?: boolean;
    inContextMenu?: boolean;
    icon?: FC<IIcon>;
    iconSize?: TSvgIconSize;
    alignment?: TAlignment;
    onlyIcon?: boolean;
}
export declare const HeaderMenuDisplayName = "HeaderMenu";
export declare const HeaderMenu: FC<IHeaderMenu>;
