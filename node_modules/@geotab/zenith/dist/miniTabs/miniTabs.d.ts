import React, { ReactNode } from "react";
import "./miniTabs.less";
import { MiniTabsItem } from "./miniTabsItem";
export interface IMiniTabElement {
    name: string;
    content: ReactNode;
    id: string;
}
export declare enum MiniTabsSize {
    Small = "small",
    Medium = "medium",
    Large = "large",
    XLarge = "xlarge"
}
export interface IMiniTabs {
    activeTabId: string;
    onTabChange: (newTabId: string) => void;
    className?: string;
    size?: MiniTabsSize;
    children: React.ReactElement<typeof MiniTabsItem> | React.ReactElement<typeof MiniTabsItem>[];
}
export declare const MiniTabs: React.FC<IMiniTabs>;
