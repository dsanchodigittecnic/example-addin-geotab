import { IZenComponentProps } from "../commonHelpers/zenComponent";
import { TIconSize } from "./iconSize";
export interface IIcon extends IZenComponentProps {
    size?: TIconSize;
    focusable?: boolean;
    title?: string;
    description?: string;
}
