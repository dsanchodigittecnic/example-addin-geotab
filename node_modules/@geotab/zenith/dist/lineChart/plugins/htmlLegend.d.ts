import { Chart, ChartDataset, LegendItem, UpdateMode } from "chart.js/auto";
import "./htmlLegend.less";
type TLegendRenderer = (item: LegendItem, dataset: ChartDataset) => string;
export interface ILegendContainer {
    containerID: string;
    labels: string[];
    renderer?: TLegendRenderer;
}
export interface ILegendOptions {
    containerID: string;
    renderer?: TLegendRenderer;
    containers?: ILegendContainer[];
    isDrive?: boolean;
}
export declare const LEGEND_PLUGIN_ID = "htmlLegend";
export declare const htmlLegendPlugin: () => {
    id: string;
    isDrive: boolean;
    afterUpdate(chart: Chart, _args: {
        mode: UpdateMode;
    }, options: ILegendOptions): void;
};
export {};
