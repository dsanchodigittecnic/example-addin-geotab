import "./sortableColumn.less";
import { FC, ReactNode } from "react";
import { IListColumnWrapper } from "../../listColumn";
import { ISortableColumnSettings } from "../sortableColumnsStorage";
import { ColumnSortDirection } from "./columnSortDirection";
import { ColumnSortDirection as ColumnSortDirectionTable } from "../../../table/table";
import { IEntityWithId } from "../../../commonHelpers/entity";
interface ISortableColumnOptions {
    isSortedBy: boolean;
    sortDirection: ColumnSortDirection;
    initialSortDirection?: ColumnSortDirectionTable;
    /**
    * @deprecated
    * @param translate - will be removed in the next major release
    */
    translate?: (s: string) => string;
}
interface ISortableHeaderCellProps {
    columnName: string;
    isSortedBy: boolean;
    sortDirection: ColumnSortDirection;
    title: ReactNode;
    tooltip: string;
    onSortChange: (state: ISortableColumnSettings) => void;
    isMobile?: boolean;
    initialSortDirection?: ColumnSortDirectionTable;
}
export declare const SortableHeaderCell: FC<ISortableHeaderCellProps>;
export declare class SortableColumnWrapper<T extends IEntityWithId> implements IListColumnWrapper<T, T> {
    private readonly columnName;
    private readonly options;
    private readonly columnTitle;
    private readonly onSort;
    constructor(columnName: string, options: Partial<ISortableColumnOptions>, columnTitle: string, onSort: (newSortSettings: ISortableColumnSettings) => void);
    renderHeader(title: ReactNode): import("react/jsx-runtime").JSX.Element;
    render(value: ReactNode, _: T | undefined): ReactNode;
    static translations: string[];
}
export {};
