import { IEntityWithId } from "../../commonHelpers/entity";
import { IListColumn, IListColumnMeta } from "../listColumn";
import { IRowEntity } from "../row/row";
declare const DEFAULT_META: {
    defaultWidth: number | undefined;
    defaultValue: string;
    lazyLoad: boolean;
    visibleOnHover: boolean;
};
export interface IListColumnStrict<T, N> extends Required<IListColumn<T, N>> {
    meta: IListColumnMeta<T, N> & typeof DEFAULT_META;
}
export declare const toBasicColumn: <T extends IRowEntity<N>, N extends IEntityWithId>(column: IListColumn<T, N>) => IListColumnStrict<T, N>;
export {};
