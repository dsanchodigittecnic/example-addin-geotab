import { IEntityWithId } from "../../commonHelpers/entity";
import { SimpleEventHandler } from "../../commonHelpers/simpleEventHandler";
import "./flexibleColumnsResizer.less";
interface IColumn extends IEntityWithId {
    meta?: {
        defaultWidth?: number;
    };
}
export interface IFlexibleColumnResizer {
    add(columns: IColumn[]): void;
    remove(): void;
    showResizerForColumn(columnIndex: number): void;
    hideResizer(): void;
    attach(event: "resize", handler: (args: [number, number]) => void): this;
    detach(event: "resize", handler?: (args: [number, number]) => void): this;
}
export declare class FlexibleColumnResizer extends SimpleEventHandler implements IFlexibleColumnResizer {
    private readonly listRef;
    private currentWidth;
    private pageX;
    private offsetLeft;
    private resizer;
    private columnIndex;
    private resizers;
    private columns;
    private createResizer;
    private readonly preventScroll;
    private readonly stopResizing;
    private readonly resizeColumn;
    private readonly endColumn;
    private readonly startColumn;
    constructor(listRef: HTMLElement);
    showResizerForColumn(columnIndex: number): void;
    hideResizer(): void;
    add(columns: IColumn[]): void;
    remove(): void;
}
export {};
