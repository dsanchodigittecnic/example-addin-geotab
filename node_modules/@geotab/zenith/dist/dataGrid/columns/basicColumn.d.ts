import { ReactNode } from "react";
import { IListColumnBasic, TFeedResult } from "../listColumn";
export declare const isPromiseLike: (value: any) => value is PromiseLike<unknown>;
export interface IColumnOptions<T, U> {
    name?: string;
    defaultValue?: U;
    render?: (entity: T, isNested: boolean) => U | PromiseLike<U | undefined> | undefined;
    renderHeader?: (title: string) => ReactNode;
}
export declare abstract class BasicColumn<T, U, N = T> implements IListColumnBasic<T, N> {
    private readonly column;
    private getValue;
    private getHeaderTitle;
    private renderCellAsync;
    protected renderHeaderCell(title: ReactNode): ReactNode;
    protected renderFeedLabel(title: ReactNode): ReactNode;
    protected renderFeedValue(value: U, entity: T | N, isNested?: boolean): ReactNode | PromiseLike<ReactNode>;
    protected abstract renderCell(value: U, entity: T | N, isNested: boolean): ReactNode | PromiseLike<ReactNode>;
    constructor(column: IColumnOptions<T, U>);
    render(entity: T | N, isNested?: boolean): string | number | boolean | import("react").ReactElement<any, string | import("react").JSXElementConstructor<any>> | Iterable<ReactNode> | PromiseLike<ReactNode> | null | undefined;
    renderHeader(title: string): ReactNode;
    renderFeed(title: string, entity: T | N, isNested?: boolean): TFeedResult;
}
