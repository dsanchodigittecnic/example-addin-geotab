import { ReactNode } from "react";
import { IListColumnBasic } from "../../listColumn";
import { BasicColumn } from "../basicColumn";
import { IMainColumnOptions } from "./mainColumnWrapper";
export interface IMainColumn<T, N = T> extends IListColumnBasic<T, N> {
    renderIcon(entity: T | N, isNested: boolean): ReactNode;
}
export declare class MainColumn<T, N = T> extends BasicColumn<T, ReactNode, N> implements IMainColumn<T, N> {
    private readonly columnId;
    private readonly isMobile;
    private readonly wrapper;
    protected renderCell(elt: ReactNode, entity: T, isNested?: boolean): ReactNode;
    protected renderHeaderCell(title: ReactNode): ReactNode;
    constructor(columnId: string, isMobile: boolean, options: IMainColumnOptions<T | N>, renderHeader?: (title: ReactNode) => ReactNode);
    renderFeed(_: string, entity: T | N, isNested?: boolean): [ReactNode, ReactNode];
    renderIcon(entity: T, isNested: boolean): ReactNode;
}
