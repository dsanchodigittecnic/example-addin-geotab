import { FC, ReactNode } from "react";
import { IListColumnWrapper } from "../../listColumn";
import { IColumnLink } from "../basicTypes";
import { IIcon } from "../../../icons/icon";
import "./actionLinkColumn.less";
import { IAction } from "../actionsColumn/actionsColumn";
import { IRowEntity } from "../../row/row";
import { IEntityWithId } from "../../../commonHelpers/entity";
export interface IActionLinkColumnOptions<T extends IRowEntity<N>, N extends IEntityWithId> {
    createIcon: (entity: T | N) => FC<IIcon>;
    createLink: (entity: T | N) => IColumnLink | string;
    createTitle: (entity: T | N) => string;
    condition?: (entity: T | N) => boolean;
    actions?: IAction<T | N>[];
    isMobile?: boolean;
}
export declare class ActionLinkColumnWrapper<T extends IRowEntity<N>, N extends IEntityWithId = T> implements IListColumnWrapper<T, N> {
    private readonly options;
    constructor(options: IActionLinkColumnOptions<T, N>);
    render(elt: ReactNode, entity?: T | N): import("react/jsx-runtime").JSX.Element | null;
    renderHeader(title: ReactNode): import("react/jsx-runtime").JSX.Element;
}
