import { IZenIdComponentProps } from "../../commonHelpers/zenComponent";
import { IEntityWithId } from "../../commonHelpers/entity";
import { IListColumnStrict } from "../extensions/toBasicColumn";
export interface IRowEntity<NestedType extends IEntityWithId> extends IEntityWithId {
    children?: NestedType[];
}
export interface IDataGridRow<T extends IRowEntity<N>, N extends IEntityWithId> extends IZenIdComponentProps {
    columns: IListColumnStrict<T, N>[];
    entity: T;
    isLastRow: boolean;
    visibleRows: Set<string>;
    flexibleColumnWidth: number;
    expanded?: boolean;
    onExpandedChange?: (isExpanded: boolean) => void;
    isTreeGrid: boolean;
    ariaPosinset: number;
    ariaSetsize: number;
}
export declare const MIN_CELL_WIDTH = 100;
export declare const DataGridRow: <T extends IRowEntity<N>, N extends IEntityWithId = T>({ id, entity, columns, visibleRows, flexibleColumnWidth, isLastRow, className, expanded, onExpandedChange, isTreeGrid, ariaPosinset, ariaSetsize }: IDataGridRow<T, N>) => import("react/jsx-runtime").JSX.Element;
