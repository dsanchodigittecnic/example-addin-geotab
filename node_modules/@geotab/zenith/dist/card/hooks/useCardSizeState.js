"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useCardSizeState = void 0;
const isAbsoluteSize_1 = require("../../cardContainer/helpers/isAbsoluteSize");
const deviceType_1 = require("../../commonHelpers/hooks/deviceType");
const useCardSizeState = (deviceType, scrollableContent, fullWidth, fullHeight, autoHeight, size) => {
    const isMobile = deviceType === deviceType_1.DeviceType.Mobile;
    const isAbsolute = (0, isAbsoluteSize_1.isAbsoluteSize)(size);
    const isRelative = !isAbsolute;
    let isAutoHeight = autoHeight;
    let isScrollable = scrollableContent && !autoHeight;
    let isFullWidth = fullWidth;
    let isFullHeight = fullHeight && !autoHeight;
    if (isRelative) {
        if (isMobile) {
            isScrollable = false;
            isFullHeight = false;
        }
    }
    else if (isMobile) {
        isScrollable = false;
        isFullHeight = false;
        isAutoHeight = !size.height || isAutoHeight;
        isFullWidth = !size.width || isFullWidth;
    }
    return {
        isAutoHeight,
        isScrollable,
        isFullWidth,
        isFullHeight
    };
};
exports.useCardSizeState = useCardSizeState;
