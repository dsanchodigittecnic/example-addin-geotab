import { TContainerSize, IAbsoluteSize } from "../../cardContainer/cardContainer";
import { DeviceType } from "../../commonHelpers/hooks/deviceType";
export declare const useCardSizeState: (deviceType: DeviceType, scrollableContent: boolean, fullWidth: boolean, fullHeight: boolean, autoHeight: boolean, size: TContainerSize | IAbsoluteSize) => {
    isAutoHeight: boolean;
    isScrollable: boolean;
    isFullWidth: boolean;
    isFullHeight: boolean;
};
