import { FC, ReactNode } from "react";
import { TooltipSize } from "../../tooltip/tooltip";
import { TAlignment } from "../../absolute/absolute";
import { IZenComponentProps } from "../../commonHelpers/zenComponent";
import "./status.less";
import "./statusIcon.less";
import "./statusButton.less";
import "./statusTooltipMobile.less";
export type TCardStatus = "info" | "error" | "warning" | "ok";
interface IStatus extends IZenComponentProps {
    status?: TCardStatus;
    isMobile: boolean;
    tooltip?: ReactNode;
    tooltipAlignment?: TAlignment;
    tooltipSize?: TooltipSize;
    title?: string;
    /**
    * @deprecated
    * @param translate - will be removed in the next major release
    */
    translate?: (s: string) => string;
}
export declare const Status: FC<IStatus>;
export {};
