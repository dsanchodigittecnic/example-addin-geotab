import { jsx as _jsx } from "react/jsx-runtime";
import { useLanguage } from "../../../utils/localization/useLanguage";
import { NavItem } from "../../navItem/navItem";
import { IconSettings4 } from "../../../icons/iconSettings4";
/**
 * @beta This component is not fully ready yet and may change in future releases.
 */
export const NavFooterAction = ({ children }) => {
    const { translate } = useLanguage();
    const itemsArray = children ? (Array.isArray(children) ? children : [children]) : [];
    const filteredItems = itemsArray.filter(child => {
        const element = child;
        return element.type && element.type.displayName === "NavItem";
    });
    if (!filteredItems.length) {
        return null;
    }
    // TODO: set menuAlignment value to "right-bottom" when positioning is fixed
    return _jsx(NavItem, { primaryIcon: IconSettings4, title: translate("Configure"), menuAlignment: "right-bottom", children: filteredItems });
};
NavFooterAction.displayName = "NavFooterAction";
