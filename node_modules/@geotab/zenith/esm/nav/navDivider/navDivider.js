import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../../commonHelpers/classNames/classNames";
/**
 * @beta This component is not fully ready yet and may change in future releases.
 */
export const NavDivider = ({ className }) => {
    const dividerClassNames = classNames([
        "zen-nav-divider",
        className !== null && className !== void 0 ? className : ""
    ]);
    return _jsx("div", { className: dividerClassNames, role: "separator" });
};
