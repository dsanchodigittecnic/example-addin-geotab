import { jsx as _jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { Tooltip } from "../tooltip/tooltip";
import { IconInfoCircle } from "../icons/iconInfoCircle";
import { useLanguage } from "../utils/localization/useLanguage";
import { classNames } from "../commonHelpers/classNames/classNames";
export const AdvancedGroupsFilterSectionTooltip = ({ ariaLabel, className = "", size = "auto", alignment = "top", text }) => {
    const { translate } = useLanguage();
    const triggerRef = useRef(null);
    const defaultTooltipText = "Use multiple operators when searching for a specific group. For example, filtering for assets that belong to the Tractor group and the Toronto group displays a list of tractors that belong to the Toronto branch.";
    const getTrigger = (currentRef, ariaLbl) => (_jsx("button", { type: "button", "aria-label": ariaLbl, className: "zen-advanced-groups-filter-section-tooltip__trigger", ref: currentRef, children: _jsx(IconInfoCircle, { className: "zen-advanced-groups-filter-section-tooltip__trigger-icon", size: "large" }) }));
    return (_jsx(Tooltip, { className: classNames(["zen-advanced-groups-filter-section-tooltip__item", className || ""]), size: size, alignment: alignment, trigger: getTrigger(triggerRef, translate(ariaLabel)), children: text || translate(defaultTooltipText) }));
};
export const TRANSLATIONS = [
    "Use multiple operators when searching for a specific group. For example, filtering for assets that belong to the Tractor group and the Toronto group displays a list of tractors that belong to the Toronto branch."
];
