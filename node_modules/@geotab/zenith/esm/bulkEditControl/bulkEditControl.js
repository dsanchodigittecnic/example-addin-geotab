import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { Select } from "../select/select";
import { useLanguage } from "../utils/localization/useLanguage";
export var BulkEditItemType;
(function (BulkEditItemType) {
    BulkEditItemType["SelectVisible"] = "selectVisible";
    BulkEditItemType["SelectIndividual"] = "selectIndividual";
    BulkEditItemType["SelectAllInFilter"] = "selectAllInFilter";
    BulkEditItemType["SelectNone"] = "selectNone";
})(BulkEditItemType || (BulkEditItemType = {}));
const selectionItems = [
    {
        id: BulkEditItemType.SelectVisible,
        children: "Select visible"
    },
    {
        id: BulkEditItemType.SelectIndividual,
        children: "Select individual"
    },
    {
        id: BulkEditItemType.SelectAllInFilter,
        children: "Select all"
    },
    {
        id: BulkEditItemType.SelectNone,
        children: "Select none"
    }
];
/**
 * @deprecated - will be removed in future releases
*/
export const BulkEditControl = ({ id, value = BulkEditItemType.SelectNone, onChange, disabled, className = "" }) => {
    const { translate } = useLanguage();
    const translatedItems = selectionItems.map(item => (Object.assign(Object.assign({}, item), { children: translate(item.children) })));
    return _jsx("div", { id: id, className: classNames(["bulk-edit-container", className || ""]), children: _jsx(Select, { items: translatedItems, value: value, onChange: onChange, disabled: disabled, title: translate("Multiple Selection Menu") }) });
};
export const TRANSLATIONS = [
    "Multiple Selection Menu",
    "Select visible",
    "Select individual",
    "Select all",
    "Select none"
];
