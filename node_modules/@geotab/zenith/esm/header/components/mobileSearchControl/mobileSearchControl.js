import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Button } from "../../../button/button";
import { ButtonType } from "../../../button/buttonType";
import { SvgIconSize } from "../../../svgIcon/svgIconSize";
import { TextIconButton, ButtonIconPosition } from "../../../textIconButton/textIconButton";
import { classNames } from "../../../commonHelpers/classNames/classNames";
import { useLanguage } from "../../../utils/localization/useLanguage";
import { IconSearch } from "../../../icons/iconSearch";
import { useDrive } from "../../../utils/theme/useDrive";
import { useDriveClassName } from "../../../utils/theme/useDriveClassName";
export const MobileSearchControl = ({ searchInput, onClick, state, className }) => {
    const { translate } = useLanguage();
    const isDrive = useDrive();
    const driveClassname = useDriveClassName("zen-mobile-search-control");
    const cancelSearchButton = _jsx(Button, { className: "zen-mobile-search-control__actions zen-mobile-search-control__actions--button", title: translate("Cancel search"), type: ButtonType.Tertiary, onClick: () => onClick(false), children: translate("Cancel") });
    const searchIconButton = _jsx(TextIconButton, { className: "zen-mobile-search-control__actions zen-mobile-search-control__actions--button", title: translate("Show page search"), type: ButtonType.TertiaryBlack, onClick: () => onClick(true), iconPosition: ButtonIconPosition.End, iconSize: isDrive ? SvgIconSize.Huge : SvgIconSize.Large, iconClasses: "zen-mobile-search-control__actions--icon", icon: IconSearch });
    const mobileSearchPanel = _jsxs("div", { className: "zen-mobile-search-control--expanded", children: [_jsx("div", { className: "zen-mobile-search-control__search", children: searchInput }), cancelSearchButton] });
    return _jsx("div", { className: classNames(["zen-mobile-search-control", driveClassname || "", className !== null && className !== void 0 ? className : ""]), children: state ? mobileSearchPanel : searchIconButton });
};
export const TRANSLATIONS = [
    "Cancel",
    "Cancel search",
    "Show page search"
];
