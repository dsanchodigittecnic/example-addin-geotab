import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import { ButtonType } from "../../../button/buttonType";
import { SvgIconSize } from "../../../svgIcon/svgIconSize";
import { TextIconButton, ButtonIconPosition } from "../../../textIconButton/textIconButton";
import { classNames } from "../../../commonHelpers/classNames/classNames";
import { useLanguage } from "../../../utils/localization/useLanguage";
import { IconSettings6 } from "../../../icons/iconSettings6";
import { useDrive } from "../../../utils/theme/useDrive";
// eslint-disable-next-line @typescript-eslint/naming-convention
export const MobileFilterControl = React.forwardRef(function MobileFilterControl({ className, onClick, count }, ref) {
    const { translate } = useLanguage();
    const isDrive = useDrive();
    const filterIconButton = _jsx(TextIconButton, { iconPosition: ButtonIconPosition.End, icon: IconSettings6, iconSize: isDrive ? SvgIconSize.Huge : SvgIconSize.Large, className: "zen-mobile-filter-control__actions zen-mobile-filter-control__actions--button", iconClasses: "zen-mobile-filter-control__actions--icon", title: translate("Show all filters"), type: ButtonType.TertiaryBlack, contentClasses: count ? "zen-mobile-filter-control__counter-wrapper" : "", onClick: onClick, ref: ref, children: count ? _jsx("div", { className: "zen-mobile-filter-control__counter", children: count }) : null });
    return _jsx("div", { className: classNames(["zen-mobile-filter-control", className !== null && className !== void 0 ? className : ""]), children: filterIconButton });
});
export const TRANSLATIONS = [
    "Show all filters"
];
