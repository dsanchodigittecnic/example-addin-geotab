var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { ButtonIconPosition, TextIconButton } from "../textIconButton/textIconButton";
import { classNames } from "../commonHelpers/classNames/classNames";
import { IconChevronLeft } from "../icons/iconChevronLeft";
import { useLanguage } from "../utils/localization/useLanguage";
export const HeaderBackDisplayName = "HeaderBack";
export const HeaderBack = (_a) => {
    var { id, className, link, onClick, title } = _a, restProps = __rest(_a, ["id", "className", "link", "onClick", "title"]);
    const { translate } = useLanguage();
    const fullClassName = classNames(["zen-header-back", className || ""]);
    const props = Object.assign({}, restProps);
    return _jsx(TextIconButton, Object.assign({ id: id, icon: IconChevronLeft, iconPosition: ButtonIconPosition.Start, type: "tertiary", className: fullClassName, onClick: onClick ? () => onClick(id) : undefined, link: link ? link : undefined, htmlType: "button", title: title ? title : translate("Back") }, props));
};
HeaderBack.displayName = HeaderBackDisplayName;
export const TRANSLATIONS = [
    "Back"
];
