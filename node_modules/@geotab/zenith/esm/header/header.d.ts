import React, { ReactElement } from "react";
import "./header.less";
import { IHeaderBack } from "./headerBack";
import { IZenComponentProps } from "../commonHelpers/zenComponent";
import { IHeaderButton } from "./headerButton";
import { IHeaderMenu } from "./headerMenu";
import { IHeaderTitle } from "../headerTitle/headerTitle";
import { IBookmark } from "../bookmark/bookmark";
export declare enum HeaderType {
    Desktop = "desktop",
    Mobile = "mobile"
}
export interface IHeader extends IZenComponentProps {
    children: ReactElement | ReactElement[] | undefined;
    onFiltersBarOpen?: () => void;
    /**
     * @deprecated
     * @param useMobileVersion - will be removed in the next major release
    */
    useMobileVersion?: boolean;
    /**
     * @deprecated
     * @param translate - will be removed in the next major release
    */
    translate?: (s: string) => string;
}
export declare const Header: React.FC<IHeader> & {
    Button: React.FC<IHeaderButton>;
} & {
    Menu: React.FC<IHeaderMenu>;
} & {
    Title: React.FC<IHeaderTitle>;
} & {
    Bookmark: React.FC<IBookmark>;
} & {
    Back: React.FC<IHeaderBack>;
};
export declare const HeaderDisplayName = "Header";
export declare const TRANSLATIONS: string[];
