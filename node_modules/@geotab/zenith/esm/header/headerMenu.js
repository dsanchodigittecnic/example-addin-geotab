var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { useState, useRef, useCallback } from "react";
import { Button } from "../button/button";
import { driveIconSize } from "../svgIcon/svgIconSize";
import { classNames } from "../commonHelpers/classNames/classNames";
import { IconChevronDownSmall } from "../icons/iconChevronDownSmall";
import { ControlledMenu } from "../menu/controlledMenu";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { useDrive } from "../utils/theme/useDrive";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
export const HeaderMenuDisplayName = "HeaderMenu";
export const HeaderMenu = (_a) => {
    var { name, children, icon, iconSize = "large", className = "", important, alignment = "bottom-right", inContextMenu, onlyIcon } = _a, restProps = __rest(_a, ["name", "children", "icon", "iconSize", "className", "important", "alignment", "inContextMenu", "onlyIcon"]);
    const isMobile = useMobile();
    const isDrive = useDrive();
    const driveClassName = useDriveClassName("zen-header-button-with-dropdown");
    const [isOpen, setIsOpen] = useState(false);
    const ref = useRef(null);
    const fullClassName = classNames([
        "zen-header-button zen-header-button--with-dropdown",
        "zen-caption",
        isMobile ? "zen-header-button--mobile" : "",
        onlyIcon ? "zen-header-button--hidden-name" : "",
        driveClassName || "",
        className
    ]);
    const props = Object.assign(Object.assign(Object.assign({}, restProps), { "data-important": important ? "1" : "0" }), (isMobile || onlyIcon ? { title: restProps.title || name } : {}));
    const onTriggerClick = useCallback(() => {
        setIsOpen(!isOpen);
    }, [isOpen]);
    const trigger = _jsxs(Button, Object.assign({ onClick: onTriggerClick, ref: ref, className: fullClassName }, props, { children: [icon ? icon({ className: "zen-caption__pre-content", size: isDrive ? driveIconSize(iconSize) : iconSize }) : null, isMobile || onlyIcon ? null : _jsx("span", { className: "zen-caption__content", children: name }), onlyIcon ? null : _jsx(IconChevronDownSmall, { size: isDrive ? driveIconSize("large") : "large", className: "zen-caption__post-content" })] }));
    return _jsxs(_Fragment, { children: [trigger, _jsx(ControlledMenu, { title: name, triggerRef: ref, isOpen: isOpen, setIsOpen: setIsOpen, alignment: alignment, children: children })] });
};
HeaderMenu.displayName = HeaderMenuDisplayName;
