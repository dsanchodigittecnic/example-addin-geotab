var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useEffect, useRef } from "react";
import { CheckboxState } from "../checkbox/checkboxState";
import { getCheckboxState } from "../checkbox/checkboxUtils";
export const TriStateCheckbox = (_a) => {
    var { checked, indeterminate } = _a, rest = __rest(_a, ["checked", "indeterminate"]);
    const ref = useRef(null);
    let checkedValue = false;
    let indeterminateValue = false;
    const state = getCheckboxState(checked, indeterminate);
    if (state === CheckboxState.On) {
        checkedValue = true;
        indeterminateValue = false;
    }
    else if (state === CheckboxState.Indeterminate) {
        checkedValue = false;
        indeterminateValue = true;
    }
    else {
        checkedValue = false;
        indeterminateValue = false;
    }
    useEffect(() => {
        if (!ref.current) {
            return;
        }
        ref.current.indeterminate = indeterminateValue;
    }, [indeterminateValue]);
    return _jsx("input", Object.assign({ ref: ref, checked: checkedValue, type: "checkbox" }, rest));
};
