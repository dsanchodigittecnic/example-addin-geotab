import { jsx as _jsx } from "react/jsx-runtime";
import { useCallback, useContext, useEffect, useLayoutEffect, useMemo, useRef } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { createPortal } from "react-dom";
import { FOCUSABLE_SELECTOR } from "../utils/focusableSelector";
import { isChildPopup } from "../utils/positioningUtils/isChildPopup";
import { useEscape } from "../commonHelpers/hooks/useEscape";
import { useResize } from "../commonHelpers/hooks/useResize";
import { useTrapFocus } from "../commonHelpers/hooks/useTrapFocus";
import { isChildOf } from "../utils/isChildOf";
import { isFocusable } from "../dialog/dialogHelpers";
import { calculatePosition as calculatePos } from "../utils/positioningUtils/calculatePosition";
import { useArrowKey } from "../commonHelpers/hooks/useArrowKey";
import { themeContext } from "../utils/theme/themeContext";
import { useScroll } from "../commonHelpers/hooks/useScroll";
import { getScrollableParent } from "../utils/getScrollableParent";
export const Absolute = ({ isOpen, id, paddingX, paddingY, className, children, triggerRef, alignment, inline, onOpenChange, useTrapFocusWithTrigger = "off", shouldHoldScroll, stateFullChilds, role, ariaLabel, ariaLabelledby, alignmentsFn, recalculateOnScroll, recalculateTrigger = true }) => {
    const popupRef = useRef(null);
    const prevScroll = useRef(0);
    const { dark } = useContext(themeContext);
    const onScrollHandler = (e) => {
        var _a;
        prevScroll.current = (_a = e.target) === null || _a === void 0 ? void 0 : _a.scrollTop;
    };
    const accessibleAttr = useMemo(() => {
        if (!role) {
            return {};
        }
        return Object.assign(Object.assign({ role }, (ariaLabelledby ? { "aria-labelledby": ariaLabelledby } : {})), (ariaLabel && !ariaLabelledby ? { "aria-label": ariaLabel } : {}));
    }, [role, ariaLabelledby, ariaLabel]);
    const darkClassName = dark ? "zen-dark" : "";
    const renderPopup = () => {
        if (isOpen) {
            const result = _jsx("div", Object.assign({}, accessibleAttr, { ref: popupRef, id: id, onScroll: shouldHoldScroll ? onScrollHandler : undefined, className: classNames(["zen-absolute", darkClassName, className || ""]), children: children }));
            if (inline) {
                return result;
            }
            return createPortal(result, document.fullscreenElement || document.body);
        }
        return "";
    };
    const parent = useMemo(() => {
        if (!isOpen || !recalculateOnScroll) {
            return null;
        }
        return getScrollableParent(triggerRef.current);
    }, [triggerRef, isOpen, recalculateOnScroll]);
    const calculatePosition = useCallback(() => calculatePos(triggerRef, popupRef, paddingX, paddingY, inline, alignment, alignmentsFn, parent ? parent : undefined), [triggerRef, paddingX, paddingY, inline, alignment, alignmentsFn, popupRef, parent]);
    useResize(calculatePosition, isOpen);
    useScroll(calculatePosition, isOpen, parent);
    useLayoutEffect(calculatePosition, [isOpen, children, recalculateTrigger, calculatePosition]);
    function handleEscape() {
        onOpenChange(false);
        if (!triggerRef.current) {
            return;
        }
        if (isFocusable(triggerRef.current)) {
            triggerRef.current.focus();
            return;
        }
        const focusable = [...Array.from(triggerRef.current.querySelectorAll(FOCUSABLE_SELECTOR))];
        (focusable[0] || triggerRef.current).focus();
    }
    const condition = useCallback(() => useTrapFocusWithTrigger === "off" ? false : true, [useTrapFocusWithTrigger]);
    useTrapFocus(popupRef, useTrapFocusWithTrigger === "withTrigger" ? triggerRef : undefined, isOpen, condition);
    useEscape(popupRef, handleEscape, isOpen);
    useArrowKey(popupRef, handleEscape, isOpen);
    useEffect(() => {
        var _a, _b;
        const closeOnClickOutside = (e) => {
            if (!popupRef.current) {
                return;
            }
            if (!e.target) {
                return;
            }
            const isClickInPopup = isChildPopup(e.target, popupRef.current);
            const isClickOnTrigger = triggerRef.current ? e.target === triggerRef.current || isChildOf(e.target, triggerRef.current) : false;
            if (isClickInPopup || isClickOnTrigger || stateFullChilds) {
                return;
            }
            onOpenChange(false);
        };
        if (isOpen) {
            document.body.addEventListener("click", closeOnClickOutside, true);
            const firstFocusable = (_a = popupRef.current) === null || _a === void 0 ? void 0 : _a.querySelector(FOCUSABLE_SELECTOR);
            if (useTrapFocusWithTrigger !== "withTrigger") {
                if (firstFocusable) {
                    firstFocusable.focus();
                }
                else {
                    (_b = popupRef.current) === null || _b === void 0 ? void 0 : _b.focus();
                }
            }
            return () => {
                document.body.removeEventListener("click", closeOnClickOutside, true);
            };
        }
        return () => { };
    }, [isOpen, onOpenChange, triggerRef, useTrapFocusWithTrigger, stateFullChilds]);
    useEffect(() => {
        var _a;
        if (isOpen) {
            (_a = popupRef.current) === null || _a === void 0 ? void 0 : _a.removeAttribute("style");
            calculatePosition();
        }
        return () => { };
    }, [calculatePosition, children, isOpen]);
    useEffect(() => {
        if (isOpen) {
            if (popupRef.current && prevScroll.current && shouldHoldScroll) {
                popupRef.current.scrollTop = prevScroll.current;
            }
        }
        else {
            prevScroll.current = 0;
        }
    }, [children, isOpen, shouldHoldScroll]);
    return renderPopup();
};
