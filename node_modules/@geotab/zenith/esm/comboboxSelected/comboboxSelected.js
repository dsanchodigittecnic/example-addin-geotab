import { jsx as _jsx } from "react/jsx-runtime";
import { ENTIRE_ORGANIZATION_GROUP_ID } from "../groupsFilter/groupsHelper";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useLanguage } from "../utils/localization/useLanguage";
import { Pill } from "../pill/pill";
import { PillBox } from "../pillBox/pillBox";
export const ComboboxSelected = ({ selection, onRemove, className, pillEmptyText }) => {
    const { translate } = useLanguage();
    const isCommonPill = selection.length === 0 || selection.length === 1 && selection[0].id === ENTIRE_ORGANIZATION_GROUP_ID;
    return _jsx("div", { className: classNames(["zen-combobox-selected", className || ""]), children: isCommonPill && pillEmptyText ? _jsx(Pill, { children: translate(pillEmptyText) })
            : _jsx(PillBox, { selectedPills: selection.map(el => (el.name ? el : Object.assign(Object.assign({}, el), { name: "..." }))), onRemove: onRemove }) });
};
export const TRANSLATIONS = [
    "Remove selection"
];
