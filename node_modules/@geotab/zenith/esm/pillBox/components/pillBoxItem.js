import { jsx as _jsx } from "react/jsx-runtime";
import { memo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { Pill } from "../../pill/pill";
import { useLanguage } from "../../utils/localization/useLanguage";
export const PillBoxItem = memo(function innerPillItem({ item, onRemove, className }) {
    var _a;
    // eslint-disable-next-line react-hooks/rules-of-hooks
    const { translate } = useLanguage();
    return _jsx(Pill, { id: item.id, closeAriaText: translate("Remove selection"), onClose: () => onRemove(item.id), className: classNames(["zen-pill-box-item zen-caption", (_a = item.className) !== null && _a !== void 0 ? _a : "", className]), children: _jsx("span", { className: "zen-caption__content", children: item.name }) }, `${item.id}-${item.name}`);
});
export const TRANSLATIONS = [
    "Remove selection"
];
