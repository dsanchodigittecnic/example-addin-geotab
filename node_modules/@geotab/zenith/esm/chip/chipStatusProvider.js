import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
export const chipStatusContext = createContext(undefined);
export const ChipStatusProvider = ({ status, children }) => (_jsx(chipStatusContext.Provider, { value: status, children: children }));
export const useChipStatus = () => {
    const status = useContext(chipStatusContext);
    return status;
};
