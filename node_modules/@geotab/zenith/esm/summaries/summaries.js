import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import { splitIntoPairs } from "./utils/splitIntoPairs";
export const Summaries = ({ type = "row", children }) => {
    if (type === "two-columns") {
        return _jsx("div", { className: `zen-summaries__${type}`, children: splitIntoPairs(children).map((pair, index) => (_jsxs("div", { className: "zen-summaries__two-columns-container", children: [pair[0], pair[1]] }, `summaries-item-${index}`))) });
    }
    return _jsx("div", { className: `zen-summaries__${type}`, children: children });
};
