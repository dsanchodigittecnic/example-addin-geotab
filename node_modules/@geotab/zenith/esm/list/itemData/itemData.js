var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { memo, useContext, useMemo } from "react";
import { viewportContext } from "../viewportProvider/viewportContext";
import { ItemDataInternal } from "./itemDataInternal";
const ItemDataInner = (_a) => {
    var { id } = _a, rest = __rest(_a, ["id"]);
    const { isInViewport } = useContext(viewportContext);
    const visible = useMemo(() => isInViewport(id), [id, isInViewport]);
    return _jsx(ItemDataInternal, Object.assign({ id: id, visible: visible }, rest), id);
};
export const ItemData = memo(ItemDataInner);
