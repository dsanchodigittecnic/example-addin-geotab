import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { Skeleton } from "../../skeleton/skeleton";
import { classNames } from "../../commonHelpers/classNames/classNames";
export const ListItemWaiting = ({ id, isDrive, isMobile }) => {
    const driveClassName = useMemo(() => isDrive
        ? isMobile ? "zen-list-item--drive" : "zen-list-item--drive-tablet"
        : "", [isDrive, isMobile]);
    return _jsx("li", { id: id, className: classNames(["zen-list-item", driveClassName]), children: _jsx(Skeleton, { className: "zen-list-item__waiting" }) });
};
