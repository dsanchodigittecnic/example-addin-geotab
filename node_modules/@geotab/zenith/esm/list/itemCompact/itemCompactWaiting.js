import { jsx as _jsx } from "react/jsx-runtime";
import { Skeleton } from "../../skeleton/skeleton";
import { useMemo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
export const ItemCompactWaiting = ({ isDrive, isMobile }) => {
    const driveClassName = useMemo(() => isDrive
        ? isMobile ? "zen-list-item-compact--drive" : "zen-list-item-compact--drive-tablet"
        : "", [isDrive, isMobile]);
    return _jsx("li", { className: classNames(["zen-list-item-compact", driveClassName]), children: _jsx(Skeleton, { className: "zen-list-item-compact__waiting" }) });
};
