import React from "react";
import { IButton } from "../button/button";
import { TButtonIconPosition } from "../textIconButton/textIconButton";
import { TSvgIconSize } from "../svgIcon/svgIconSize";
import { IIcon } from "../icons/icon";
export interface IPageHeaderButton extends Omit<IButton, "onClick" | "icon" | "title"> {
    id: string;
    icon: React.FC<IIcon>;
    title: string;
    iconPosition?: TButtonIconPosition;
    iconClasses?: string;
    iconSize?: TSvgIconSize;
    onClick?: (id: string, event: React.MouseEvent) => void;
}
export declare const PageHeaderButtonDisplayName = "PageHeaderButton";
export declare const PageHeaderButton: React.FC<IPageHeaderButton>;
