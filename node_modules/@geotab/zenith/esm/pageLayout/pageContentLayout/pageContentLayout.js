import { jsx as _jsx } from "react/jsx-runtime";
import * as React from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
// eslint-disable-next-line @typescript-eslint/naming-convention
export const PageContentLayout = React.forwardRef(function PageContentLayout({ id, className, children, oldStyle }, ref) {
    const cssClassName = classNames([
        "zen-page-layout__content",
        oldStyle ? "zen-page-layout__content--old" : "",
        className || ""
    ]);
    return _jsx("div", { id: id, className: cssClassName, ref: ref, children: children });
});
PageContentLayout.displayName = "PageContentLayout";
