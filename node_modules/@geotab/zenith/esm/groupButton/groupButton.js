import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import React, { useCallback, useId } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
const GroupButtonOption = ({ nameGroup, labelName, value, disabled, checked, onChange }) => {
    const inputGroupId = useId();
    const inputKey = useId();
    const labelKey = useId();
    return _jsxs(React.Fragment, { children: [_jsx("input", { type: "radio", onChange: onChange, checked: checked, id: inputGroupId, value: value, disabled: disabled, className: "zen-group-button__input", name: nameGroup }, inputKey), _jsx("label", { htmlFor: inputGroupId, className: "zen-group-button__label zen-ellipsis", children: labelName }, labelKey)] });
};
export const GroupButton = ({ fullWidth, disabled, onChange, groupData, className }) => {
    const nameGroup = useId();
    const onChangeHandler = useCallback((e) => { onChange && onChange(e); }, [onChange]);
    const driveClasses = useDriveClassName("zen-group-button");
    return _jsx("div", { className: classNames(["zen-group-button", driveClasses || "", fullWidth ? "zen-group-button--full-width" : "", className || ""]), children: groupData.map((el, index) => _jsx(GroupButtonOption, { nameGroup: nameGroup, labelName: el.name, value: el.value, disabled: !!disabled, checked: el.selected, onChange: onChangeHandler }, `${nameGroup}_${index}_btngroup`)) });
};
