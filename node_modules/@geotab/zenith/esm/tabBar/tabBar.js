import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo } from "react";
import { TabBarContent } from "./tabBarContent";
import { classNames } from "../commonHelpers/classNames/classNames";
import { Tabs } from "../tabs/tabs";
export const TabBar = ({ className, tabs, activeTabId, onTabChange }) => {
    const memoizedTabs = useMemo(() => tabs, [tabs]);
    return _jsx("div", { className: classNames(["zen-tab-bar", className ? className : ""]), children: memoizedTabs.length ? _jsxs(_Fragment, { children: [_jsx(Tabs, { tabs: tabs, activeTabId: activeTabId, onTabChange: onTabChange }), _jsx(TabBarContent, { activeId: activeTabId, tabs: memoizedTabs })] }) : null });
};
