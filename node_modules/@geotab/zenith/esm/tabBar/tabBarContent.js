import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
export const TabBarContent = ({ tabs, activeId }) => {
    const tabsContent = tabs.map(tab => _jsx("div", { className: classNames(["zen-tab-bar-content-wrapper", activeId !== tab.id ? "zen-tab-bar-content-wrapper--hidden" : ""]), role: "tabpanel", "aria-labelledby": tab.id, tabIndex: 0, id: `panel-${tab.id}`, hidden: activeId !== tab.id ? true : false, children: tab.content }, tab.id));
    return _jsx("div", { className: "zen-tab-bar-content", children: tabsContent });
};
