const isRequiredPropsValid = (toast) => typeof toast.id === "string" && (typeof toast.header === "string" || typeof toast.header === "object") && typeof toast.onClose === "function";
export const isToastValid = (toast) => {
    if (!isRequiredPropsValid(toast)) {
        return false;
    }
    return true;
};
