import { isAlertValid } from "./isAlertValid";
export const addZenithAlert = (event, addAlert) => {
    const alert = event.detail;
    if (!isAlertValid(alert)) {
        return;
    }
    addAlert(alert);
};
