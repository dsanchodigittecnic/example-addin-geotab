var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from "react";
import { Button } from "../button/button";
import { classNames } from "../commonHelpers/classNames/classNames";
import { SvgIconSize } from "../svgIcon/svgIconSize";
import { useDrive } from "../utils/theme/useDrive";
import { useMobile } from "../commonHelpers/hooks/useMobile";
export const ButtonIconPosition = {
    Start: "start",
    End: "end"
};
// eslint-disable-next-line @typescript-eslint/naming-convention
export const TextIconButton = React.forwardRef(function TextIcon(_a, ref) {
    var { icon, iconClasses, iconPosition = ButtonIconPosition.Start, iconSize, children, className, contentClasses } = _a, restProps = __rest(_a, ["icon", "iconClasses", "iconPosition", "iconSize", "children", "className", "contentClasses"]);
    const isDrive = useDrive();
    const defaultIconSize = isDrive ? SvgIconSize.Huge : SvgIconSize.Large;
    const isMobile = useMobile();
    const buttonIconSize = iconSize ? iconSize : defaultIconSize;
    const svgIconClasses = React.useMemo(() => {
        const postfix = iconPosition === ButtonIconPosition.End ? "post-content" : "pre-content";
        return classNames([`zen-caption__${children ? postfix : "icon"}`, iconClasses !== null && iconClasses !== void 0 ? iconClasses : ""]);
    }, [iconPosition, iconClasses, children]);
    const buttonExtraClasses = React.useMemo(() => classNames(["zen-caption", !isMobile && isDrive ? "zen-button--huge" : "", className !== null && className !== void 0 ? className : ""]), [className, isMobile, isDrive]);
    // init react functional component
    const svgIcon = React.useMemo(() => React.createElement(icon, {
        className: svgIconClasses,
        size: buttonIconSize
    }), [icon, buttonIconSize, svgIconClasses]);
    const isStartIconPosition = iconPosition === ButtonIconPosition.Start;
    return _jsxs(Button, Object.assign({ ref: ref, className: buttonExtraClasses }, restProps, { children: [isStartIconPosition ? svgIcon : null, children && _jsx("span", { className: classNames(["zen-caption__content", contentClasses !== null && contentClasses !== void 0 ? contentClasses : ""]), children: children }), !isStartIconPosition ? svgIcon : null] }));
});
