import { useContext, useMemo } from "react";
import { Theme, themeContext } from "./themeContext";
export const useDrive = () => {
    const { theme } = useContext(themeContext);
    const isDrive = useMemo(() => theme === Theme.Drive, [theme]);
    return isDrive;
};
