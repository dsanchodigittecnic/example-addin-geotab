import { createContext } from "react";
export var Theme;
(function (Theme) {
    Theme[Theme["Default"] = 0] = "Default";
    Theme[Theme["Drive"] = 1] = "Drive";
})(Theme || (Theme = {}));
export const themeContext = createContext({ theme: Theme.Default, dark: false });
