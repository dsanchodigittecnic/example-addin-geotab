export const isChildOf = (el, parent) => {
    if (el === parent) {
        return true;
    }
    if (el.parentElement) {
        return isChildOf(el.parentElement, parent);
    }
    return false;
};
