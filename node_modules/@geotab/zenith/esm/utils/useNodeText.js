import { useEffect, useState } from "react";
export const useNodeText = (ref) => {
    const [text, setText] = useState("");
    useEffect(() => {
        if (!ref.current) {
            return;
        }
        const textContent = ref.current.textContent || "";
        setText(textContent);
    }, [ref]);
    return text;
};
