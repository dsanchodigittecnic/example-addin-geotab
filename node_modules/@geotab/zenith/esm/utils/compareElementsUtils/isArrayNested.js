import { isPrimitive } from "./isPrimitive";
export const isArrayNested = (array) => {
    if (!Array.isArray(array)) {
        return false;
    }
    for (const element of array) {
        if (!isPrimitive(element)) {
            return true;
        }
    }
    return false;
};
