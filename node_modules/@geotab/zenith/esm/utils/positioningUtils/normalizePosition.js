import { normalizeHorizontally } from "./normalizeHorizontally";
import { normalizeVertically } from "./normalizeVertically";
export const normalizePosition = (position, popupRect, parentRect) => {
    normalizeVertically(position, popupRect, parentRect.height);
    normalizeHorizontally(position, popupRect, parentRect.width);
};
