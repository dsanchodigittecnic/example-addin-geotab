import { jsx as _jsx } from "react/jsx-runtime";
import { useContext } from "react";
import { createContext } from "react";
export const headerButtonsFormatContext = createContext({
    buttons: [],
    priority: false
});
export const HeaderButtonsProvider = ({ buttons, priority, children }) => (_jsx(headerButtonsFormatContext.Provider, { value: {
        buttons: buttons || [],
        priority: priority || false
    }, children: children }));
export const useHeaderButtons = () => {
    const { buttons, priority } = useContext(headerButtonsFormatContext);
    return { buttons, priority };
};
