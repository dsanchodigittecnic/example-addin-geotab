import { CheckboxState } from "./checkboxState";
export const getCheckboxState = (checked = false, indeterminate = false) => checked ? CheckboxState.On : indeterminate ? CheckboxState.Indeterminate : CheckboxState.Off;
