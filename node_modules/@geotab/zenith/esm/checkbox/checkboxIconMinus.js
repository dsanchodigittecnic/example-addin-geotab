import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
export const CheckboxIconMinus = ({ className, size }) => {
    const classes = useMemo(() => classNames([
        "zen-icon",
        (size ? `zen-icon--${size}` : ""),
        className !== null && className !== void 0 ? className : ""
    ]), [size, className]);
    return _jsx("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 10 2", focusable: "false", tabIndex: -1, "aria-hidden": "true", className: classes, "data-name": "CheckboxIconMinus", children: _jsx("line", { x1: "1", y1: "1", x2: "9", y2: "1", strokeWidth: "2", strokeLinecap: "round" }) });
};
