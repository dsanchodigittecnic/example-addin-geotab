import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { SvgIconSize } from "../svgIcon/svgIconSize";
import { PaginationType } from "./paginationType";
import { ButtonIconPosition, TextIconButton } from "../textIconButton/textIconButton";
import { ButtonType } from "../button/buttonType";
import { useLanguage } from "../utils/localization/useLanguage";
import { IconChevronRight } from "../icons/iconChevronRight";
import { IconArrowRight } from "../icons/iconArrowRight";
export const PaginationArrow = ({ value, disabled, type = PaginationType.Normal, arrowButtonHandler }) => {
    const { translate } = useLanguage();
    const previousButtonTitle = translate("Previous page");
    const nextButtonTitle = translate("Next page");
    const buttonTitle = value === "next" ? nextButtonTitle : previousButtonTitle;
    return _jsx(TextIconButton, { className: "zen-pagination__arrow-navigation-button", "data-page": value, "aria-label": buttonTitle, title: buttonTitle, disabled: disabled, onClick: arrowButtonHandler, type: type === PaginationType.Compact ? ButtonType.TertiaryBlack : ButtonType.Secondary, icon: type === PaginationType.Compact ? IconChevronRight : IconArrowRight, iconSize: type === PaginationType.Compact ? SvgIconSize.Big : SvgIconSize.Large, iconPosition: ButtonIconPosition.Start, iconClasses: classNames(["zen-pagination__arrow-navigation-button-icon", value === "previous" ? "zen-pagination__arrow-navigation-button-icon--rotate" : ""]) });
};
export const TRANSLATIONS = [
    "Previous page",
    "Next page"
];
