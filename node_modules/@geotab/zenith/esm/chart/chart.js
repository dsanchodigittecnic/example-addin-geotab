var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { PieChart } from "./pieChart";
import { LineChart } from "./lineChart";
import { BarChart } from "./barChart";
import { useLanguage } from "../utils/localization/useLanguage";
const isPieChart = (type, _) => type === "pie";
const isBarChart = (type, _) => type === "bar";
export const Chart = (_a) => {
    var { type } = _a, rest = __rest(_a, ["type"]);
    const { translate } = useLanguage();
    const chartComponent = isPieChart(type, rest) ?
        _jsx(PieChart, Object.assign({}, rest)) :
        isBarChart(type, rest) ?
            _jsx(BarChart, Object.assign({}, rest)) :
            _jsx(LineChart, Object.assign({}, rest));
    return _jsxs("figure", { style: { margin: 0, padding: 0, height: "100%" }, children: [_jsx("figcaption", { className: "screen-reader-only", children: translate("Chart data table and visualization") }), chartComponent] });
};
