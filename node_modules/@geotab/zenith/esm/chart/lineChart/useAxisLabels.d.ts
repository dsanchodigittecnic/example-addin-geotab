import { ILineChartOptions } from "./interfaces";
import { IBarChartOptions } from "../barChart/interfaces";
import { IChartAxis } from "../chartAxis/chartAxis";
export declare const useAxisLabels: (axis: IChartAxis | false | undefined, options: ILineChartOptions | IBarChartOptions) => {
    xLabels: string | undefined;
    yLabels: string[];
};
