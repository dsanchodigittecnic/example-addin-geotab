import { Chart, ChartDataset, LegendItem } from "chart.js/auto";
import "./legend.less";
import { ILegendOptions } from "./interfaces";
type TLegendRenderer = (item: LegendItem, dataset: ChartDataset) => string;
export interface ILegendContainer {
    labels: string[];
    renderer?: TLegendRenderer;
}
export declare const LEGEND_PLUGIN_ID = "zenithLegend";
export declare const legendPlugin: (containerId: string, options: ILegendOptions | undefined | false, isDrive: boolean, isHidden: (index: number) => boolean, toggleHidden: (index: number) => void) => {
    id: string;
    isDrive: boolean;
    afterUpdate(chart: Chart<"line"> | Chart<"bar"> | Chart<"pie">): void;
};
export {};
