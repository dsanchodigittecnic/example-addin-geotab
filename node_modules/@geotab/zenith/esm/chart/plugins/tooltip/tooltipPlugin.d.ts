import { Chart } from "chart.js";
import { ITooltipOptions } from "./interfaces";
import { TTooltipPosition } from "./renderTooltip";
interface ITooltipMeta {
    x: number | null;
    y: number | null;
    enabled: boolean;
    index: number | undefined;
    datasetIndex: number | undefined;
    mouseMove: (event: MouseEvent) => void;
    mouseOut: () => void;
}
interface IChartTooltipMeta extends Chart<"bar" | "pie"> {
    zenithTooltip: ITooltipMeta;
}
export declare const TooltipPlugin: (containerId: string, options: ITooltipOptions | undefined | false, alignment: TTooltipPosition | undefined, isHidden: (index: number) => boolean, isDark: boolean) => {
    id: string;
    afterInit: (chart: IChartTooltipMeta) => void;
    afterDraw: (chart: IChartTooltipMeta) => void;
    beforeDestroy: (chart: IChartTooltipMeta) => void;
};
export {};
