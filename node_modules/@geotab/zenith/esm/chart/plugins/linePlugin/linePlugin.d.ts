import Chart from "chart.js/auto";
import { ILineOptions } from "./interfaces";
interface IVerticalLineMeta {
    x: number | null;
    y: number | null;
    enabled: boolean;
    mouseMove: (event: MouseEvent) => void;
    mouseOut: () => void;
}
interface ILineChartVerticalMeta extends Chart<"line"> {
    verticalLine: IVerticalLineMeta;
}
export declare const LinePlugin: (containerId: string, options: ILineOptions | undefined | false, isDark: boolean) => {
    id: string;
    afterInit: (chart: ILineChartVerticalMeta) => void;
    afterDraw: (chart: ILineChartVerticalMeta) => void;
    beforeDestroy: (chart: ILineChartVerticalMeta) => void;
};
export {};
