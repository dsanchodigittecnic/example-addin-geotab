import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { cloneElement, isValidElement } from "react";
const isReactElement = (element) => isValidElement(element);
export const ChartInsight = ({ icon, text }) => {
    const sizedIcon = icon && isReactElement(icon)
        ? cloneElement(icon, {
            size: "large",
            className: "zen-chart-insight__icon"
        })
        : null;
    return _jsxs("div", { className: "zen-chart-insight", children: [sizedIcon, _jsx("div", { className: "zen-chart-insight__text", children: text })] });
};
