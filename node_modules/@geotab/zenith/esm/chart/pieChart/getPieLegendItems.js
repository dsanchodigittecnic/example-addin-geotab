import { formatValue } from "../plugins/legend/formatValue";
export const getPieLegendItems = (data, options, isHidden) => {
    if (data.datasets.length === 0) {
        return [];
    }
    const labels = (data.labels || []);
    const dataset = data.datasets[0];
    const colors = dataset.backgroundColor || [];
    const legendItems = dataset.data.map((item, index) => ({
        color: colors[index] && typeof colors[index] === "string" ? colors[index] : "",
        text: labels[index] && typeof labels[index] === "string" ? labels[index] : "",
        value: isHidden(index) ? undefined : formatValue((item).toFixed(2), index, options === null || options === void 0 ? void 0 : options.unit),
        index: index,
        hidden: false
    }));
    return legendItems;
};
