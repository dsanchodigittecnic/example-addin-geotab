var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useCallback, useContext, useEffect, useMemo } from "react";
import { Line } from "../react-chartjs/typedCharts";
import { Chart as ChartJS, CategoryScale, LinearScale, PointElement, LineElement, Title, Tooltip, Legend, Filler, TimeScale } from "chart.js/auto";
import "../react-chartjs/dateAdapter";
import { deepMerge } from "../commonHelpers/utils";
import { getDefaultDatasetStyle } from "./lineChart/getDefaultDatasetStyle";
import { useDrive } from "../utils/theme/useDrive";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { themeContext } from "../utils/theme/themeContext";
import { generateId } from "../commonHelpers/generateId";
import { classNames } from "../commonHelpers/classNames/classNames";
import { legendPlugin } from "./plugins/legend/legendPlugin";
import { ChartAxis } from "./chartAxis/chartAxis";
import { isDateRange } from "./utils/isDateRange";
import { convertDates } from "./utils/convertDates";
import { useSummary } from "./lineChart/useSummary";
import { useAxisLabels } from "./lineChart/useAxisLabels";
import { getYAxisMeta } from "./utils/getYAxisMeta";
import { getDefaultOptions } from "./lineChart/getDefaultOptions";
import { useHidden } from "./utils/useHidden";
import { LinePlugin } from "./plugins/linePlugin/linePlugin";
import { useLanguage } from "../utils/localization/useLanguage";
import { useChartTicks } from "./lineChart/useChartTicks";
import { ChartInsight } from "./chartInsight/chartInsight";
import { getXAxisMeta } from "./utils/getXAxisMeta";
import { userFormatContext } from "../utils/userFormat/userFormatContext";
import { Summary } from "../summary/summary";
import { AccessibleChart } from "./accessibleChart/accessibleChart";
ChartJS.register(CategoryScale, LinearScale, PointElement, LineElement, Title, Tooltip, Legend, Filler, TimeScale);
export const LineChart = (_a) => {
    var { data, options = {}, axis, insight, summary, legend, tooltip, plugins, className } = _a, rest = __rest(_a, ["data", "options", "axis", "insight", "summary", "legend", "tooltip", "plugins", "className"]);
    const { translate } = useLanguage();
    const isDrive = useDrive();
    const isMobile = useMobile();
    const fontSize = useMemo(() => isDrive ? (isMobile ? 14 : 16) : 12, [isDrive, isMobile]);
    const { dark } = useContext(themeContext);
    useEffect(() => {
        ChartJS.defaults.font = {
            family: "Roboto",
            size: fontSize
        };
        ChartJS.defaults.color = dark ? "#FFFFFF" : "#1F2833";
    }, [fontSize, dark]);
    const legendId = useMemo(() => generateId(), []);
    const tooltipId = useMemo(() => generateId(), []);
    const { isHidden, toggleHidden } = useHidden();
    const { toLocalDateTime } = useContext(userFormatContext);
    const isTimedData = useMemo(() => isDateRange(data, true), [data]);
    const chartData = useMemo(() => {
        const chData = Object.assign({}, data);
        chData.datasets = chData.datasets.map((ds, i) => getDefaultDatasetStyle(ds, i));
        chData.datasets = chData.datasets.reduce((acc, dataset, dsIndex) => {
            acc.push(dataset);
            if (dataset.trendData) {
                acc.push(Object.assign(Object.assign({}, dataset), { data: dataset.trendData, borderDash: [5, 5], parentIndex: dsIndex }));
            }
            return acc;
        }, []);
        chData.datasets = chData.datasets.map((ds, i) => isHidden(ds.parentIndex !== undefined ? ds.parentIndex : i) ? Object.assign(Object.assign({}, ds), { hidden: true }) : ds);
        return convertDates(chData, toLocalDateTime);
    }, [data, isHidden, toLocalDateTime]);
    const yAxisMeta = useMemo(() => getYAxisMeta(chartData), [chartData]);
    const xAxisMeta = useMemo(() => getXAxisMeta(chartData, isTimedData), [chartData, isTimedData]);
    const { chartRef, ticks } = useChartTicks();
    const defOptions = useMemo(() => getDefaultOptions(yAxisMeta, xAxisMeta, isTimedData, ticks, dark, translate), [yAxisMeta, xAxisMeta, isTimedData, ticks, dark, translate]);
    const chartOptions = deepMerge(defOptions, options);
    const chartTooltip = useMemo(() => LinePlugin(tooltipId, tooltip, dark), [tooltip, tooltipId, dark]);
    const chartLegend = useMemo(() => legendPlugin(legendId, legend, isDrive, isHidden, toggleHidden), [isDrive, legend, legendId, isHidden, toggleHidden]);
    const chartPlugins = useMemo(() => [...(plugins || []), chartTooltip, chartLegend], [plugins, chartTooltip, chartLegend]);
    const { xLabels, yLabels } = useAxisLabels(axis, chartOptions);
    const intSummary = useSummary(summary, chartData);
    const isMainHidden = useMemo(() => isHidden(0), [isHidden]);
    const toggleMainDataset = useCallback(() => toggleHidden(0), [toggleHidden]);
    return _jsxs("div", { className: classNames(["zen-chart", className || ""]), ref: chartRef, children: [insight
                ? _jsx(ChartInsight, Object.assign({}, insight))
                : null, summary !== false
                ? _jsx(Summary, Object.assign({}, intSummary, { onToggle: toggleMainDataset, isEnabled: isMainHidden }))
                : null, xLabels || yLabels.length > 0
                ? _jsx(ChartAxis, { xLabel: xLabels, yLabels: yLabels })
                : null, _jsx("div", { className: "zen-chart__chart", children: _jsx(Line, Object.assign({ width: 100, height: 100, options: chartOptions, data: chartData, plugins: chartPlugins, "aria-hidden": true }, rest)) }), _jsx("div", { className: "zen-chart__legend", id: legendId }), _jsx(AccessibleChart, { type: "line", data: chartData, options: chartOptions })] });
};
