var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useContext, useEffect, useMemo } from "react";
import { Pie } from "../react-chartjs/typedCharts";
import { Chart as ChartJS, PieController, ArcElement } from "chart.js/auto";
import "../react-chartjs/dateAdapter";
import { deepMerge } from "../commonHelpers/utils";
import { useDrive } from "../utils/theme/useDrive";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { themeContext } from "../utils/theme/themeContext";
import { generateId } from "../commonHelpers/generateId";
import { classNames } from "../commonHelpers/classNames/classNames";
import { legendPlugin } from "./plugins/legend/legendPlugin";
import { useSummary } from "./lineChart/useSummary";
import { getDefaultOptions } from "./pieChart/getDefaultOptions";
import { getDefaultDatasetStyle } from "./pieChart/getDefaultDatasetStyle";
import { useHidden } from "./utils/useHidden";
import { TooltipPlugin } from "./plugins/tooltip/tooltipPlugin";
import { ChartInsight } from "./chartInsight/chartInsight";
import { Summary } from "../summary/summary";
import { AccessibleChart } from "./accessibleChart/accessibleChart";
ChartJS.register(PieController, ArcElement);
export const PieChart = (_a) => {
    var { data, options = {}, insight, summary, legend, tooltip, plugins, className } = _a, rest = __rest(_a, ["data", "options", "insight", "summary", "legend", "tooltip", "plugins", "className"]);
    const isDrive = useDrive();
    const isMobile = useMobile();
    const fontSize = useMemo(() => isDrive ? (isMobile ? 14 : 16) : 12, [isDrive, isMobile]);
    const { dark } = useContext(themeContext);
    useEffect(() => {
        ChartJS.defaults.font = {
            family: "Roboto",
            size: fontSize
        };
        ChartJS.defaults.color = dark ? "#FFFFFF" : "#1F2833";
    }, [fontSize, dark]);
    const legendId = useMemo(() => generateId(), []);
    const tooltipId = useMemo(() => generateId(), []);
    const defOptions = useMemo(() => getDefaultOptions(), []);
    const chartOptions = deepMerge(defOptions, options);
    const { isHidden, toggleHidden } = useHidden();
    const chartData = useMemo(() => {
        const chData = Object.assign({}, data);
        chData.datasets = chData.datasets.map(ds => getDefaultDatasetStyle(ds));
        chData.datasets[0].data = chData.datasets[0].data.map((value, index) => isHidden(index) ? 0 : value);
        return chData;
    }, [data, isHidden]);
    const chartTooltip = useMemo(() => TooltipPlugin(tooltipId, tooltip, "left", isHidden, dark), [tooltipId, tooltip, isHidden, dark]);
    const chartLegend = useMemo(() => legendPlugin(legendId, legend, isDrive, isHidden, toggleHidden), [isDrive, legend, legendId, isHidden, toggleHidden]);
    const chartPlugins = useMemo(() => [...(plugins || []), chartLegend, chartTooltip], [chartLegend, chartTooltip, plugins]);
    const intSummary = useSummary(summary, chartData);
    return _jsxs("div", { className: classNames(["zen-chart", className || ""]), children: [insight
                ? _jsx(ChartInsight, Object.assign({}, insight))
                : null, summary
                ? _jsx(Summary, Object.assign({}, intSummary))
                : null, _jsx("div", { className: "zen-chart__chart", children: _jsx(Pie, Object.assign({ options: chartOptions, data: chartData, plugins: chartPlugins }, rest, { "aria-hidden": true })) }), _jsx("div", { className: "zen-chart__legend", id: legendId }), _jsx(AccessibleChart, { type: "pie", data: chartData })] });
};
