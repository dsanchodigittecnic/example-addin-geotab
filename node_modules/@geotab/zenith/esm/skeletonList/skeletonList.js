import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { Skeleton } from "../skeleton/skeleton";
export const SkeletonList = ({ className, width, size = "small" }) => {
    const styleWidth = width ? { style: { width: `${width}px` } } : {};
    const itemWidth = 115;
    const itemHeight = 14;
    const getSizeCount = (value) => {
        if (value === "large") {
            return 12;
        }
        if (value === "medium") {
            return 8;
        }
        return 4;
    };
    const count = getSizeCount(size);
    return _jsx("div", Object.assign({}, styleWidth, { className: classNames(["zen-skeleton-list", className || ""]), children: Array.from({ length: count }, (_, index) => (_jsx(Skeleton, { width: itemWidth, height: itemHeight }, `skeleton-${index}`))) }));
};
