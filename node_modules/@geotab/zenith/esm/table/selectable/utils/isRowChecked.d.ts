import { IEntityWithId } from "../../../commonHelpers/entity";
import { IRowEntity } from "../../../dataGrid/row/row";
import { SelectionState } from "../interfaces";
import { TSelectionStrategy } from "../useSelectableRows";
export declare const isRowChecked: <T extends IRowEntity<N>, N extends IEntityWithId>(selections: Set<string>, state: SelectionState, strategy: TSelectionStrategy | undefined, entity: T | N, isNested: boolean) => {
    indeterminate: boolean;
    checked: boolean;
};
