import { SelectionState } from "../interfaces";
export const calculateSelectionState = (entities, selections, all, checkboxVisible, checkboxDisabled, strategy) => {
    if (selections.size === 0) {
        return all ? SelectionState.All : SelectionState.None;
    }
    return entities.every(entity => {
        if (!checkboxVisible(entity)) {
            return true;
        }
        if (checkboxDisabled(entity)) {
            return true;
        }
        if (strategy === "nested") {
            if (entity.children) {
                return entity.children.every(child => {
                    if (!checkboxVisible(child)) {
                        return true;
                    }
                    if (checkboxDisabled(child)) {
                        return true;
                    }
                    return selections.has(child.id);
                });
            }
            return false;
        }
        return selections.has(entity.id);
    }) ? SelectionState.CurrentPage : SelectionState.Partial;
};
