import { IEntityWithId } from "../../../commonHelpers/entity";
import { IRowEntity } from "../../../dataGrid/row/row";
import { SelectionState } from "../interfaces";
import { TSelectionStrategy } from "../useSelectableRows";
export declare const calculateSelectionState: <T extends IRowEntity<N>, N extends IEntityWithId>(entities: T[], selections: Set<string>, all: boolean, checkboxVisible: (e: T | N) => boolean, checkboxDisabled: (e: T | N) => boolean, strategy?: TSelectionStrategy) => SelectionState;
