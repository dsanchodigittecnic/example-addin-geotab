import { ReactNode } from "react";
import { IZenComponentProps } from "../../commonHelpers/zenComponent";
import { SelectionState } from "./interfaces";
interface ISelectableHeader extends IZenComponentProps {
    label: string;
    title: ReactNode;
    checked: boolean;
    indeterminate: boolean;
    disabled?: boolean;
    onChange?: (checked: boolean) => void;
    calculateSelectionState: () => SelectionState;
    onSelect?: (selectionState: SelectionState) => void;
    checkboxInHeader?: boolean;
    header?: boolean;
    turnOffSelectAll?: boolean;
}
export declare const SelectableHeader: ({ label, title, checked, indeterminate, disabled, onChange, calculateSelectionState, onSelect, checkboxInHeader, header, turnOffSelectAll }: ISelectableHeader) => import("react/jsx-runtime").JSX.Element;
export declare const TRANSLATIONS: string[];
export {};
