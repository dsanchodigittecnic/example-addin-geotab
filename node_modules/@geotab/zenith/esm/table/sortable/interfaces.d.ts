export declare enum ColumnSortDirection {
    Ascending = "asc",
    Descending = "desc"
}
export interface ISortableValue {
    sortColumn: string;
    sortDirection: ColumnSortDirection;
}
export interface ISortableStorage {
    getItem: () => ISortableValue | undefined;
    setItem: (settings: ISortableValue) => void;
    clear: () => void;
}
export interface ISortableExternalStorage {
    getItem: (key: string) => string | undefined | null;
    removeItem: (key: string) => void;
    setItem: (key: string, settings: string) => void;
}
