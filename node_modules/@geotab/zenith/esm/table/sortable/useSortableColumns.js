import { jsx as _jsx } from "react/jsx-runtime";
import { useCallback, useMemo } from "react";
import { SortableColumnsStorage } from "./sortableStorage";
import { SortableHeaderCell } from "./sortableHeader";
import { ActionsColumn } from "../../dataGrid/columns/actionsColumn/actionsColumn";
const SortableWrapperName = "Sortable";
export const getSortableValue = (pageName, defaultValue) => {
    const storage = new SortableColumnsStorage(pageName, defaultValue);
    return storage.getItem();
};
export const useSortableColumns = (columns, sortable, isMobile) => {
    const settingsStore = useMemo(() => (sortable === null || sortable === void 0 ? void 0 : sortable.storage) || new SortableColumnsStorage((sortable === null || sortable === void 0 ? void 0 : sortable.pageName) || ""), [sortable === null || sortable === void 0 ? void 0 : sortable.pageName, sortable === null || sortable === void 0 ? void 0 : sortable.storage]);
    const sortSettings = useMemo(() => (sortable === null || sortable === void 0 ? void 0 : sortable.value) || settingsStore.getItem(), [settingsStore, sortable === null || sortable === void 0 ? void 0 : sortable.value]);
    const saveSettingsToTheStore = useCallback((newSettings) => 
    // not to block main thread this operation can be postponed
    new Promise((resolve, reject) => {
        setTimeout(() => {
            try {
                settingsStore.setItem(newSettings);
                resolve();
            }
            catch (e) {
                reject(e);
            }
        }, 0);
    }), [settingsStore]);
    const onSort = useCallback(async (newSortSettings) => {
        await saveSettingsToTheStore(newSortSettings);
        sortable === null || sortable === void 0 ? void 0 : sortable.onChange(newSortSettings);
    }, [saveSettingsToTheStore, sortable]);
    const sortableColumns = useMemo(() => {
        if (!sortable) {
            return columns;
        }
        return columns.map(column => {
            const isSortable = column.sortable !== false && !(column.columnComponent instanceof ActionsColumn);
            if (!isSortable) {
                return column;
            }
            const isSortedBy = (sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortColumn) === column.id;
            const wrapper = {
                name: SortableWrapperName,
                renderHeader: (title) => {
                    var _a;
                    return _jsx(SortableHeaderCell, { columnName: column.id, isSortedBy: isSortedBy, sortDirection: isSortedBy ? sortSettings.sortDirection : undefined, title: title, tooltip: column.title, onSortChange: onSort, initialSortDirection: (_a = column.meta) === null || _a === void 0 ? void 0 : _a.initialSortDirection });
                },
                render: (value, _) => value
            };
            const columnWrappers = (column.wrappers || []).filter(w => w.name !== SortableWrapperName);
            return Object.assign(Object.assign({}, column), { wrappers: [...columnWrappers, wrapper] });
        });
    }, [columns, onSort, sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortColumn, sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortDirection, sortable]);
    const feedSortControl = useMemo(() => {
        var _a;
        if (!sortable) {
            return null;
        }
        if (!isMobile) {
            return null;
        }
        const column = columns[0];
        const isSortedBy = (sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortColumn) === column.id;
        return _jsx(SortableHeaderCell, { columnName: column.id.toString(), isSortedBy: isSortedBy, sortDirection: isSortedBy ? sortSettings.sortDirection : undefined, title: column.title, tooltip: column.title, onSortChange: onSort, isMobile: true, initialSortDirection: (_a = column.meta) === null || _a === void 0 ? void 0 : _a.initialSortDirection });
    }, [columns, isMobile, onSort, sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortColumn, sortSettings === null || sortSettings === void 0 ? void 0 : sortSettings.sortDirection, sortable]);
    return { sortableColumns, feedSortControl };
};
