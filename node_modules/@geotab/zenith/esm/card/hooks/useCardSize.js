import { useLayoutEffect, useState } from "react";
import { useResize } from "../../commonHelpers/hooks/useResize";
import { getContainerSize } from "../../cardContainer/helpers/getContainerSize";
export const useCardSize = (callback, elem, size) => {
    const [cardSize, setCardSize] = useState(size);
    useResize(() => {
        var _a;
        setCardSize(getContainerSize(((_a = elem.current) === null || _a === void 0 ? void 0 : _a.clientWidth) || 0));
    }, true);
    useLayoutEffect(() => {
        callback(cardSize);
    }, [cardSize, callback]);
};
