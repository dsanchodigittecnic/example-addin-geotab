import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { createElement, useMemo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { useDriveClassName } from "../../utils/theme/useDriveClassName";
import { useDrive } from "../../utils/theme/useDrive";
import { IconChevronRightSmall } from "../../icons/iconChevronRightSmall";
export const Title = ({ isMobile, link, title, id, icon, iconType, className = "" }) => {
    const driveClassNames = useDriveClassName("zen-card-title");
    const isDrive = useDrive();
    const iconClassName = iconType ? `zen-card-title__icon--${iconType}` : "";
    const iconSize = useMemo(() => isDrive ? "huge" : "large", [isDrive]);
    return _jsxs("div", { className: classNames(["zen-card-title", driveClassNames || ""]), children: [icon
                ? _jsx("div", { className: classNames(["zen-card-title__icon", iconClassName]), children: createElement(icon, { size: iconSize }) })
                : null, link
                ? _jsxs("a", { id: id, className: classNames(["zen-card-title__link"]), href: link, children: [_jsx("span", { title: title, className: classNames(["zen-card-title__link-text"]), children: title }), _jsx("div", { className: "zen-card-title__link-chevron", children: _jsx(IconChevronRightSmall, { size: iconSize, className: "zen-card-title__chevron-icon" }) })] })
                : _jsx("span", { title: title, id: id, className: classNames(["zen-card-title__text", (isMobile && !isDrive) ? "zen-card-title__text--mobile" : "", className]), children: title })] });
};
