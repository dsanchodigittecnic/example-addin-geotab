import { jsx as _jsx } from "react/jsx-runtime";
import { createElement, useMemo } from "react";
import { IconCheckRadio } from "../icons/iconCheckRadio";
import { IconInfoCircle } from "../icons/iconInfoCircle";
import { IconWarning } from "../icons/iconWarning";
import { IconException } from "../icons/iconException";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
import { classNames } from "../commonHelpers/classNames/classNames";
import { IconGraphPresentation } from "../icons/iconGraphPresentation";
const TypeIcon = ({ type, className }) => {
    const iconsByType = useMemo(() => ({
        "error": IconException,
        "success": IconCheckRadio,
        "warning": IconWarning,
        "info": IconInfoCircle,
        "upsell": IconGraphPresentation
    }), []);
    const bannerDriveClassName = useDriveClassName("zen-banner__icon-block");
    return _jsx("div", { className: classNames(["zen-banner__icon-block", className || ""]), children: createElement(iconsByType[type], {
            size: bannerDriveClassName ? "huge" : "large",
            className: `zen-push__icon--${type}`
        }) });
};
export default TypeIcon;
