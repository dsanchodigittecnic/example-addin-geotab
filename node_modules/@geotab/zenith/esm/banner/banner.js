import { jsx as _jsx } from "react/jsx-runtime";
import { BannerSingleLine } from "./bannerSingleLine";
import { BannerMultiline } from "./bannerMultipLine";
import { useMobile } from "../commonHelpers/hooks/useMobile";
export const Banner = ({ children, header, type = "warning", icon, action, size, onClose, multiline, role = "status", ariaAtomic = "true", ariaLive = "polite", primaryAction, className }) => {
    const isMobile = useMobile();
    const isMultiline = multiline === undefined ? isMobile : multiline;
    return isMultiline ? _jsx(BannerMultiline, { children,
        header,
        type,
        icon,
        size,
        action,
        onClose,
        role,
        ariaAtomic,
        ariaLive,
        primaryAction,
        className }) : _jsx(BannerSingleLine, { children,
        header,
        type,
        size,
        icon,
        action,
        onClose,
        role,
        ariaAtomic,
        ariaLive,
        primaryAction,
        className });
};
