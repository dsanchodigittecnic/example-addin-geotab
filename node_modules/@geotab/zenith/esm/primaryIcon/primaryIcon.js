import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { renderIcon } from "./renderIcon";
export const PrimaryIcon = ({ icon, className, onClick, link, target, title, disabled, style }) => {
    const iconNode = useMemo(() => renderIcon(icon, className), [icon, className]);
    const topClassName = useMemo(() => classNames([
        "zen-primary-icon",
        onClick || link ? "zen-primary-icon--active" : "",
        disabled ? "zen-primary-icon--disabled" : "",
        style === "secondary" ? "zen-primary-icon--secondary" : "",
        className || ""
    ]), [onClick, link, className, disabled, style]);
    if (onClick) {
        return _jsx("button", { className: topClassName, onClick: onClick, disabled: disabled, title: title, "aria-label": title, children: iconNode });
    }
    if (link) {
        return _jsx("a", { className: topClassName, href: disabled ? undefined : link, target: target, title: title, "aria-label": title, children: iconNode });
    }
    return _jsx("div", { className: topClassName, children: iconNode });
};
