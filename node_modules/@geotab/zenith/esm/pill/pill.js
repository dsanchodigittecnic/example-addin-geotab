var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { NonActionablePill, TRANSLATIONS as NON_ACTIONABLE_PILL_TRANSLATIONS } from "./components/pillNonActionable/pillNonActionable";
import { ActionablePill } from "./components/pillActionable/pillActionable";
import { getPillSizeClassName } from "./utils/getPillSizeClassName";
import { useDrive } from "../utils/theme/useDrive";
export const Pill = (_a) => {
    var { className } = _a, props = __rest(_a, ["className"]);
    const isActionable = !!props.onChange && props.active !== undefined;
    const isFixedSize = props.size && props.size !== "default" && props.size !== "fullWidth";
    const isEmptyContent = !props.children;
    const isDrive = useDrive();
    const pillClassNames = classNames([
        "zen-pill-new",
        isDrive ? "zen-pill-new--drive" : "",
        !isFixedSize ? getPillSizeClassName(props.size || "default") : "",
        isEmptyContent ? "zen-pill-new--empty" : "",
        className !== null && className !== void 0 ? className : ""
    ]);
    if (isActionable) {
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        return _jsx(ActionablePill, Object.assign({ active: props.active, onChange: props.onChange, className: pillClassNames }, props));
    }
    return _jsx(NonActionablePill, Object.assign({ className: pillClassNames }, props));
};
export const TRANSLATIONS = [
    ...NON_ACTIONABLE_PILL_TRANSLATIONS
];
