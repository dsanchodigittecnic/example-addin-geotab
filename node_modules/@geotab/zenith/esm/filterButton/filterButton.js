var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo, forwardRef } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { IconChevronDownSmall } from "../icons/iconChevronDownSmall";
import { Button } from "../button/button";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
import { useDrive } from "../utils/theme/useDrive";
// eslint-disable-next-line @typescript-eslint/naming-convention
export const FilterButton = forwardRef((function FilterButton(_a, ref) {
    var { id, title, disabled, quantity, className, children, onClick, onMouseOver, onMouseOut, fullWidth, maxWidth, minWidth, isActive = false, type = "secondary", ariaLabel } = _a, other = __rest(_a, ["id", "title", "disabled", "quantity", "className", "children", "onClick", "onMouseOver", "onMouseOut", "fullWidth", "maxWidth", "minWidth", "isActive", "type", "ariaLabel"]);
    const driveComponentClass = useDriveClassName("zen-filter-button");
    const isDrive = useDrive();
    const hasQuantity = typeof quantity === "number";
    const isLargeNumber = hasQuantity && quantity > 99;
    const onClickEvent = useMemo(() => onClick
        ? (e) => onClick(e.nativeEvent)
        : () => { }, [onClick]);
    const inlineStyle = useMemo(() => maxWidth || minWidth ? { style: Object.assign(Object.assign({}, (maxWidth ? { maxWidth: `${maxWidth}px` } : {})), (minWidth ? { minWidth: `${minWidth}px` } : {})) } : {}, [maxWidth, minWidth]);
    return _jsxs(Button, Object.assign({ id: id, title: title, disabled: disabled, type: type, className: classNames([
            "zen-filter-button",
            driveComponentClass || "",
            `zen-filter-button--${type}`,
            className || "",
            isActive ? "zen-filter-button--active" : "",
            fullWidth ? "zen-filter-button--full-width" : ""
        ]), onClick: onClickEvent, onMouseOver: onMouseOver, onMouseOut: onMouseOut, ref: ref, htmlType: "button", "aria-label": ariaLabel }, inlineStyle, other, { children: [children ? _jsx("div", { className: classNames(["zen-filter-button__label", "zen-ellipsis", fullWidth ? "zen-filter-button__label--full-width" : ""]), children: children }) : null, _jsxs("div", { className: "zen-filter-button__tags", children: [hasQuantity ? _jsx("div", { className: "zen-filter-button__quantity", children: isLargeNumber ? "99+" : quantity }) : null, _jsx("div", { className: "zen-filter-button__icon", children: _jsx(IconChevronDownSmall, { size: isDrive ? "huge" : "large" }) })] })] }));
}));
