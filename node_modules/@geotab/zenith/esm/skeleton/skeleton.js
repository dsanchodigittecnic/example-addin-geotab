import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useLanguage } from "../utils/localization/useLanguage";
export const Skeleton = ({ type = "text", height, width = "100%", className }) => {
    const { translate } = useLanguage();
    const classNameList = classNames(["zen-skeleton", `zen-skeleton--${type}`, className || ""]);
    return _jsx("div", { role: "progressbar", "aria-label": translate("Loading"), style: { height: height, width: width ? width : "100%" }, className: classNameList.trim() });
};
