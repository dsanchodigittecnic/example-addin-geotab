import { FOCUSABLE_SELECTOR } from "../../utils/focusableSelector";
export const findLastFocusable = (element) => {
    if (!element) {
        return undefined;
    }
    const ulElement = element.closest("ul");
    const focusable = ulElement === null || ulElement === void 0 ? void 0 : ulElement.querySelectorAll(FOCUSABLE_SELECTOR);
    return focusable === null || focusable === void 0 ? void 0 : focusable[focusable.length - 1];
};
