import { FOCUSABLE_SELECTOR } from "../../utils/focusableSelector";
export const findFirstFocusable = (element) => {
    if (!element) {
        return undefined;
    }
    const ulElement = element.closest("ul");
    return ulElement === null || ulElement === void 0 ? void 0 : ulElement.querySelector(FOCUSABLE_SELECTOR);
};
