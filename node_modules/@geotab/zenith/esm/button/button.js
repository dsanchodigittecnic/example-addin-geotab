var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import * as React from "react";
import { ButtonType } from "./buttonType";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
// eslint-disable-next-line @typescript-eslint/naming-convention
export const Button = React.forwardRef(function Button(_a, ref) {
    var { className, type, disabled, id, title, onClick, onMouseOver, onMouseOut, onMouseDown, onMouseUp, onKeyDown, onKeyUp, children, link, target, htmlType, rel } = _a, other = __rest(_a, ["className", "type", "disabled", "id", "title", "onClick", "onMouseOver", "onMouseOut", "onMouseDown", "onMouseUp", "onKeyDown", "onKeyUp", "children", "link", "target", "htmlType", "rel"]);
    const modifierClassName = type && type !== ButtonType.Secondary ? `zen-button--${type}` : "";
    const disabledClassName = disabled ? "zen-button--disabled" : "";
    const driveClasses = useDriveClassName("zen-button");
    const commonArgs = {
        id,
        title,
        "aria-label": title,
        className: classNames([
            "zen-button",
            driveClasses || "",
            modifierClassName,
            disabledClassName,
            className !== null && className !== void 0 ? className : ""
        ])
    };
    if (link) {
        return _jsx("a", Object.assign({ role: "link", rel: rel, target: target, href: disabled ? undefined : link, "aria-disabled": disabled, onKeyDown: onKeyDown, onKeyUp: onKeyUp, onMouseOver: onMouseOver, onMouseOut: onMouseOut, onClick: onClick }, commonArgs, other, { children: children }));
    }
    return _jsx("button", Object.assign({ onClick: onClick, onMouseOver: onMouseOver, onMouseOut: onMouseOut, onMouseDown: onMouseDown, onMouseUp: onMouseUp, onKeyDown: onKeyDown, onKeyUp: onKeyUp, ref: ref, type: htmlType, disabled: disabled }, commonArgs, other, { children: children }));
});
Button.displayName = "Button";
