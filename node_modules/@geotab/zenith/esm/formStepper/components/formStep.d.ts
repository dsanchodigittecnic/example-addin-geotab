import { FC } from "react";
import "./formStep.less";
export interface IStepItem {
    id: string;
    label?: string;
    optional?: boolean;
}
export interface IFormStep extends IStepItem {
    isFirst?: boolean;
    value?: string | number;
    onClick?: () => void;
    isError?: boolean;
    isCurrent?: boolean;
    isComplete?: boolean;
}
export declare const FormStep: FC<IFormStep>;
