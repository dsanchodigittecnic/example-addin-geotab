import { jsx as _jsx } from "react/jsx-runtime";
import { IconCheck } from "../../icons/iconCheck";
import { useMobile } from "../../commonHelpers/hooks/useMobile";
import { FormStepRaw } from "./formStepRaw";
import { useLanguage } from "../../utils/localization/useLanguage";
export const FormStep = ({ isFirst, value, isError = false, isCurrent = false, isComplete = false, label, onClick, optional, id }) => {
    const isMobile = useMobile();
    const { translate } = useLanguage();
    const type = isCurrent ? "current" : isError ? "error" : isComplete ? "complete" : "incomplete";
    const content = isCurrent ? value : isError ? "!" : isComplete ? _jsx(IconCheck, {}) : value;
    const labelSecondary = optional ? translate("Optional") : undefined;
    return (_jsx("div", { className: `zen-form-step ${isFirst ? "zen-form-step--first" : ""} ${isMobile ? "zen-form-step--mobile" : ""} zen-form-step--${type}`, children: _jsx(FormStepRaw, { id: id, type: type, isFirst: isFirst, isMobile: isMobile, onClick: onClick, content: content, label: label, labelSecondary: labelSecondary }) }));
};
