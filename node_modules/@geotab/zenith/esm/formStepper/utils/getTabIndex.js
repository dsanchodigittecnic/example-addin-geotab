export const getTabIndex = (type) => {
    switch (type) {
        case "complete":
        case "current":
        case "error":
            return 0;
        case "incomplete":
        default:
            return -1;
    }
};
