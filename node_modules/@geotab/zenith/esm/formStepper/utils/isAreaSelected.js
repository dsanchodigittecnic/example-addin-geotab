export const isAreaSelected = (type) => {
    switch (type) {
        case "current":
            return true;
        case "complete":
        case "error":
        case "incomplete":
        default:
            return false;
    }
};
