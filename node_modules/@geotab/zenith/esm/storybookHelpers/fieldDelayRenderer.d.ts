import { IEntityWithId } from "../commonHelpers/entity";
export declare class FieldDelayRenderer<T extends IEntityWithId> {
    private readonly store;
    getRenderer(colId: keyof T, entity: T): T[keyof T] | Promise<T[keyof T]>;
}
