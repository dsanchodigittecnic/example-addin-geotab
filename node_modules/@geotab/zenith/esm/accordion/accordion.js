import { jsx as _jsx } from "react/jsx-runtime";
import { createElement as _createElement } from "react";
import { AccordionItem } from "./accordionItem/accordionItem";
import { classNames } from "../commonHelpers/classNames/classNames";
export const Accordion = ({ data = [], className = "", activeIds = [], onItemClick }) => {
    const fullClassName = classNames(["zen-accordion", className]);
    return _jsx("div", { className: fullClassName, children: data.map((item) => _createElement(AccordionItem, Object.assign({}, item, { key: item.id, isActive: activeIds.indexOf(item.id) !== -1, onClick: () => onItemClick && onItemClick(item.id) }))) });
};
