import { CheckboxState } from "../checkbox/checkboxState";
import "./selectList.less";
import { ISelectListItem } from "./selectListItem";
import { IZenIdComponentProps } from "../commonHelpers/zenComponent";
import { FC } from "react";
interface ICommonProps extends IZenIdComponentProps {
    multiselect?: boolean;
    selectItemOnFocus?: boolean;
    showCheckbox?: boolean;
    items: ISelectListItem[];
    value?: Map<string, CheckboxState>;
    title?: string;
    disabled?: boolean;
    forceSelection?: boolean;
    style?: React.CSSProperties;
}
export interface IPassiveSelectList extends ICommonProps {
    activeIndex?: number;
    onChangeItem?: (id: string, state: CheckboxState) => void;
}
export interface ISelectList extends ICommonProps {
    title: string;
    onChange?: (state: Map<string, CheckboxState>) => void;
    onClick?: (id: string) => void;
}
export interface IListState {
    itemsState: Map<string, CheckboxState>;
    isItemsStateChanged: boolean;
    activeIndex: number;
    searchStr: string;
    level: string;
}
export interface IListStateProps {
    value?: Map<string, CheckboxState>;
    multiselect: boolean;
    items: ISelectListItem[];
}
export declare const defaultListItemHeight = 40;
export declare const SelectList: FC<ISelectList>;
export declare const PassiveSelectList: (props: any) => import("react/jsx-runtime").JSX.Element;
export {};
