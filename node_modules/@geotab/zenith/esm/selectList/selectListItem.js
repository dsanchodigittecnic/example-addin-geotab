import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { CheckboxState } from "../checkbox/checkboxState";
import { CheckboxNonInteractive } from "../checkboxNonInteractive/checkboxNonInteractive";
import { classNames } from "../commonHelpers/classNames/classNames";
import { IconChevronRight } from "../icons/iconChevronRight";
import { useDriveClassName } from "../utils/theme/useDriveClassName";
export const SelectListItem = ({ id, disabled = false, state = CheckboxState.Off, showCheckbox = false, focused = false, onChange, onClick, multiLevel, className = "", children }) => {
    const driveClassName = useDriveClassName("zen-select-item");
    const onClickFunc = () => {
        if (disabled) {
            return;
        }
        const newState = state === CheckboxState.On ? CheckboxState.Off : CheckboxState.On;
        if (multiLevel && !showCheckbox) {
            onClick && onClick(id);
        }
        onChange && onChange(id, newState);
    };
    return _jsx("li", { "data-id": id, className: classNames([
            "zen-select-item",
            showCheckbox ? "zen-select-item--with-checkbox" : "",
            focused ? "zen-select-item--focused" : "",
            driveClassName || "",
            className
        ]), role: "option", "aria-selected": state === CheckboxState.On, "aria-disabled": disabled, onClick: onClickFunc, id: id, children: _jsxs("div", { className: classNames([
                "zen-select-list__item-content",
                "zen-select-item__wrapper",
                "zen-ellipsis",
                multiLevel ? "zen-select-item__wrapper--with-arrow" : ""
            ]), children: [showCheckbox ?
                    _jsx(CheckboxNonInteractive, { className: "zen-select-item__control-view", checked: state === CheckboxState.On, indeterminate: state === CheckboxState.Indeterminate, disabled: disabled, children: children })
                    : children, multiLevel && !showCheckbox ? _jsx(IconChevronRight, { size: "large" }) : null] }) });
};
