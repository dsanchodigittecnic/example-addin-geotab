export const getSelectItemsForTimePicker = (count) => {
    const resultArray = [];
    for (let i = 0; i <= count; i++) {
        const value = i.toString().padStart(2, "0");
        resultArray.push({ id: value, children: value });
    }
    return resultArray;
};
