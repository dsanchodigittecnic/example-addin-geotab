import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { ButtonType } from "../../button/buttonType";
import { IconLayoutWindow3 } from "../../icons/iconLayoutWindow3";
import { IconLayoutWindow4 } from "../../icons/iconLayoutWindow4";
import { SvgIconSize } from "../../svgIcon/svgIconSize";
import { ButtonIconPosition, TextIconButton } from "../../textIconButton/textIconButton";
import { useLanguage } from "../../utils/localization/useLanguage";
export const FeedExpandControl = ({ className = "", isExpanded, onClick }) => {
    const { translate } = useLanguage();
    const classes = classNames([className, "zen-data-feed-expand-control"]);
    const title = isExpanded ? translate("Collapse feed") : translate("Expand feed");
    return _jsx(TextIconButton, { className: classes, icon: isExpanded ? IconLayoutWindow4 : IconLayoutWindow3, iconSize: SvgIconSize.Large, iconPosition: ButtonIconPosition.Start, "aria-label": title, title: title, onClick: onClick, type: ButtonType.TertiaryBlack });
};
export const TRANSLATIONS = [
    "Collapse feed",
    "Expand feed"
];
