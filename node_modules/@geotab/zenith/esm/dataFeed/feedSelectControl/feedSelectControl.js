import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { SvgIconSize } from "../../svgIcon/svgIconSize";
import { ButtonIconPosition, TextIconButton } from "../../textIconButton/textIconButton";
import { ButtonType } from "../../button/buttonType";
import { IconBulkSelect } from "../../icons/iconBulkSelect";
import { forwardRef } from "react";
export const FeedSelectControl = forwardRef(({ className = "", title, onClick }, ref) => {
    const classes = classNames([className, "data-feed-select-control"]);
    return _jsx(TextIconButton, { ref: ref, className: classes, icon: IconBulkSelect, iconSize: SvgIconSize.Large, iconPosition: ButtonIconPosition.Start, "aria-label": title, title: title, onClick: onClick, type: ButtonType.TertiaryBlack });
});
FeedSelectControl.displayName = "FeedSelectControl";
