import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Button } from "../button/button";
import { ButtonType } from "../button/buttonType";
import { useMemo } from "react";
import { useLanguage } from "../utils/localization/useLanguage";
export const DateSelectionWrapper = ({ onCancel, onClear, onReset, onApply, children }) => {
    const { translate } = useLanguage();
    const cancelControl = useMemo(() => onCancel ? _jsx(Button, { className: "zen-date-selection__cancel", onClick: onCancel, type: ButtonType.Tertiary, children: translate("Cancel") }) : "", [onCancel, translate]);
    const clearControl = useMemo(() => onClear ? _jsx(Button, { className: "zen-date-selection__clear", onClick: onClear, type: ButtonType.Secondary, children: translate("Clear") }) : "", [onClear, translate]);
    const resetControl = useMemo(() => onReset ? _jsx(Button, { className: "zen-date-selection__reset", onClick: onReset, type: ButtonType.Secondary, children: translate("Reset") }) : "", [onReset, translate]);
    const applyControl = useMemo(() => onApply ? _jsx(Button, { className: "zen-date-selection__apply", onClick: onApply, type: ButtonType.Primary, children: translate("Apply") }) : "", [onApply, translate]);
    return _jsxs("div", { className: "zen-date-selection", children: [_jsx("div", { className: "zen-date-selection__control", children: children }), _jsx("div", { className: "zen-date-selection__delimiter" }), _jsxs("div", { className: "zen-date-selection__buttons", children: [cancelControl, _jsx("div", { className: "zen-date-selection__buttons-spacer" }), clearControl, resetControl, applyControl] })] });
};
export const TRANSLATIONS = [
    "Cancel",
    "Clear",
    "Reset",
    "Apply"
];
