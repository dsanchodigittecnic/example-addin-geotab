import { jsx as _jsx } from "react/jsx-runtime";
import { useCallback, useMemo } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useLayoutFullScreen } from "./layoutFullScreenProvider";
import { LayoutFullScreenElementProvider } from "./layoutFullScreenElementProvider";
import { useUniqueId } from "../commonHelpers/useUniqueId";
export const LayoutFullScreen = ({ className = "", children }) => {
    const dataId = useUniqueId();
    const { setFullScreenElement, fullScreenElement } = useLayoutFullScreen();
    const fullScreenElContent = useMemo(() => _jsx("div", { className: classNames(["zen-layout__full-screen-element",
            fullScreenElement === dataId ? "zen-layout__full-screen-element--visible" : "", className]), "data-id": dataId, children: children }), [children, className, dataId, fullScreenElement]);
    const handleSetFullScreenElement = useCallback((val) => {
        setFullScreenElement(val);
    }, [setFullScreenElement]);
    return (_jsx(LayoutFullScreenElementProvider, { elementId: dataId, setFullScreenElement: handleSetFullScreenElement, children: fullScreenElContent }));
};
export const LayoutFullScreenDisplayName = "LayoutFullScreen";
LayoutFullScreen.displayName = LayoutFullScreenDisplayName;
