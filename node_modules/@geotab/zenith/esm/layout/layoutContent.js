import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useLayoutSize } from "./layoutSizeProvider";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { useLayoutFullScreen } from "./layoutFullScreenProvider";
export const LayoutContent = ({ className = "", children, internalPaddings = true }) => {
    const isMobile = useMobile();
    const { outerContentHeight, isSetHeight } = useLayoutSize();
    const { fullScreenElement } = useLayoutFullScreen();
    const otherStyle = useMemo(() => (isSetHeight && outerContentHeight ? { style: { height: `${outerContentHeight}px` } } : {}), [isSetHeight, outerContentHeight]);
    return (_jsx("div", Object.assign({ className: classNames(["zen-layout__content", fullScreenElement ? "zen-layout__content--full-screen" : "", internalPaddings ? "zen-layout__content--internal-paddings" : "", isMobile ? "zen-layout__content--mobile" : "", className]) }, otherStyle, { children: children })));
};
