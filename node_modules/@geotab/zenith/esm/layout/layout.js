import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
/* eslint-disable @typescript-eslint/naming-convention */
import React, { memo, useId } from "react";
import { LayoutHeader } from "./layoutHeader";
import { LayoutContent } from "./layoutContent";
import { LayoutSummaryTiles } from "./layoutSummaryTiles";
import { cloneElement, createRef, useCallback, useEffect, useMemo, useState } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useThrottle } from "../commonHelpers/hooks/useThrottle";
import { useResize } from "../commonHelpers/hooks/useResize";
import { LayoutSizeProvider } from "./layoutSizeProvider";
import { LayoutFullScreenProvider } from "./layoutFullScreenProvider";
import { getArrayOfChildren } from "../header/headerHelpers";
import { HeaderDisplayName } from "../header/header";
import { SummaryTileBarDisplayName } from "../summaryTileBar/summaryTileBar";
import { LayoutFullScreen } from "./layoutFullScreen";
import { SummaryTileDisplayName } from "../summaryTile/summaryTile";
import { FullScreenButton } from "../dataGrid/entitiesListActions/actions/fullscreenButton";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { PageHeaderDisplayName } from "../pageHeader/pageHeader";
export const getContentPaddings = (isMobile, startWithToolbar) => {
    const paddingsCount = startWithToolbar ? 1 : 2;
    return isMobile ? paddingsCount * 8 : paddingsCount * 24;
};
const LayoutInner = ({ id, className, pageScrolling = true, children }) => {
    const layoutId = useId();
    const layoutRef = createRef();
    const middleRef = createRef();
    const isMobile = useMobile();
    const [middleElBottom, setMiddleElBottom] = useState(undefined);
    const [fullScreenElement, setFullScreenElement] = useState(undefined);
    const [minHeight, setMinHeight] = useState(window.innerHeight);
    const [startWithToolbar, setStartWithToolbar] = useState(false);
    const isFullScreen = useCallback(() => fullScreenElement !== undefined, [fullScreenElement]);
    const [header, summaryTileBar, content] = useMemo(() => {
        const h = [];
        const s = [];
        const c = [];
        const sc = [];
        const cc = [];
        const childrenArray = getArrayOfChildren(children).filter((child) => child !== null && child !== undefined);
        // eslint-disable-next-line complexity
        childrenArray.forEach((child) => {
            const isValidElement = React.isValidElement(child);
            if (!h.length && child.type === LayoutHeader && isValidElement) {
                h.push(child);
                return;
            }
            if (!h.length && isValidElement && child.type.displayName === HeaderDisplayName) {
                h.push(_jsx(LayoutHeader, { children: child }, "header-key"));
                return;
            }
            if (!h.length && isValidElement && child.type.displayName === PageHeaderDisplayName) {
                h.push(_jsx(LayoutHeader, { children: child }, "header-key"));
                return;
            }
            if (!s.length && child.type === LayoutSummaryTiles && isValidElement) {
                s.push(child);
                return;
            }
            if (!s.length && isValidElement && child.type.displayName === SummaryTileBarDisplayName) {
                s.push(_jsx(LayoutSummaryTiles, { children: child }, "summary-tiles-key"));
                return;
            }
            if (!s.length && isValidElement && child.type.displayName === SummaryTileDisplayName) {
                sc.push(child);
                return;
            }
            if (!c.length && isValidElement && child.type === LayoutContent) {
                c.push(child);
                return;
            }
            if (isValidElement && !c.length) {
                cc.push(child);
                return;
            }
        });
        if (cc.length && !c.length) {
            c.push(_jsx(LayoutContent, { children: cc }, "content-key"));
        }
        if (sc.length && !s.length) {
            s.push(_jsx(LayoutSummaryTiles, { children: sc }, "summary-tiles-key"));
        }
        return [h, s, c];
    }, [children]);
    const calculateLayoutParams = useCallback((elem, middleEl) => {
        var _a, _b;
        if (!elem) {
            return;
        }
        const layoutO = elem.getBoundingClientRect().top === (((_a = elem.parentElement) === null || _a === void 0 ? void 0 : _a.getBoundingClientRect().top) || 0) ? elem.getBoundingClientRect().top
            : Math.abs(elem.getBoundingClientRect().top - (((_b = elem.parentElement) === null || _b === void 0 ? void 0 : _b.getBoundingClientRect().top) || 0));
        if (middleEl) {
            const middleElRect = middleEl.getBoundingClientRect();
            const middleElBottomPosition = middleEl.offsetTop + middleElRect.height;
            setMiddleElBottom(middleElBottomPosition);
        }
        const layoutOffset = layoutO > window.innerHeight ? 0 : layoutO;
        setMinHeight(window.innerHeight - layoutOffset);
    }, []);
    const checkIsStartWithToolbar = useCallback((fullScreen) => {
        var _a;
        const contentEl = (_a = layoutRef.current) === null || _a === void 0 ? void 0 : _a.querySelector(".zen-layout__content");
        if (!contentEl) {
            return false;
        }
        const selector = fullScreen ? ".zen-layout__full-screen-element--visible .zen-data-grid-actions"
            : ".zen-data-grid-actions";
        const container = fullScreen ? contentEl : contentEl.firstElementChild;
        const result = container === null || container === void 0 ? void 0 : container.querySelector(selector);
        if (result) {
            return true;
        }
        return false;
    }, [layoutRef]);
    const resizeCallBack = useThrottle(() => {
        calculateLayoutParams(layoutRef.current, middleRef.current);
    }, 100);
    useResize(resizeCallBack, true);
    const isFullScreenCurrent = isFullScreen();
    useEffect(() => {
        calculateLayoutParams(layoutRef.current, middleRef.current);
    }, [calculateLayoutParams, layoutRef, middleRef]);
    useEffect(() => {
        setTimeout(() => {
            const isStartWithToolbar = checkIsStartWithToolbar(isFullScreenCurrent);
            setStartWithToolbar(isStartWithToolbar);
        }, 0);
    }, [isFullScreenCurrent, checkIsStartWithToolbar]);
    const memoizedHeader = useMemo(() => header, [header]);
    const memoizedContent = useMemo(() => content, [content]);
    const memoizedSummaryTileBar = useMemo(() => summaryTileBar[0] ? cloneElement(summaryTileBar[0], { ref: middleRef })
        : _jsx("div", { className: "zen-layout__middle-element", ref: middleRef }), [middleRef, summaryTileBar]);
    const handleSetFullScreenElement = useCallback((dataId) => {
        setFullScreenElement(dataId);
    }, []);
    const memoizedOuterContentHeight = useMemo(() => isFullScreenCurrent ? minHeight : minHeight - (middleElBottom || 0), [isFullScreenCurrent, middleElBottom, minHeight]);
    const memoizedContentHeight = useMemo(() => {
        const contentPaddings = getContentPaddings(isMobile, startWithToolbar);
        return memoizedOuterContentHeight - contentPaddings;
    }, [isMobile, memoizedOuterContentHeight, startWithToolbar]);
    const heightStyle = useMemo(() => {
        if (isFullScreenCurrent) {
            return { style: { maxHeight: `${minHeight}px`, minHeight: `${minHeight}px` } };
        }
        if (!pageScrolling) {
            return { style: { maxHeight: `${minHeight}px`, overflowY: "hidden", minHeight: `${minHeight}px` } };
        }
        return { style: { minHeight: `${minHeight}px` } };
    }, [isFullScreenCurrent, minHeight, pageScrolling]);
    useEffect(() => {
        var _a, _b;
        if (isFullScreenCurrent) {
            const fullscreenEl = (_a = layoutRef.current) === null || _a === void 0 ? void 0 : _a.querySelector(".zen-layout__full-screen-element--visible");
            if (fullscreenEl) {
                let parent = fullscreenEl.parentElement;
                while (parent && !parent.classList.contains("zen-layout__content")) {
                    parent.dataset["fullScreenParent"] = "true";
                    parent = parent.parentElement;
                }
            }
        }
        else {
            const previousFullscreenParent = (_b = layoutRef.current) === null || _b === void 0 ? void 0 : _b.querySelectorAll("[data-full-screen-parent='true']");
            if (previousFullscreenParent && previousFullscreenParent.length) {
                previousFullscreenParent.forEach((el) => {
                    el.removeAttribute("data-full-screen-parent");
                });
            }
        }
    }, [isFullScreenCurrent, layoutRef]);
    return _jsx("section", Object.assign({ id: id || layoutId, ref: layoutRef, className: classNames(["zen-layout", !pageScrolling ? "zen-layout--no-scroll" : "", className || ""]) }, heightStyle, { children: _jsx(LayoutFullScreenProvider, { isFullScreen: isFullScreen, setFullScreenElement: handleSetFullScreenElement, fullScreenElement: fullScreenElement, children: _jsxs(LayoutSizeProvider, { contentHeight: memoizedContentHeight, outerContentHeight: memoizedOuterContentHeight, isSetHeight: !pageScrolling, children: [isFullScreenCurrent ? null : memoizedHeader, isFullScreenCurrent ? null : memoizedSummaryTileBar, memoizedContent] }) }) }));
};
export const Layout = memo(LayoutInner);
Layout.Header = LayoutHeader;
Layout.Content = LayoutContent;
Layout.SummaryTiles = LayoutSummaryTiles;
Layout.FullScreen = LayoutFullScreen;
Layout.FullScreenButton = FullScreenButton;
