var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import React, { useCallback, useEffect, useId, useMemo, useState } from "react";
import { SelectList } from "../selectList/selectList";
import { CheckboxState } from "../checkbox/checkboxState";
import { Popup } from "../popup/popup";
import { classNames } from "../commonHelpers/classNames/classNames";
import { ButtonIconPosition, TextIconButton } from "../textIconButton/textIconButton";
import { IconChevronDownSmall } from "../icons/iconChevronDownSmall";
const getSelected = (map) => {
    for (const [key, val] of map.entries()) {
        if (val === CheckboxState.On) {
            return key;
        }
    }
    return null;
};
export const ParallelSelection = ({ selected, items, triggerRef, title, onSelect, isMobileView, alignment }) => {
    const selectListId = useId();
    const [outerIsOpen, setOuterIsOpen] = useState(undefined);
    const handleSelection = useCallback((state) => {
        const selection = getSelected(state);
        if (selection) {
            onSelect(selection);
            setOuterIsOpen(false);
        }
    }, [onSelect]);
    useEffect(() => {
        if (outerIsOpen === false) {
            setOuterIsOpen(undefined);
        }
    }, [outerIsOpen]);
    const selectList = useMemo(() => _jsx(SelectList, { id: selectListId, title: title, onChange: handleSelection, multiselect: false, selectItemOnFocus: false, value: new Map([[selected, CheckboxState.On]]), items: items, className: "zen-parallel-selection__select" }), [selectListId, title, handleSelection, selected, items]);
    return _jsx(Popup, { alignment: alignment, className: classNames(["zen-parallel-selection zen-parallel-selection-popup", isMobileView ? "zen-parallel-selection-popup--mobile" : ""]), triggerRef: triggerRef, triggerBy: "click", outerIsOpen: outerIsOpen, recalculateOnScroll: true, children: selectList });
};
// eslint-disable-next-line @typescript-eslint/naming-convention
export const ParallelSelectionTrigger = React.forwardRef(function ParallelSelectionButton(_a, ref) {
    var { className, title } = _a, otherProps = __rest(_a, ["className", "title"]);
    return _jsx(TextIconButton, Object.assign({ className: className || "", type: "tertiary-black", icon: IconChevronDownSmall, ref: ref, title: title, iconPosition: ButtonIconPosition.Start }, otherProps));
});
