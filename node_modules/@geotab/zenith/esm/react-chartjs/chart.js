var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useEffect, useRef, forwardRef, useCallback } from "react";
import { Chart as ChartJS } from "chart.js/auto";
import { reforwardRef, cloneData, setOptions, setLabels, setDatasets } from "./utils";
const ChartComponent = function (props, ref) {
    const _a = props, { height = 150, width = 300, redraw = false, datasetIdKey, type, data, options, plugins = [], fallbackContent, updateMode } = _a, canvasProps = __rest(_a, ["height", "width", "redraw", "datasetIdKey", "type", "data", "options", "plugins", "fallbackContent", "updateMode"]);
    const canvasRef = useRef(null);
    const chartRef = useRef();
    const renderChart = useCallback(() => {
        if (!canvasRef.current) {
            return;
        }
        chartRef.current = new ChartJS(canvasRef.current, {
            type,
            data: cloneData(data, datasetIdKey),
            options: options && Object.assign({}, options),
            plugins
        });
        reforwardRef(ref, chartRef.current);
    }, [data, datasetIdKey, options, plugins, ref, type]);
    const destroyChart = useCallback(() => {
        reforwardRef(ref, null);
        if (chartRef.current) {
            chartRef.current.destroy();
            chartRef.current = null;
        }
    }, [ref]);
    useEffect(() => {
        if (!redraw && chartRef.current && options) {
            setOptions(chartRef.current, options);
        }
    }, [redraw, options]);
    useEffect(() => {
        if (!redraw && chartRef.current) {
            setLabels(chartRef.current.config.data, data.labels);
        }
    }, [redraw, data.labels]);
    useEffect(() => {
        if (!redraw && chartRef.current) {
            setDatasets(chartRef.current.config.data, data.datasets, datasetIdKey);
        }
    }, [redraw, data.datasets, datasetIdKey]);
    useEffect(() => {
        if (!chartRef.current) {
            return;
        }
        if (redraw) {
            destroyChart();
            setTimeout(renderChart);
        }
        else {
            chartRef.current.update(updateMode);
        }
    }, [redraw, options, data.labels, data.datasets, updateMode, destroyChart, renderChart]);
    useEffect(() => {
        if (!chartRef.current) {
            return;
        }
        destroyChart();
        setTimeout(renderChart);
    }, [destroyChart, renderChart, type]);
    useEffect(() => {
        renderChart();
        return () => destroyChart();
    }, [destroyChart, renderChart]);
    return (_jsx("div", { style: { position: "relative", width: "100%", height: "100%" }, children: _jsx("canvas", Object.assign({ ref: canvasRef, role: "img", height: height, width: width }, canvasProps, { children: fallbackContent })) }));
};
export const Chart = forwardRef(ChartComponent);
