export const isDropdownStateTheSame = (newState, oldState) => {
    if (newState.length !== oldState.length) {
        return false;
    }
    return newState.sort().join(",") === oldState.sort().join(",");
};
