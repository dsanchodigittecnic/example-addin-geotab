export const setGridProp = (gridState, prop, stateProp, maxValue) => {
    if (prop && typeof prop === "number" && prop > 0) {
        gridState[stateProp] = prop > maxValue ? maxValue : prop;
    }
};
