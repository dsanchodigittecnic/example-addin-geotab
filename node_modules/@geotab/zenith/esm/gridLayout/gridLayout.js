import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo, useRef, useState } from "react";
import { classNames } from "../commonHelpers/classNames/classNames";
import { useGridClasses } from "./hooks/useGridClasses";
import { useStretch } from "./hooks/useStretch";
export const GridLayout = ({ children, className = "", stretch = false }) => {
    const childrenArr = useMemo(() => [], []);
    const [childrenArrState, setChildrenArrState] = useState(childrenArr);
    const ref = useRef(null);
    useGridClasses(children, childrenArr);
    useStretch(stretch, childrenArr, ref, setChildrenArrState);
    return _jsx("div", { ref: ref, className: classNames(["zen-grid", className]), children: childrenArrState });
};
