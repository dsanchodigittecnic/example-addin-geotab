import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import { ButtonIconPosition, TextIconButton } from "../../../../../textIconButton/textIconButton";
import { SvgIconSize } from "../../../../../svgIcon/svgIconSize";
import { useMobile } from "../../../../../commonHelpers/hooks/useMobile";
export const BulkActionButton = forwardRef(function inner({ title, icon, clickHandler, onClick, disabled, type }, ref) {
    // eslint-disable-next-line react-hooks/rules-of-hooks
    const isMobile = useMobile();
    return _jsx(TextIconButton, { ref: ref, className: "zen-bulk-actions__action-button", icon: icon, iconPosition: ButtonIconPosition.Start, iconSize: SvgIconSize.Large, type: type || (isMobile ? "tertiary-black" : "secondary"), onClick: onClick || clickHandler, disabled: disabled, "aria-label": title, title: isMobile ? title : undefined, children: !isMobile ? _jsx("span", { className: "bulk-actions__action-button-title", children: title }) : null });
});
