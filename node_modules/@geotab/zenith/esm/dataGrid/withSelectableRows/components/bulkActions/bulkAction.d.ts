import { FC } from "react";
import { IIcon } from "../../../../icons/icon";
import { DeviceType } from "../../../../commonHelpers/hooks/deviceType";
import { IButton } from "../../../../button/button";
export interface IBulkActionCommon {
    id?: string;
    title: string;
    icon: FC<IIcon>;
    disabled?: boolean;
    /**
    * @deprecated
    * @param gridType - will be removed in the next major release
    */
    gridType?: DeviceType;
    type?: Pick<IButton, "type">["type"];
}
