import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef, useCallback, useMemo, useEffect, useState } from "react";
import { useFetch } from "../../commonHelpers/hooks/useFetch";
import { generateId } from "../../commonHelpers/generateId";
import { LoadingCell } from "./components/loadingRowCell";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { defaultColumnComponent } from "../extensions/defaultColumnComponent";
export const DEFAULT_LOADING_ROWS = 20;
const getDefaultEntitiesLoadingColumn = (columnId, originalColumnComponent) => {
    const renderFeed = (title, entity, isNested) => originalColumnComponent.renderFeed
        ? originalColumnComponent.renderFeed(title, entity, isNested)
        : [
            originalColumnComponent.renderHeader ? originalColumnComponent.renderHeader(title) : title,
            originalColumnComponent.render(entity, isNested)
        ];
    return {
        render: (entity, isNested) => entity.isLoading ? _jsx(LoadingCell, { children: entity[columnId] }) : originalColumnComponent.render(entity, isNested),
        renderHeader: (title) => originalColumnComponent.renderHeader ? originalColumnComponent.renderHeader(title) : title,
        renderFeed: (title, entity, isNested) => entity.isLoading
            ? [_jsx(LoadingCell, { children: title }, entity.id), _jsx(LoadingCell, { children: entity[columnId] }, entity.id)]
            : renderFeed(title, entity, isNested)
    };
};
const getEntitiesLoadingColumn = (column) => column.loadingEntitiesColumnComponent || getDefaultEntitiesLoadingColumn(column.id, column.columnComponent || defaultColumnComponent(column.id));
// T - type of entity
// P - props of wrapped component
// eslint-disable-next-line @typescript-eslint/naming-convention
export const withLoading = (DataGridComponent) => forwardRef(
// eslint-disable-next-line @typescript-eslint/naming-convention
function InnerWithLoadingList(props, ref) {
    const { entitiesPromise, loadingRows, columns, options } = props;
    const [data, setData] = useState([]);
    const state = useFetch({}, entitiesPromise);
    const sampleEntity = useMemo(() => columns.map((column) => ({ [column.id]: column.title })).reduce((acc, el) => (Object.assign(Object.assign({}, acc), el))), [columns]);
    const generateLoadingEntities = useCallback(() => (new Array(loadingRows || DEFAULT_LOADING_ROWS).fill(undefined)).map(() => (Object.assign(Object.assign({}, sampleEntity), { isLoading: true, id: generateId() }))), [loadingRows, sampleEntity]);
    useEffect(() => {
        if (state.data) {
            setData(prevData => {
                const newData = state.data || [];
                return (options === null || options === void 0 ? void 0 : options.addToEnd) ? [...prevData, ...newData] : newData;
            });
        }
    }, [state.data, options === null || options === void 0 ? void 0 : options.addToEnd]);
    const selectMode = useMemo(() => {
        if (state.isLoading) {
            return false;
        }
        return props.selectMode;
    }, [state.isLoading, props.selectMode]);
    return _jsx(DataGridComponent, Object.assign({}, props, { selectMode: selectMode, className: classNames([
            props.className || "",
            state.isLoading ? "zen-data-grid--loading" : ""
        ]), entities: (state.isLoading)
            ? (options === null || options === void 0 ? void 0 : options.addToEnd) ? [...data, ...generateLoadingEntities()] : generateLoadingEntities()
            : data, columns: columns.map(column => (Object.assign(Object.assign({}, column), { meta: Object.assign({ renderPlaceholder: (entity) => _jsx(LoadingCell, { children: entity[column.id] }), renderFeedPlaceholder: (entity) => [
                    _jsx(LoadingCell, { children: column.title }, `${column.id}_label`),
                    _jsx(LoadingCell, { children: entity[column.id] }, `${column.id}_value`)
                ] }, column.meta), columnComponent: state.isLoading ? getEntitiesLoadingColumn(column) : column.columnComponent || defaultColumnComponent(column.id) }))), ref: ref }), "grid");
});
