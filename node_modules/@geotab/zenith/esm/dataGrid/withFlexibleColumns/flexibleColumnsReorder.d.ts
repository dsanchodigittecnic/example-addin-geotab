import "./flexibleColumnsReorder.less";
import { SimpleEventHandler } from "../../commonHelpers/simpleEventHandler";
import { IFlexibleColumnResizer } from "./flexibleColumnsResizer";
export interface IFlexibleColumnReorder {
    add(): void;
    remove(): void;
    attach(event: "reorder", handler: (args: [number, number]) => void): this;
    detach(event: "reorder", handler?: (args: [number, number]) => void): this;
}
export declare class FlexibleColumnReorder extends SimpleEventHandler implements IFlexibleColumnReorder {
    private readonly list;
    private readonly resizer;
    private readonly isDraggable;
    private pageX;
    private pageY;
    private offsetLeft;
    private offsetTop;
    private pointerOffset;
    private dragElt;
    private targetCellElt;
    private closest;
    private columnWidths;
    private columnIndex;
    private draggables;
    private createColumnToDrag;
    private cloneCell;
    private cloneCellContent;
    private getFirstDraggable;
    private getLastDraggable;
    private calculateClosestColumn;
    private readonly preventScroll;
    private readonly stopMoving;
    private readonly moveColumn;
    private readonly endColumn;
    private readonly createDragElement;
    private readonly startColumn;
    constructor(list: HTMLElement, resizer: IFlexibleColumnResizer, isDraggable: (index: number) => boolean);
    add(): void;
    remove(): void;
}
