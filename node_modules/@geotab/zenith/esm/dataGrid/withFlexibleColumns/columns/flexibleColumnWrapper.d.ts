import "./flexibleColumn.less";
import { ReactNode } from "react";
import { IListColumnWrapper } from "../../listColumn";
import { IEntityWithId } from "../../../commonHelpers/entity";
interface IFlexibleColumnOptions {
    draggable?: boolean;
    resizable?: boolean;
}
export declare class FlexibleColumnWrapper<T extends IEntityWithId> implements IListColumnWrapper<T, T> {
    private readonly options;
    constructor(options: Partial<IFlexibleColumnOptions>);
    renderHeader(title: ReactNode): import("react/jsx-runtime").JSX.Element;
    render(value: ReactNode, _: T | undefined): ReactNode;
}
export {};
