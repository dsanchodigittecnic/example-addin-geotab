import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
export const defaultColumnComponent = (colId) => ({
    render: (entity) => entity[colId],
    renderFeed: (title, entity) => [_jsx(_Fragment, { children: title }), _jsx(_Fragment, { children: entity[colId] })],
    renderHeader: (title) => title
});
