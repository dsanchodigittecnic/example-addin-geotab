export const getRowClassName = (rowClassName, entity) => {
    if (!rowClassName) {
        return "";
    }
    if (typeof rowClassName === "function") {
        return rowClassName(entity);
    }
    return rowClassName;
};
