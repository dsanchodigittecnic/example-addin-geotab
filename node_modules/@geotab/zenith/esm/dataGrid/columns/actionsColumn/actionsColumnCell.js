var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { createElement, forwardRef } from "react";
import { ButtonIconPosition, TextIconButton } from "../../../textIconButton/textIconButton";
import { ButtonType } from "../../../button/buttonType";
import { SvgIconSize } from "../../../svgIcon/svgIconSize";
export const ActionColumnCell = forwardRef(function inner(_a, ref) {
    var { tag, hash, title, href, icon } = _a, rest = __rest(_a, ["tag", "hash", "title", "href", "icon"]);
    return tag === "button"
        ? _jsx(TextIconButton, Object.assign({ className: "zen-actions-column__action", "data-hash": hash, title: title, "aria-label": title, type: ButtonType.Light, icon: icon, iconSize: SvgIconSize.Bigger, iconPosition: ButtonIconPosition.Start, iconClasses: "zen-icon--no-events", ref: ref }, rest))
        : _jsx("a", Object.assign({ className: "zen-actions-column__action zen-button zen-button--light zen-caption", "data-hash": hash, title: title, "aria-label": title, href: href, ref: ref }, rest, { children: createElement(icon, { size: SvgIconSize.Bigger, className: "zen-icon--no-events" }) }));
});
