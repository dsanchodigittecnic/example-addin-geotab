import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { BasicColumn } from "../basicColumn";
import { generateId } from "../../../commonHelpers/generateId";
import { ActionsButton } from "./actionsButton";
import { ActionsMenu } from "../../../table/actions/actionsMenu";
export class ActionsColumn extends BasicColumn {
    renderIconActions(entity, actions, isNested) {
        const entityActions = typeof actions === "function"
            ? entity ? actions(entity, isNested || false).filter(action => action.condition === undefined || action.condition(entity)) : []
            : actions.filter(action => action.condition === undefined || action.condition(entity));
        const entityId = (entity === null || entity === void 0 ? void 0 : entity.id) || "";
        return entityActions.length > 0 ? _jsx("div", { className: "zen-actions-column", children: entityActions.map(action => _jsx(ActionsButton, { action: action, entity: entity }, action.id)) }, [entityId, ...entityActions.map(a => a.id)].join("-")) : null;
    }
    updateActions(options) {
        this.actions = options.actions ? options.actions.map(action => { var _a; return (Object.assign(Object.assign({}, action), { id: action.id || generateId(), actions: (_a = action.actions) === null || _a === void 0 ? void 0 : _a.map(a => (Object.assign(Object.assign({}, a), { id: generateId() }))) })); }) : undefined;
        this.renderFn = options.render;
        this.renderAsyncFn = options.renderAsync;
        this.headerActions = (options.headerActions || []).map(action => { var _a; return (Object.assign(Object.assign({}, action), { id: action.id || generateId(), actions: (_a = action.actions) === null || _a === void 0 ? void 0 : _a.map(a => (Object.assign(Object.assign({}, a), { id: generateId() }))) })); });
        if (typeof options.primaryActions === "function") {
            this.primaryActions = options.primaryActions;
        }
        else {
            this.primaryActions = (options.primaryActions || []).map(action => { var _a; return (Object.assign(Object.assign({}, action), { id: action.id || generateId(), actions: (_a = action.actions) === null || _a === void 0 ? void 0 : _a.map(a => (Object.assign(Object.assign({}, a), { id: generateId() }))) })); });
        }
    }
    renderCell(_, entity, isNested) {
        if (this.options.render || this.options.renderAsync) {
            return _jsxs("div", { className: "zen-actions-column", children: [this.primaryActions && (typeof this.primaryActions === "function" || this.primaryActions.length > 0) && this.renderIconActions(entity, this.primaryActions, isNested || false), this.options.render && _jsx(ActionsMenu, { entity: entity, isNested: isNested || false, render: this.options.render }), this.options.renderAsync && _jsx(ActionsMenu, { entity: entity, isNested: isNested || false, renderAsync: this.options.renderAsync })] });
        }
        return this.renderIconActions(entity, (this.actions || []));
    }
    renderHeaderCell(_) {
        return this.renderIconActions(undefined, this.headerActions);
    }
    constructor(actionsOptions) {
        super({ render: () => "" });
        Object.defineProperty(this, "actionsOptions", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: actionsOptions
        });
        Object.defineProperty(this, "actions", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "headerActions", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: []
        });
        Object.defineProperty(this, "primaryActions", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "renderFn", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "renderAsyncFn", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.updateActions(actionsOptions);
    }
    get options() {
        return this.actionsOptions;
    }
    set options(actionsOptions) {
        this.actionsOptions = actionsOptions;
        this.updateActions(actionsOptions);
    }
    get columnActions() {
        return (this.actions ? this.actions[0] : undefined) || this.renderFn;
    }
    get columnActionsAsync() {
        return this.renderAsyncFn || undefined;
    }
    get columnPrimaryActions() {
        return this.primaryActions || undefined;
    }
}
