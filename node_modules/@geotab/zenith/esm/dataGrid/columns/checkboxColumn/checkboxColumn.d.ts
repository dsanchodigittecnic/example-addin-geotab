import { ReactNode } from "react";
import { IListColumnBasic, TFeedResult } from "../../listColumn";
import { ICheckboxColumnOptions, SelectionState } from "./checkboxColumnWrapper";
import { ISimpleEventHandler, SimpleEventHandler } from "../../../commonHelpers/simpleEventHandler";
import { IRowEntity } from "../../row/row";
import { IEntityWithId } from "../../../commonHelpers/entity";
interface ISelectEvents {
    "change": [id: string, selected: boolean];
}
export interface ICheckboxColumn<T extends IRowEntity<N>, N extends IEntityWithId = T> extends ISimpleEventHandler<ISelectEvents>, IListColumnBasic<T, N> {
    selectionState: SelectionState;
}
export declare class CheckboxColumn<T extends IRowEntity<N>, N extends IEntityWithId = T> extends SimpleEventHandler implements ICheckboxColumn<T, N> {
    private readonly wrapper;
    constructor(selections: Set<string>, options: Partial<ICheckboxColumnOptions<T | N>>);
    render(entity: T | N): ReactNode;
    renderHeader(title: ReactNode): ReactNode;
    renderFeed(title: string, entity: T | N): TFeedResult;
    get selectionState(): SelectionState;
    set selectionState(state: SelectionState);
}
export {};
