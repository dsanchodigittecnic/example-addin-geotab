import { jsx as _jsx } from "react/jsx-runtime";
import { CheckboxHeaderCell } from "./checkboxHeaderCell";
import { CheckboxColumnWrapper } from "./checkboxColumnWrapper";
export class LimitedCheckboxColumnWrapper extends CheckboxColumnWrapper {
    constructor(selections, options, menuTriggerRef) {
        super(selections, Object.assign(Object.assign({}, options), { disabled: options.disabled ? options.disabled : (entity) => !this.selected.has(entity.id) && this.selected.size >= this.opts.limit }), menuTriggerRef);
        Object.defineProperty(this, "selected", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "opts", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.selected = selections;
        this.opts = options;
    }
    renderHeader(title) {
        return _jsx(CheckboxHeaderCell.Content, { children: title });
    }
}
