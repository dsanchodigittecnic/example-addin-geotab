import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { ButtonIconPosition, TextIconButton } from "../../../textIconButton/textIconButton";
import { ActionsButton } from "../actionsColumn/actionsButton";
import { generateId } from "../../../commonHelpers/generateId";
export class ActionLinkColumnWrapper {
    constructor(options) {
        Object.defineProperty(this, "options", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: options
        });
    }
    render(elt, entity) {
        if (!entity) {
            return null;
        }
        const entityActions = this.options.condition === undefined || this.options.condition(entity);
        const isDisabled = !entityActions;
        const link = isDisabled ? undefined : this.options.createLink(entity);
        const title = this.options.createTitle(entity);
        const icon = this.options.createIcon(entity);
        const getActionWithId = (action) => {
            var _a;
            return (Object.assign(Object.assign({ id: generateId() }, action), { actions: (_a = action.actions) === null || _a === void 0 ? void 0 : _a.map(a => (Object.assign({ id: generateId() }, a))) }));
        };
        const entityId = entity["id"] || generateId();
        const lintUrl = typeof link === "string" ? link : link === null || link === void 0 ? void 0 : link.href;
        return _jsxs("div", { className: "zen-action-link-column", children: [_jsx(TextIconButton, { disabled: isDisabled, link: lintUrl || "", title: title, "aria-label": title, className: "zen-action-link-column__link", icon: icon, iconSize: "large", type: "primary", iconPosition: ButtonIconPosition.Start }), _jsx("div", { className: "zen-action-link-column__content", children: elt }), this.options.actions
                    ? _jsx("div", { className: "zen-action-link-column__actions", children: this.options.actions
                            .map(getActionWithId)
                            .map(action => _jsx(ActionsButton, { action: action, entity: entity, isMobile: !!this.options.isMobile }, `${entityId}_${action.id}`)) })
                    : null] });
    }
    renderHeader(title) {
        return _jsx("div", { className: "zen-action-link-column", children: _jsx("div", { className: "zen-action-link-column__content", children: title }) });
    }
}
