import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { ActionsButton } from "../actionsColumn/actionsButton";
import { generateId } from "../../../commonHelpers/generateId";
export class ActionButtonsWrapper {
    constructor(options) {
        Object.defineProperty(this, "options", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: options
        });
    }
    render(elt, entity) {
        if (!entity) {
            return null;
        }
        const getActionWithId = (action) => {
            var _a;
            return (Object.assign(Object.assign({ id: generateId() }, action), { actions: (_a = action.actions) === null || _a === void 0 ? void 0 : _a.map(a => (Object.assign({ id: generateId() }, a))) }));
        };
        const entityId = entity["id"] || generateId();
        return _jsxs("div", { className: "zen-action-link-column", children: [_jsx("div", { className: "zen-action-link-column__content", children: elt }), this.options.actions
                    ? _jsx("div", { className: "zen-action-link-column__actions", children: this.options.actions
                            .map(getActionWithId)
                            .map(action => _jsx(ActionsButton, { action: action, entity: entity, isMobile: true }, `${entityId}_${action.id}`)) })
                    : null] });
    }
    renderHeader(title) {
        return _jsx("div", { className: "zen-action-link-column", children: _jsx("div", { className: "zen-action-link-column__content", children: title }) });
    }
}
