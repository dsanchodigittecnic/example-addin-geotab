import { ReactNode } from "react";
import { IListColumnBasic } from "../../listColumn";
import { BasicColumn, IColumnOptions } from "../basicColumn";
import { IActionLinkColumnOptions } from "./actionLinkColumnWrapper";
import { IRowEntity } from "../../row/row";
import { IEntityWithId } from "../../../commonHelpers/entity";
export declare class ActionLinkColumn<T extends IRowEntity<N>, N extends IEntityWithId = T> extends BasicColumn<T, ReactNode, N> implements IListColumnBasic<T, N> {
    private readonly wrapper;
    protected renderCell(elt: ReactNode, entity: T): ReactNode;
    protected renderHeaderCell(title: ReactNode): ReactNode;
    constructor(column: IColumnOptions<T, ReactNode>, options: IActionLinkColumnOptions<T, N>);
}
