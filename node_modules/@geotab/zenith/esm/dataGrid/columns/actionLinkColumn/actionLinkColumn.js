import { BasicColumn } from "../basicColumn";
import { ActionLinkColumnWrapper } from "./actionLinkColumnWrapper";
export class ActionLinkColumn extends BasicColumn {
    renderCell(elt, entity) {
        return this.wrapper.render(elt, entity);
    }
    renderHeaderCell(title) {
        return this.wrapper.renderHeader(title);
    }
    constructor(column, options) {
        super(column);
        Object.defineProperty(this, "wrapper", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.wrapper = new ActionLinkColumnWrapper(options);
    }
}
