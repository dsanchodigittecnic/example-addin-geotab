import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
export const TriggerContext = createContext({
    triggerComponent: null
});
export const TriggerContextProvider = ({ value, children }) => _jsx(TriggerContext.Provider, { value: Object.assign({}, value), children: children });
export const useMobileTrigger = () => {
    const { triggerComponent } = useContext(TriggerContext);
    return { mobileTriggerRef: triggerComponent };
};
