import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { classNames } from "../commonHelpers/classNames/classNames";
import { FiltersContainer } from "./filtersContainer/filtersContainer";
import { FiltersBarActions } from "./filtersBarActions/filtersBarActions";
import { Children, useCallback, useEffect, useMemo, useReducer, useRef } from "react";
import { FiltersBarSidePanel, FiltersBarSidePanelContainer, TRANSLATIONS as SidePanelTranslations } from "./filtersBarSidePanel/filtersBarSidePanel";
import { FiltersBarSearch } from "./components/filtersBarSearch/filtersBarSearch";
import { FiltersBarGroupButton } from "./components/filtersBarGroupButton/filtersBarGroupButton";
import { FiltersBarGroupsFilter } from "./components/filtersBarGroupsFilter/filtersBarGroupsFilter";
import { FiltersBarPeriodPicker, TRANSLATIONS as PeriodPickerTranslations } from "./components/filtersBarPeriodPicker/filtersBarPeriodPicker";
import { FiltersBarDropdown, TRANSLATIONS as DropdownTranslations } from "./components/filtersBarDropdown/filtersBarDropdown";
import { FiltersBarDropdownWithCheckbox } from "./components/filtersBarDropdownWithCheckbox/filtersBarDropdownWithCheckbox";
import { FiltersBarRange, TRANSLATIONS as RangeTranslations } from "./components/filtersBarRange/filtersBarRange";
import { isCustomElementValid } from "./utils/isCustomElementValid";
import { sidePanelReducer, StateActionType } from "./filtersBarSidePanel/sidePanelReducer";
import { filtersBarReducer, FiltersBarStateActionType, getInitialFiltersBarState } from "./filtersBarReducer";
import { useMobile } from "../commonHelpers/hooks/useMobile";
import { getNumberOfChangedFilters } from "./utils/getNumberOfChangedFilters";
import { useFiltersBarComponent } from "./filtersBarProvider";
import { useSearchComponent } from "./filtersBarProviderSearch";
import { useMobileTrigger } from "./filtersBarProviderTrigger";
import { FOCUSABLE_SELECTOR } from "../utils/focusableSelector";
/* eslint-enable @typescript-eslint/naming-convention */
export const FiltersBarDisplayName = "FiltersBar";
const getChildren = (children) => (!children
    ? []
    : (Children.count(children) === 1 ? [children] : children)).filter(c => !!c);
const allowedComponents = [FiltersBarSearch, FiltersBarDropdown, FiltersBarGroupButton, FiltersBarGroupsFilter, FiltersBarPeriodPicker, FiltersBarRange];
const getAllowedChildren = (items) => items.reduce((acc, item) => {
    if (!item) {
        return acc;
    }
    if (allowedComponents.includes(item.type) || isCustomElementValid(item)) {
        acc.push(item);
    }
    return acc;
}, []);
const getCurrentFiltersState = (children) => {
    const state = {};
    Children.forEach(children, (child) => {
        state[child.props.id] = {};
        state[child.props.id].state = child.props.state;
    });
    return state;
};
const mergeStates = (initialState, newState) => {
    const state = Object.assign({}, initialState);
    Object.keys(newState).forEach(key => {
        if (state[key]) {
            state[key] = newState[key] ? newState[key] : undefined;
        }
    });
    return state;
};
export const FiltersBar = ({ className, children, isAllFiltersVisible, hideAllFiltersButton, parentElement, selectedQuantity, 
// eslint-disable-next-line @typescript-eslint/no-unused-vars
ignoreNewFunctionality, pillName, pillIcon, pillType, toggleAllFilters, getDefaultFiltersState, onClearAllFilters, onApplyAllFilters }) => {
    // eslint-disable-next-line @typescript-eslint/no-unused-expressions
    ignoreNewFunctionality; // TODO: remove after deprecated ignoreNewFunctionality removed
    const allFiltersRef = useRef(null);
    const emptyFiltersRef = useRef(null);
    const isMobile = useMobile();
    const { setSearchComponent } = useSearchComponent();
    const { mobileTriggerRef } = useMobileTrigger();
    const { setFiltersBarToggleAllFiltersFn, onUpdateNumberOfChangedFiltersFn } = useFiltersBarComponent();
    const isAllFiltersButtonHidden = !isMobile && hideAllFiltersButton;
    const prevIsShouldSync = useRef(isMobile && isAllFiltersVisible);
    const prevIsOpen = useRef(isAllFiltersVisible);
    const toolbarRef = useRef(null);
    useEffect(() => {
        var _a, _b;
        if (!isAllFiltersVisible) {
            if (!isMobile && ((_a = allFiltersRef.current) === null || _a === void 0 ? void 0 : _a.getAttribute("aria-expanded")) === "true") {
                allFiltersRef.current.setAttribute("aria-expanded", "false");
            }
            if (isMobile && ((_b = mobileTriggerRef === null || mobileTriggerRef === void 0 ? void 0 : mobileTriggerRef.current) === null || _b === void 0 ? void 0 : _b.getAttribute("aria-expanded")) === "true") {
                mobileTriggerRef.current.setAttribute("aria-expanded", "false");
            }
        }
    }, [isAllFiltersVisible, isMobile, mobileTriggerRef]);
    const childrenArray = useMemo(() => getChildren(children), [children]);
    const filterBarChildren = useMemo(() => getAllowedChildren(childrenArray), [childrenArray]);
    const stateFromChildren = useMemo(() => getCurrentFiltersState(filterBarChildren), [filterBarChildren]);
    const [filtersBarState, dispatchFiltersBarState] = useReducer(filtersBarReducer, getInitialFiltersBarState(stateFromChildren, getDefaultFiltersState, onApplyAllFilters === undefined));
    const [sidePanelState, dispatchSidePanelState] = useReducer(sidePanelReducer, stateFromChildren);
    useEffect(() => {
        // need for dynamic items loading
        if (Object.keys(stateFromChildren).length > Object.keys(sidePanelState).length) {
            dispatchSidePanelState({ type: StateActionType.Reinitialize, payload: stateFromChildren });
        }
    }, [stateFromChildren, sidePanelState]);
    useEffect(() => {
        dispatchFiltersBarState({ type: FiltersBarStateActionType.ChangeChildrenState, payload: { state: stateFromChildren, defaultStateFn: getDefaultFiltersState } });
    }, [stateFromChildren, getDefaultFiltersState]);
    useEffect(() => {
        if (onUpdateNumberOfChangedFiltersFn) {
            onUpdateNumberOfChangedFiltersFn(filtersBarState.numberOfChangedFilters);
        }
    }, [filtersBarState.numberOfChangedFilters, onUpdateNumberOfChangedFiltersFn]);
    const memoizedShouldUpdate = useMemo(() => {
        if (isMobile && isAllFiltersVisible) {
            prevIsShouldSync.current = true;
            return true;
        }
        prevIsShouldSync.current = false;
        return false;
    }, [isMobile, isAllFiltersVisible]);
    useEffect(() => {
        if (prevIsShouldSync.current && memoizedShouldUpdate) {
            prevIsShouldSync.current = false;
            if (prevIsOpen.current) {
                prevIsOpen.current = false;
                dispatchSidePanelState({ type: StateActionType.Reinitialize, payload: mergeStates(stateFromChildren, sidePanelState) });
                return;
            }
            Children.forEach(filterBarChildren, (child) => {
                dispatchSidePanelState({ type: StateActionType.Change, payload: { id: child.props.id, value: { state: child.props.state } } });
            });
        }
    }, [filterBarChildren, memoizedShouldUpdate, sidePanelState, stateFromChildren]);
    const searchChild = childrenArray.find(child => child && child.type === FiltersBarSearch);
    setSearchComponent(searchChild);
    useEffect(() => {
        setFiltersBarToggleAllFiltersFn(toggleAllFilters);
    }, [toggleAllFilters, setFiltersBarToggleAllFiltersFn]);
    const toggleAllFiltersCallback = useCallback((value) => {
        if (!isAllFiltersVisible && value) {
            Children.forEach(filterBarChildren, (child) => {
                dispatchSidePanelState({ type: StateActionType.Change, payload: { id: child.props.id, value: { state: child.props.state } } });
            });
        }
        toggleAllFilters(value);
        prevIsOpen.current = value;
    }, [isAllFiltersVisible, toggleAllFilters, filterBarChildren]);
    const onClosePanel = useCallback(() => toggleAllFiltersCallback(false), [toggleAllFiltersCallback]);
    const handleApplyAllFilters = useCallback((isSidePanel) => (value) => {
        var _a;
        if (onApplyAllFilters) {
            const newValue = value ? value : sidePanelState;
            const newState = isSidePanel ? Object.keys(newValue).reduce((res, key) => {
                var _a;
                res[key] = { state: (_a = newValue[key]) === null || _a === void 0 ? void 0 : _a.state };
                return res;
            }, {}) : filtersBarState.childrenState;
            isSidePanel && dispatchFiltersBarState({ type: FiltersBarStateActionType.ChangeChildrenState, payload: { state: newState, defaultStateFn: getDefaultFiltersState } });
            dispatchFiltersBarState({ type: FiltersBarStateActionType.UpdateNumber, payload: newState });
            onApplyAllFilters(newState);
            (_a = allFiltersRef.current) === null || _a === void 0 ? void 0 : _a.focus();
        }
    }, [onApplyAllFilters, sidePanelState, filtersBarState.childrenState, getDefaultFiltersState]);
    const handleClearClick = useCallback(() => {
        const defaultState = getDefaultFiltersState();
        dispatchFiltersBarState({ type: FiltersBarStateActionType.ChangeChildrenState, payload: { state: defaultState, defaultStateFn: getDefaultFiltersState } });
        dispatchFiltersBarState({ type: FiltersBarStateActionType.UpdateNumber, payload: defaultState });
        filterBarChildren.forEach(child => {
            const changeHandler = child.props.onChange;
            const newState = defaultState[child.props.id];
            const isAllowUnsetValue = child.type === FiltersBarPeriodPicker;
            // @ts-expect-error: Let's ignore a compile error. if value undefined, and a value is passed to the function undefined, it can be passed because the interface is IDateRangeUnsetValue
            ((newState === null || newState === void 0 ? void 0 : newState.state) || isAllowUnsetValue) && changeHandler(newState.state);
        });
        onClearAllFilters && onClearAllFilters();
        onApplyAllFilters && onApplyAllFilters(defaultState);
        if (allFiltersRef.current) {
            allFiltersRef.current.focus();
        }
        else if (toolbarRef.current) {
            const focusable = toolbarRef.current.querySelector(FOCUSABLE_SELECTOR);
            focusable === null || focusable === void 0 ? void 0 : focusable.focus();
        }
    }, [filterBarChildren, getDefaultFiltersState, onApplyAllFilters, onClearAllFilters]);
    const isApplyButtonShown = useMemo(() => {
        if (!onApplyAllFilters) {
            return false;
        }
        const defaultState = getDefaultFiltersState();
        if (Object.keys(defaultState).length === Object.keys(filtersBarState.prevChildrenState).length) {
            return getNumberOfChangedFilters(filtersBarState.prevChildrenState, filtersBarState.childrenState) > 0;
        }
        return getNumberOfChangedFilters(filtersBarState.childrenState, filtersBarState.prevChildrenState) > 0;
    }, [filtersBarState.childrenState, filtersBarState.prevChildrenState, getDefaultFiltersState, onApplyAllFilters]);
    const sidePanel = useMemo(() => {
        const sidePanelChild = childrenArray.find(child => child && child.type === FiltersBarSidePanelContainer);
        if (!sidePanelChild) {
            return _jsx(FiltersBarSidePanel, { parentElement: parentElement, isOpen: isAllFiltersVisible, triggerRef: isMobile ? mobileTriggerRef || emptyFiltersRef : allFiltersRef, getDefaultFiltersState: getDefaultFiltersState, onClosePanel: onClosePanel, state: sidePanelState, onApplyAllFilters: handleApplyAllFilters(true), dispatch: dispatchSidePanelState, pillName: pillName, pillIcon: pillIcon, pillType: pillType, children: filterBarChildren });
        }
        const sidePanelChildren = getAllowedChildren(getChildren(sidePanelChild.props.children));
        return _jsx(FiltersBarSidePanel, { parentElement: parentElement, isOpen: isAllFiltersVisible, triggerRef: isMobile ? mobileTriggerRef || emptyFiltersRef : allFiltersRef, getDefaultFiltersState: getDefaultFiltersState, onClosePanel: onClosePanel, className: sidePanelChild.props.className, state: sidePanelChild.props.state, onApplyAllFilters: handleApplyAllFilters(true), dispatch: sidePanelChild.props.dispatch, pillName: pillName, pillIcon: pillIcon, pillType: pillType, children: sidePanelChildren });
    }, [childrenArray, parentElement, isAllFiltersVisible, isMobile, mobileTriggerRef, getDefaultFiltersState, onClosePanel, handleApplyAllFilters,
        pillName, pillIcon, pillType, sidePanelState, filterBarChildren]);
    const noActions = useMemo(() => isAllFiltersButtonHidden && (selectedQuantity === undefined && filtersBarState.currentNumberOfChangedFilters === 0 || selectedQuantity === 0)
        && (!isApplyButtonShown || onApplyAllFilters === undefined), [isAllFiltersButtonHidden, selectedQuantity, filtersBarState.currentNumberOfChangedFilters, isApplyButtonShown, onApplyAllFilters]);
    return _jsxs(_Fragment, { children: [!isMobile && _jsxs("div", { ref: toolbarRef, className: classNames(["zen-filters-toolbar", noActions ? "zen-filters-toolbar--no-actions" : "", className !== null && className !== void 0 ? className : ""]), children: [_jsx(FiltersContainer, { children: filterBarChildren }), _jsx(FiltersBarActions, { allFiltersRef: allFiltersRef, onShowAllFilters: () => toggleAllFiltersCallback(true), onClearFilters: handleClearClick, selectedQuantity: selectedQuantity !== undefined ? selectedQuantity : filtersBarState.currentNumberOfChangedFilters, selectedQuantityToDisplay: selectedQuantity !== undefined ? selectedQuantity : filtersBarState.numberOfChangedFilters, hideAllFiltersButton: isAllFiltersButtonHidden, onApplyButtonClick: onApplyAllFilters ? handleApplyAllFilters(false) : undefined, isApplyButtonShown: isApplyButtonShown })] }), !isAllFiltersButtonHidden ? sidePanel : null] });
};
FiltersBar.Search = FiltersBarSearch;
FiltersBar.Dropdown = FiltersBarDropdown;
FiltersBar.DropdownWithCheckbox = FiltersBarDropdownWithCheckbox;
FiltersBar.GroupButton = FiltersBarGroupButton;
FiltersBar.GroupsFilter = FiltersBarGroupsFilter;
FiltersBar.PeriodPicker = FiltersBarPeriodPicker;
FiltersBar.Range = FiltersBarRange;
FiltersBar.SidePanel = FiltersBarSidePanelContainer;
FiltersBar.displayName = FiltersBarDisplayName;
export const TRANSLATIONS = [...new Set([
        ...SidePanelTranslations,
        ...DropdownTranslations,
        ...PeriodPickerTranslations,
        ...RangeTranslations
    ]).values()];
