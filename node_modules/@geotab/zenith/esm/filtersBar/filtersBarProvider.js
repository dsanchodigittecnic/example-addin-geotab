import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useCallback, useContext, useRef } from "react";
export const FiltersBarContext = createContext({
    setFiltersBarToggleAllFiltersFn: () => { },
    onUpdateNumberOfChangedFiltersFn: () => { }
});
export const FiltersBarContextProvider = ({ value, children }) => {
    const filtersBarToggleAllFiltersRef = useRef(null);
    const setFiltersBarToggleFn = useCallback((fn) => {
        var _a;
        if (((_a = filtersBarToggleAllFiltersRef.current) === null || _a === void 0 ? void 0 : _a.fn) !== fn) {
            filtersBarToggleAllFiltersRef.current = { fn: fn };
            setTimeout(() => {
                value.setFiltersBarToggleAllFiltersFn({ fn: fn });
            }, 0);
        }
    }, [value]);
    return _jsx(FiltersBarContext.Provider, { value: { setFiltersBarToggleAllFiltersFn: setFiltersBarToggleFn, onUpdateNumberOfChangedFiltersFn: value.onUpdateNumberOfChangedFiltersFn }, children: children });
};
export const useFiltersBarComponent = () => {
    const { setFiltersBarToggleAllFiltersFn, onUpdateNumberOfChangedFiltersFn } = useContext(FiltersBarContext);
    return { setFiltersBarToggleAllFiltersFn, onUpdateNumberOfChangedFiltersFn };
};
