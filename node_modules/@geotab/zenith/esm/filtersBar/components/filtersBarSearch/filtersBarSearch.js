import { jsx as _jsx } from "react/jsx-runtime";
import { SearchInputRaw } from "../../../searchInputRaw/searchInputRaw";
import { SidePanelCell } from "../../../sidePanel/sidePanelCell/sidePanelCell";
import { useUniqueId } from "../../../commonHelpers/useUniqueId";
export const FiltersBarSearchDisplayName = "FiltersBarSearch";
export const FiltersBarSearch = ({ className, state, props, sidePanelTitle, useSidePanelView, onChange: onChangeState }) => {
    const inputId = useUniqueId();
    const filterBarLabelledId = useUniqueId();
    const component = _jsx(SearchInputRaw, Object.assign({}, props, { id: inputId, onChange: value => onChangeState({ value }), value: state.value, className: className }));
    if (!useSidePanelView) {
        return component;
    }
    return _jsx(SidePanelCell, { title: sidePanelTitle, role: "group", labelledBy: filterBarLabelledId, titleId: filterBarLabelledId, children: component });
};
FiltersBarSearch.displayName = FiltersBarSearchDisplayName;
