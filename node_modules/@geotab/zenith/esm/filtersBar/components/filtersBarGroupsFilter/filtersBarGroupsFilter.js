import { jsx as _jsx } from "react/jsx-runtime";
import { GroupsFilter } from "../../../groupsFilter/groupsFilter";
import { SidePanelCell } from "../../../sidePanel/sidePanelCell/sidePanelCell";
import { useUniqueId } from "../../../commonHelpers/useUniqueId";
export const FiltersBarGroupsFilter = ({ props, state, sidePanelTitle, useSidePanelView, onChange: onChangeState }) => {
    const component = _jsx(GroupsFilter, Object.assign({}, props, { fullSize: useSidePanelView, initialFilterState: state.value, onChange: (value) => onChangeState({ value }) }));
    const groupFilterLabelledId = useUniqueId();
    if (!useSidePanelView) {
        return component;
    }
    return _jsx(SidePanelCell, { title: sidePanelTitle, role: "group", labelledBy: groupFilterLabelledId, titleId: groupFilterLabelledId, className: "zen-filters-bar-groups-filter--side-panel", children: component });
};
