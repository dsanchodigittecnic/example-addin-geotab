import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import { memo, useCallback, useContext, useEffect, useMemo, useState } from "react";
import { SidePanelCell } from "../../../sidePanel/sidePanelCell/sidePanelCell";
import { classNames } from "../../../commonHelpers/classNames/classNames";
import { CUSTOM_CALENDAR_ID, DateRangeInner } from "../../../dateRangeInner/dateRangeInner";
import { useUniqueId } from "../../../commonHelpers/useUniqueId";
import { useLanguage } from "../../../utils/localization/useLanguage";
import { getOptionsPeriodMap, getRangeOptionsArray, getSelectedRangeFromValue } from "./getRangeOption";
import { DateRange } from "../../../dateRange/dateRange";
import { userFormatContext } from "../../../utils/userFormat/userFormatContext";
import { normalizeDates } from "./normalizeDates";
const DateRangeInnerMemo = memo(DateRangeInner);
export const FiltersBarPeriodPicker = ({ props, hasApplyButton, className, useSidePanelView, sidePanelTitle, state, defaultState, onChange, checkError, sidePanelMaxGroupedItemsLength = 5 }) => {
    const { options: optionsArg, withCalendar, timeSelect, disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter, lockStartDate, lockEndDate, classNamePopup } = props;
    const { translate } = useLanguage();
    const { toLocalDateTime, toReverseOffsetDateTime, weekStartsOnSunday } = useContext(userFormatContext);
    const id = useUniqueId();
    const optionsMap = useMemo(() => getOptionsPeriodMap(optionsArg, translate, toLocalDateTime, weekStartsOnSunday ? "Sunday" : "Monday", toReverseOffsetDateTime), [optionsArg, toLocalDateTime, toReverseOffsetDateTime, translate, weekStartsOnSunday]);
    const [selectedRange, setSelectedRange] = useState(getSelectedRangeFromValue(optionsMap, state));
    const filterBarLabelledId = useUniqueId();
    const [prevSelection, setPrevSelection] = useState(getSelectedRangeFromValue(optionsMap, state));
    const getCustomRange = useCallback(() => ({ from: (prevSelection === null || prevSelection === void 0 ? void 0 : prevSelection.from) || new Date(), to: (prevSelection === null || prevSelection === void 0 ? void 0 : prevSelection.to) || new Date() }), [prevSelection]);
    const options = useMemo(() => getRangeOptionsArray(optionsArg, translate, toLocalDateTime, weekStartsOnSunday ? "Sunday" : "Monday", toReverseOffsetDateTime, withCalendar || false, prevSelection && prevSelection.label === CUSTOM_CALENDAR_ID, prevSelection ? getCustomRange : undefined), [optionsArg, translate, toLocalDateTime, weekStartsOnSunday, toReverseOffsetDateTime, withCalendar, prevSelection, getCustomRange]);
    const hasCustomOption = useMemo(() => options.find(option => option.id === CUSTOM_CALENDAR_ID) !== undefined, [options]);
    const [customDate, setCustomDate] = useState(normalizeDates(state === null || state === void 0 ? void 0 : state.from.toISOString(), state === null || state === void 0 ? void 0 : state.to.toISOString(), disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter));
    const handleDateRangeSelection = useCallback((val) => {
        var _a, _b;
        const selection = options.find(option => option.id === val);
        if (selection && val !== CUSTOM_CALENDAR_ID && selection.getRange) {
            const range = selection.getRange();
            const normalizedDates = normalizeDates(range.from.toISOString(), range.to.toISOString(), disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter);
            const newRange = val === CUSTOM_CALENDAR_ID ?
                { from: new Date(normalizedDates.from), to: new Date(normalizedDates.to) }
                : range;
            setCustomDate({ from: newRange.from.toISOString(), to: newRange.to.toISOString() });
            // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
            checkError && checkError(!range.from || !range.to);
            onChange(Object.assign({ label: selection.id }, newRange));
        }
        else {
            const startDateForNormalize = ((_a = selection === null || selection === void 0 ? void 0 : selection.getRange) === null || _a === void 0 ? void 0 : _a.call(selection)) && (selectedRange === null || selectedRange === void 0 ? void 0 : selectedRange.label) !== val ? selection.getRange().from.toISOString() : state === null || state === void 0 ? void 0 : state.from.toISOString();
            const endDateForNormalize = ((_b = selection === null || selection === void 0 ? void 0 : selection.getRange) === null || _b === void 0 ? void 0 : _b.call(selection)) && (selectedRange === null || selectedRange === void 0 ? void 0 : selectedRange.label) !== val ? selection.getRange().to.toISOString() : state === null || state === void 0 ? void 0 : state.to.toISOString();
            const normalizedDates = normalizeDates(startDateForNormalize, endDateForNormalize, disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter);
            const newRange = val === CUSTOM_CALENDAR_ID ?
                { from: new Date(normalizedDates.from), to: new Date(normalizedDates.to) }
                : undefined;
            const newValueFrom = (newRange === null || newRange === void 0 ? void 0 : newRange.from) || new Date();
            const newValueTo = (newRange === null || newRange === void 0 ? void 0 : newRange.to) || new Date();
            setSelectedRange({ label: val, from: newValueFrom, to: newValueTo });
            setCustomDate({ from: newValueFrom.toISOString(), to: newValueTo.toISOString() });
            onChange({ label: val, from: newValueFrom, to: newValueTo });
        }
    }, [options, disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter, checkError, onChange, selectedRange === null || selectedRange === void 0 ? void 0 : selectedRange.label, state === null || state === void 0 ? void 0 : state.from, state === null || state === void 0 ? void 0 : state.to]);
    useEffect(() => {
        const currentState = getSelectedRangeFromValue(optionsMap, state);
        if ((prevSelection === null || prevSelection === void 0 ? void 0 : prevSelection.label) !== (currentState === null || currentState === void 0 ? void 0 : currentState.label)) {
            setSelectedRange(currentState);
            setPrevSelection(currentState);
        }
        if (useSidePanelView && (defaultState === null || defaultState === void 0 ? void 0 : defaultState.label) === CUSTOM_CALENDAR_ID && (currentState === null || currentState === void 0 ? void 0 : currentState.label) === CUSTOM_CALENDAR_ID &&
            (prevSelection === null || prevSelection === void 0 ? void 0 : prevSelection.label) === CUSTOM_CALENDAR_ID &&
            defaultState.from.toISOString() === currentState.from.toISOString() && defaultState.to.toISOString() === currentState.to.toISOString()
            && customDate.from && customDate.to && (customDate.from !== defaultState.from.toISOString() || customDate.to !== defaultState.to.toISOString())) {
            const normalizedDates = normalizeDates(defaultState.from.toISOString(), defaultState.to.toISOString(), disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter);
            const newRange = { from: new Date(normalizedDates.from), to: new Date(normalizedDates.to) };
            const updatedState = { label: CUSTOM_CALENDAR_ID, from: newRange.from, to: newRange.to };
            setSelectedRange(updatedState);
            setPrevSelection(updatedState);
            setCustomDate({ from: newRange.from.toISOString(), to: newRange.to.toISOString() });
        }
    }, [prevSelection === null || prevSelection === void 0 ? void 0 : prevSelection.label, state, defaultState, useSidePanelView, customDate.from, customDate.to,
        disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter, optionsMap]);
    const handleCustomDate = useCallback((value) => {
        const isHasError = !value.from || !value.to;
        checkError && checkError(isHasError);
        setSelectedRange({ label: CUSTOM_CALENDAR_ID, from: lockStartDate || value.from, to: lockEndDate || (lockStartDate ? value.to || value.from : value.to) });
        setCustomDate({ from: lockStartDate || value.from, to: lockEndDate || (lockStartDate ? value.to || value.from : value.to) });
        !isHasError && onChange({
            label: CUSTOM_CALENDAR_ID,
            from: new Date((lockStartDate || value.from)),
            to: new Date((lockEndDate || (lockStartDate ? value.to || value.from : value.to)))
        });
    }, [checkError, lockEndDate, lockStartDate, onChange]);
    const range = useMemo(() => ({ from: customDate.from, to: customDate.to }), [customDate.from, customDate.to]);
    const component = useMemo(() => _jsx(_Fragment, { children: _jsx(DateRangeInnerMemo, { id: `${id}-daterange`, options: options, onOptionSelect: handleDateRangeSelection, onCalendarSelect: hasCustomOption ? handleCustomDate : undefined, selection: selectedRange === null || selectedRange === void 0 ? void 0 : selectedRange.label, range: range, timeSelect: timeSelect, disableFutureDates: disableFutureDates, disablePastDates: disablePastDates, disableDatesBefore: disableDatesBefore, disableDatesAfter: disableDatesAfter }) }), [id, options, handleDateRangeSelection, hasCustomOption, handleCustomDate, selectedRange === null || selectedRange === void 0 ? void 0 : selectedRange.label, range, timeSelect, disableFutureDates, disablePastDates, disableDatesBefore, disableDatesAfter]);
    const popup = useMemo(() => {
        const commonProps = Object.assign(Object.assign({}, props), { hasApplyButton: useSidePanelView ? false : props.hasApplyButton || hasApplyButton, id: id, className: classNames(["zen-filters-bar-period-picker", useSidePanelView ? "zen-filters-bar-period-picker--full-width" : "", className !== null && className !== void 0 ? className : ""]), classNamePopup: classNames(["zen-filters-bar-period-picker__popup", useSidePanelView ? "zen-filters-bar-period-picker__popup--full-width" : "", classNamePopup || ""]), classNameWrapper: classNames([useSidePanelView ? "zen-filters-bar-period-picker-wrapper" : ""]), options: optionsArg });
        return props.allowUnsetValue ? _jsx(DateRange, Object.assign({}, commonProps, { value: state, onChange: onChange, defaultValue: defaultState, allowUnsetValue: true })) : _jsx(DateRange, Object.assign({}, commonProps, { value: state, onChange: onChange, defaultValue: defaultState, allowUnsetValue: false }));
    }, [props, useSidePanelView, id, className, classNamePopup, optionsArg, state, onChange, defaultState, hasApplyButton]);
    if (!options.length && !hasCustomOption) {
        return "";
    }
    if (!useSidePanelView) {
        return popup;
    }
    const isUsePopup = options.length > sidePanelMaxGroupedItemsLength;
    return _jsx(SidePanelCell, { role: "group", labelledBy: filterBarLabelledId, titleId: filterBarLabelledId, title: sidePanelTitle, withoutSidePaddings: !isUsePopup, children: isUsePopup ? popup : component });
};
export const TRANSLATIONS = [
    "Clear",
    "Reset",
    "Apply",
    "Date range filter",
    "The start date cannot be empty.",
    "The end date cannot be empty."
];
