import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../../../commonHelpers/classNames/classNames";
import { useUniqueId } from "../../../commonHelpers/useUniqueId";
import { GroupButton } from "../../../groupButton/groupButton";
import { SidePanelCell } from "../../../sidePanel/sidePanelCell/sidePanelCell";
export const FiltersBarGroupButton = ({ className, state, props, onChange: onChangeState, useSidePanelView, sidePanelTitle }) => {
    const groupData = props.groupData;
    const groupButtonLabelledId = useUniqueId();
    const fullGroupData = groupData.map(item => (Object.assign(Object.assign({}, item), { selected: item.value === state.selectedValue })));
    const onChangeEvent = (event) => {
        const value = event.target.value;
        onChangeState({ selectedValue: value });
    };
    const component = _jsx(GroupButton, Object.assign({}, props, { fullWidth: useSidePanelView, className: classNames([className !== null && className !== void 0 ? className : ""]), groupData: fullGroupData, onChange: onChangeEvent }));
    if (!useSidePanelView) {
        return component;
    }
    return _jsx(SidePanelCell, { role: "group", title: sidePanelTitle, titleId: groupButtonLabelledId, labelledBy: groupButtonLabelledId, children: component });
};
