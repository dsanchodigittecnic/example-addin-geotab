var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useCallback, useMemo, useRef, useState } from "react";
import { SidePanelCell } from "../../../sidePanel/sidePanelCell/sidePanelCell";
import { Dropdown, TRANSLATIONS as DropdownTranslations } from "../../../dropdown/dropdown";
import { PillBox } from "../../../pillBox/pillBox";
import { classNames } from "../../../commonHelpers/classNames/classNames";
import { useMobile } from "../../../commonHelpers/hooks/useMobile";
import { useUniqueId } from "../../../commonHelpers/useUniqueId";
import { FiltersBarSidePanelDropdownWithCheckbox } from "../../filtersBarSidePanel/components/filtersBarSidePanelDropdownWithCheckbox/filtersBarSidePanelDropdownWithCheckbox";
import { FOCUSABLE_SELECTOR } from "../../../utils/focusableSelector";
// eslint-disable-next-line complexity
export const FiltersBarDropdownWithCheckbox = ({ sidePanelTitle, className, props, state, useSidePanelView, onChange }) => {
    const isFullSelectionMode = useMemo(() => "isAllSelected" in state && typeof (state).isAllSelected === "boolean", [state]);
    const selection = useMemo(() => new Set(state.selected), [state]);
    const isMobile = useMobile();
    const isCurrentAllSelected = useMemo(() => isFullSelectionMode ? state.isAllSelected : undefined, [isFullSelectionMode, state]);
    const isCurrentChecked = useMemo(() => state.isChecked, [state.isChecked]);
    const [_, setNamedFnReady] = useState(false);
    const namedItemsFnRef = useRef(items => items.map(el => ({ id: el, name: "..." })));
    const dropdownLabelledId = useUniqueId();
    const sidePanelCellRef = useRef(null);
    const handleFocusOnRemove = useCallback((newSelectedFirstItem) => {
        var _a, _b, _c;
        if (newSelectedFirstItem) {
            const pillBoxElement = (_a = sidePanelCellRef.current) === null || _a === void 0 ? void 0 : _a.querySelector(`.zen-filters-bar-dropdown-with-checkbox__pill-box .zen-pill-box__pill[data-id="${newSelectedFirstItem}"]`);
            const focusable = pillBoxElement ?
                [...Array.from(pillBoxElement.querySelectorAll(FOCUSABLE_SELECTOR))] : undefined;
            (_b = focusable === null || focusable === void 0 ? void 0 : focusable[0]) === null || _b === void 0 ? void 0 : _b.focus();
        }
        else {
            const focusable = sidePanelCellRef.current ? [...Array.from(sidePanelCellRef.current.querySelectorAll(FOCUSABLE_SELECTOR))] : undefined;
            (_c = focusable === null || focusable === void 0 ? void 0 : focusable[0]) === null || _c === void 0 ? void 0 : _c.focus();
        }
    }, []);
    const handleRemove = useCallback((removedId) => {
        const newSelection = Array.from(selection).filter(el => el !== removedId);
        if (isFullSelectionMode) {
            onChange({ selected: newSelection,
                isAllSelected: false, isChecked: state.isChecked });
        }
        else {
            onChange({ selected: newSelection,
                isChecked: state.isChecked });
        }
        handleFocusOnRemove(newSelection[0]);
    }, [handleFocusOnRemove, isFullSelectionMode, onChange, selection, state]);
    const componentWidth = useMemo(() => useSidePanelView ? undefined : props.width, [useSidePanelView, props.width]);
    const onChangeFullSelectionWithCheckbox = useCallback((newValue) => {
        onChange({ selected: newValue.selected.map(el => el.id), isAllSelected: newValue.isAllSelected || false, isChecked: newValue.isChecked });
    }, [onChange]);
    const onChangeWithCheckbox = useCallback((newValue) => {
        onChange({ selected: newValue.selected.map(el => el.id), isChecked: newValue.isChecked });
    }, [onChange]);
    const { getData } = props, restProps = __rest(props, ["getData"]);
    const memoizedGetData = useMemo(() => getData, [getData]);
    const commonProps = useMemo(() => (Object.assign(Object.assign({}, restProps), { width: componentWidth, inputId: props.inputId && useSidePanelView ? `${props.inputId}-side-panel` : props.inputId, className: classNames([className || "", useSidePanelView ? "zen-filters-bar-dropdown-with-checkbox--side-panel" : "",
            !useSidePanelView && !props.width ? "zen-filters-bar-dropdown-with-checkbox--toolbar" : ""]), classNamePopup: classNames(["zen-filters-bar-dropdown-with-checkbox-popup", props.classNamePopup || ""]), fullWidthTriggerButton: useSidePanelView ? true : props.fullWidthTriggerButton, error: undefined, hasApplyButton: useSidePanelView ? false : props.hasApplyButton, getNamedItems: useSidePanelView ? (fn) => {
            namedItemsFnRef.current = fn;
            setNamedFnReady(val => !val);
        } : undefined })), [className, componentWidth, props.classNamePopup, props.fullWidthTriggerButton, props.hasApplyButton, props.inputId, props.width, restProps, useSidePanelView]);
    const componentWithSeparateCheckbox = isFullSelectionMode ? (_jsx(FiltersBarSidePanelDropdownWithCheckbox, { props: Object.assign({}, commonProps), state: state, onChange: onChange, getData: memoizedGetData })) : _jsx(FiltersBarSidePanelDropdownWithCheckbox, { props: Object.assign({}, commonProps), state: state, onChange: onChange, getData: memoizedGetData });
    const component = isFullSelectionMode ? (_jsx(Dropdown, Object.assign({}, commonProps, { checkboxLabel: props.checkboxLabel, getData: props.getData, value: { selected: Array.from(selection), isAllSelected: isCurrentAllSelected || false, isChecked: isCurrentChecked }, onChange: onChangeFullSelectionWithCheckbox, defaultValue: props.defaultValue, multiselect: true, selectAllButton: true }))) : (_jsx(Dropdown, Object.assign({}, commonProps, { checkboxLabel: props.checkboxLabel, getData: props.getData, value: { selected: Array.from(selection), isChecked: isCurrentChecked }, onChange: onChangeWithCheckbox, defaultValue: props.defaultValue })));
    if (!useSidePanelView) {
        return component;
    }
    const namedPills = namedItemsFnRef.current(Array.from(selection)).map(el => (el.name ? el : Object.assign(Object.assign({}, el), { name: "..." })));
    return _jsx(SidePanelCell, { title: sidePanelTitle, role: "group", labelledBy: dropdownLabelledId, titleId: dropdownLabelledId, children: _jsxs("div", { ref: sidePanelCellRef, children: [isMobile ? component : componentWithSeparateCheckbox, props.multiselect || props.multiselect === undefined ? _jsx(PillBox, { className: "zen-filters-bar-dropdown-with-checkbox__pill-box", selectedPills: namedPills, onRemove: handleRemove }) : null] }) });
};
export const TRANSLATIONS = [...new Set([...DropdownTranslations])];
