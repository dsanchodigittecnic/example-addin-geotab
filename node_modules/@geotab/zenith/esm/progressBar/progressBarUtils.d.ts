import { ProgressBarType } from "./progressBar";
export declare const isValidProggressBar: (min: number, max: number, now: number) => boolean;
export declare const getProgressBarValue: (min: number, max: number, now: number) => string | null;
export declare const getProgressBarAttr: (type: ProgressBarType, min: number, max: number, now: string | null) => {
    className: string;
    role: string;
    "aria-valuemin": number;
    "aria-valuemax": number;
};
