import { useRef, useState } from "react";
export const useToastState = () => {
    const [toasts, setToasts] = useState([]);
    const targetEvent = useRef(new EventTarget());
    return {
        toasts,
        setToasts,
        targetEvent
    };
};
