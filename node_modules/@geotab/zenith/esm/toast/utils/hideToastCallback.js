export const hideToastCallback = ((event, hideToast) => {
    var _a;
    const id = (_a = event.detail) === null || _a === void 0 ? void 0 : _a.id;
    if (!id || typeof id !== "string") {
        return;
    }
    hideToast(id);
});
