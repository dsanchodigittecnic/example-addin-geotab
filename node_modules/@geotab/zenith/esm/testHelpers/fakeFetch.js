let loadTimeout = null;
export const fetchData = () => {
    loadTimeout && clearTimeout(loadTimeout);
    return new Promise((res, rej) => {
        loadTimeout = window.setTimeout(() => Math.random() > 0.5 ? res("Fetched Data") : rej(new Error("Error while fetching data")), 5000);
    });
};
