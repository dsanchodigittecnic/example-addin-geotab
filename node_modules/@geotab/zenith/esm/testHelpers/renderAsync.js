import { act } from "react";
import { render } from "@testing-library/react";
export const renderAsync = async (component) => act(async () => {
    const result = render(component);
    await Promise.resolve();
    return result;
});
