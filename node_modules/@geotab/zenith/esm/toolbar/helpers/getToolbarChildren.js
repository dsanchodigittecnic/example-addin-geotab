import React from "react";
export const getToolbarChildren = (children) => {
    if (!children) {
        return undefined;
    }
    return (React.Children.count(children) === 1 ? [children] : children);
};
