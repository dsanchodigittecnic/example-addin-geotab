export const getCustomizableElement = (elt) => {
    if (!elt) {
        return {};
    }
    const blockName = elt.dataset.name || "";
    const moreButtonIndex = Array.from(elt.childNodes).findIndex(el => el.classList.contains("zen-toolbar__item--more"));
    return { [blockName]: [moreButtonIndex] };
};
