import { jsx as _jsx } from "react/jsx-runtime";
import { useRef, useEffect, useContext } from "react";
import { FeedbackContext } from "../../feedbackContainer/feedbackContext";
export const ToastAnimation = ({ id, children, isOpen, animation }) => {
    const ref = useRef(null);
    const { startToastAnimation, finishToastAnimation } = useContext(FeedbackContext);
    useEffect(() => {
        if (!ref.current || !isOpen || !animation) {
            return;
        }
        startToastAnimation(id);
        if (animation === "top-bottom") {
            ref.current.animate([
                { bottom: "100%" },
                { bottom: "0%" }
            ], {
                duration: 300,
                iterations: 1,
                delay: 0
            }).onfinish = () => {
                finishToastAnimation(id);
            };
            return;
        }
        if (animation === "bottom-top") {
            ref.current.animate([
                { top: "100%" },
                { top: "0%" }
            ], {
                duration: 300,
                iterations: 1,
                delay: 0
            }).onfinish = () => {
                finishToastAnimation(id);
            };
            return;
        }
        if (animation === "left-right") {
            ref.current.animate([
                { left: "0%" },
                { left: "120%" }
            ], {
                duration: 300,
                iterations: 1,
                delay: 0,
                fill: "forwards"
            }).onfinish = () => {
                finishToastAnimation(id);
            };
            return;
        }
        ref.current.animate([
            { left: "120%" },
            { left: "0%" }
        ], {
            duration: 300,
            iterations: 1,
            delay: 0
        }).onfinish = () => {
            finishToastAnimation(id);
        };
    }, [isOpen, animation, id, startToastAnimation, finishToastAnimation]);
    return isOpen && _jsx("div", { style: { position: "relative" }, ref: ref, children: children });
};
