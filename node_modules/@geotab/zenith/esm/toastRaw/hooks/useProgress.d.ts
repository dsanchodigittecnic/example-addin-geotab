export declare const MIN_SECONDS_TO_SHOW = 7;
interface IUseProgress {
    isOpen: boolean;
    onClose: () => void;
    showSeconds?: number;
}
export declare const useProgress: ({ isOpen, onClose, showSeconds }: IUseProgress) => {
    progress: number;
};
export {};
