import { CSSProperties } from "react";
import { IAbsoluteSize, TContainerSize } from "../cardContainer";
interface IAbsoluteSizeStyles extends Pick<CSSProperties, "width" | "height" | "minHeight"> {
}
export declare const useAbsoluteSize: (size: TContainerSize | IAbsoluteSize, isMobile: boolean, autoHeight: boolean, fullWidth: boolean) => IAbsoluteSizeStyles;
export {};
