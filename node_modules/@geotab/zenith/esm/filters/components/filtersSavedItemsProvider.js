import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
export const filtersSavedItemsContext = createContext({ items: new Set(), active: new Set(), onRemove: undefined, onApply: undefined, onSave: undefined });
export const FiltersSavedItemsProvider = ({ items, active, onRemove, onApply, onSave, children }) => (_jsx(filtersSavedItemsContext.Provider, { value: { items, active, onRemove, onApply, onSave }, children: children }));
export const useFiltersSavedItems = () => {
    const { items, active, onRemove, onApply, onSave } = useContext(filtersSavedItemsContext);
    return { items, active, onRemove, onApply, onSave };
};
