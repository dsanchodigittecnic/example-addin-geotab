var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { Chip } from "../../chip/chip";
import { useChipStatus } from "../../chip/chipStatusProvider";
export const FiltersChipDisplayName = "FiltersChip";
export const FiltersChip = (_a) => {
    var { className, name, state, onChange: onChangeState, id } = _a, componentProps = __rest(_a, ["className", "name", "state", "onChange", "id"]);
    const chipStatus = useChipStatus();
    const component = useMemo(() => _jsx(Chip, Object.assign({ id: id }, componentProps, { className: classNames(["zen-filters-chip", className || ""]), active: state, status: state ? chipStatus || undefined : undefined, onChange: () => onChangeState(!state), children: name })), [id, componentProps, className, state, chipStatus, name, onChangeState]);
    return component;
};
FiltersChip.displayName = FiltersChipDisplayName;
