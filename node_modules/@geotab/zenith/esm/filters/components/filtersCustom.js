import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo, useRef } from "react";
import { IconFilter } from "../../icons/iconFilter";
import { Chip } from "../../chip/chip";
export const FiltersCustomDisplayName = "FiltersCustom";
export const FiltersCustom = ({ state, defaultState, name, chipIcon, chipName, id }) => {
    const triggerRef = useRef(null);
    const chipComponent = useMemo(() => _jsx(Chip, { status: state === defaultState ? undefined : "active", isOpen: false, disabled: false, onClick: () => { }, onClose: state === defaultState ? undefined : () => { }, triggerRef: triggerRef, icon: chipIcon || IconFilter, id: id, children: chipName || name }), [state, defaultState, chipIcon, id, chipName, name]);
    return chipComponent;
};
FiltersCustom.displayName = FiltersCustomDisplayName;
