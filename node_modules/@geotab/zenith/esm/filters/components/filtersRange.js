var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { IconFilter } from "../../icons/iconFilter";
import { Range } from "../../range/range";
export const FiltersRangeDisplayName = "FiltersRange";
export const FiltersRange = (_a) => {
    var { className, state, defaultState, name, onChange: onChangeState, id, chipIcon } = _a, otherRangeProps = __rest(_a, ["className", "state", "defaultState", "name", "onChange", "id", "chipIcon"]);
    const component = useMemo(() => _jsx(Range, Object.assign({}, otherRangeProps, { label: name, value: state, onChange: onChangeState, defaultValue: defaultState, className: classNames(["zen-filters-range", className || ""]), chip: true, chipId: id, chipName: name, chipIcon: chipIcon || IconFilter })), [otherRangeProps, chipIcon, state, onChangeState, defaultState, className, id, name]);
    return component;
};
FiltersRange.displayName = FiltersRangeDisplayName;
