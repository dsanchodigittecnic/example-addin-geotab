var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { classNames } from "../../commonHelpers/classNames/classNames";
import { DateRange } from "../../dateRange/dateRange";
import { IconCalendar } from "../../icons/iconCalendar";
export const FiltersDateRangeDisplayName = "FiltersDateRange";
export const FiltersDateRange = (_a) => {
    var { className, state, defaultState, name, onChange: onChangeState, id, allowUnsetValue, chipIcon } = _a, dateRangeProps = __rest(_a, ["className", "state", "defaultState", "name", "onChange", "id", "allowUnsetValue", "chipIcon"]);
    const component = useMemo(() => allowUnsetValue ? _jsx(DateRange, Object.assign({}, dateRangeProps, { value: state, onChange: onChangeState, defaultValue: defaultState, allowUnsetValue: true, className: classNames(["zen-filters-date-range", className || ""]), chip: true, chipId: id, chipName: name, chipIcon: chipIcon || IconCalendar })) : _jsx(DateRange, Object.assign({}, dateRangeProps, { value: state, onChange: onChangeState, defaultValue: defaultState, allowUnsetValue: false, className: classNames(["zen-filters-date-range", className || ""]), chip: true, chipId: id, chipName: name, chipIcon: chipIcon || IconCalendar })), [dateRangeProps, id, state, defaultState, className, name, onChangeState, allowUnsetValue, chipIcon]);
    return component;
};
FiltersDateRange.displayName = FiltersDateRangeDisplayName;
