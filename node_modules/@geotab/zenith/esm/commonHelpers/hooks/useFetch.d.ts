export interface IFetchState<T> {
    isLoading: boolean;
    data?: T;
    error?: Error;
}
export declare const useFetch: <T>(initialState: Partial<IFetchState<T>>, fetchPromise?: Promise<T>) => IFetchState<T>;
