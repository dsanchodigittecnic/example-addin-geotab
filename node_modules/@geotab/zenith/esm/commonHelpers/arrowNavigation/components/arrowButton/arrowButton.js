import { jsx as _jsx } from "react/jsx-runtime";
import { classNames } from "../../../classNames/classNames";
import { Button } from "../../../../button/button";
import { IconChevronLeft } from "../../../../icons/iconChevronLeft";
import { IconChevronRight } from "../../../../icons/iconChevronRight";
import { useDrive } from "../../../../utils/theme/useDrive";
import { driveIconSize } from "../../../../svgIcon/svgIconSize";
// eslint-disable-next-line @typescript-eslint/naming-convention
export const ArrowNavButton = ({ value, isHidden, disabled, className, arrowButtonHandler, onMouseDown, onMouseUp }) => {
    const isDrive = useDrive();
    const iconSize = isDrive ? driveIconSize("large") : "large";
    return _jsx(Button, { type: "tertiary-black", className: classNames(["zen-arrow-nav-button", isHidden ? "zen-arrow-nav-button--invisible" : "", className !== null && className !== void 0 ? className : ""]), "aria-label": value, disabled: disabled, onClick: arrowButtonHandler, "data-action": value, onMouseDown: onMouseDown, onMouseUp: onMouseUp, children: value === "left"
            ? _jsx(IconChevronLeft, { size: iconSize, className: "zen-arrow-nav-button__icon" })
            : _jsx(IconChevronRight, { size: iconSize, className: "zen-arrow-nav-button__icon" }) });
};
