/// <reference types="react" />
import { TArrowsNavValue } from "../arrowNavigationHelper";
export interface IClearTimersProps {
    action: TArrowsNavValue | null;
    timeoutTimer: React.MutableRefObject<number | null>;
    intervalTimer: React.MutableRefObject<number | null>;
}
export declare const useClearTimers: ({ action, timeoutTimer, intervalTimer }: IClearTimersProps) => void;
