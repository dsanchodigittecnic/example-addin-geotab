import { calculateWithPrecision } from "./calculateWithPrecision";
export const getDecreasedValue = (value, step, min) => {
    const decreasedValue = calculateWithPrecision(Number(value) || 0, step, false);
    return decreasedValue < min ? min : decreasedValue;
};
