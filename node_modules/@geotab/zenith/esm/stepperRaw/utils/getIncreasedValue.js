import { calculateWithPrecision } from "./calculateWithPrecision";
export const getIncreasedValue = (value, step, max) => {
    const increasedValue = calculateWithPrecision(Number(value) || 0, step);
    return increasedValue > max ? max : increasedValue;
};
