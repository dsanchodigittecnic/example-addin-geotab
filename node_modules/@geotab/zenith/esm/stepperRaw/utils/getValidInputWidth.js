import { INPUT_DEFAULT_WIDTH, INPUT_DEFAULT_WIDTH_DRIVE } from "../stepperRaw";
export const getValidInputWidth = (width, isDrive) => {
    const minWidth = isDrive ? INPUT_DEFAULT_WIDTH_DRIVE : INPUT_DEFAULT_WIDTH;
    return width < minWidth ? minWidth : width;
};
