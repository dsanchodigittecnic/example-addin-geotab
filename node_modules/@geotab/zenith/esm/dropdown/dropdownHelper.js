import { ENTIRE_ORGANIZATION_GROUP_ID, getGroupDescription } from "../groupsFilter/groupsHelper";
import { getChildList } from "./stateReducer/stateReducerHelper";
export const getGroupsTreeItem = (stateObj, itemId) => {
    const item = stateObj.groupsMap[itemId];
    return item ? Object.assign({}, item) : undefined;
};
export const getListDataWithDisabled = (listData, isIgnoreDisabled) => isIgnoreDisabled ? listData : listData.filter(el => !el.disabled);
export const isAllChildrenSelected = (stateObj, currentId) => {
    if (!currentId) {
        return false;
    }
    const selectedIds = new Set(stateObj.selectedIds);
    const currentEl = getGroupsTreeItem(stateObj, currentId);
    return currentEl && currentEl.children && currentEl.children.length ? getListDataWithDisabled(currentEl.children, stateObj.isNestedList).every(el => selectedIds.has(el.id)) : false;
};
export const isEveryItemSelected = (listData, selectedItems) => {
    if (!selectedItems.length || !listData.length) {
        return false;
    }
    const selectedItemsSet = new Set(selectedItems);
    return listData.every(el => selectedItemsSet.has(el.id));
};
export const getDataForChange = (listData, selectedItems, isDeselect) => {
    const selectedItemsSet = new Set(selectedItems);
    const result = [];
    for (const item of listData) {
        const isSelected = selectedItemsSet.has(item.id);
        if ((isDeselect && isSelected) || (!isDeselect && !isSelected)) {
            result.push(item.id);
        }
    }
    return result;
};
export const isAllItemsHasOneParent = (listData, childrenArr) => {
    if (listData.length === 0) {
        return true;
    }
    const childrenArrSet = new Set(childrenArr);
    return !listData.every(el => childrenArrSet.has(el.id));
};
export function prepareComboOptionsForList(actionListData, translate, options, getSelectedFn, getSelectedChildFn, isFlatList, getTitle, isAllSelected) {
    const selectedIds = new Set(getSelectedFn());
    const preparedOptions = options && options.length ? options.map(option => {
        const selectedChild = getSelectedChildFn(option.id);
        return Object.assign({ label: getGroupDescription(option, translate), property: option.id, checked: isAllSelected ? true : selectedIds.has(option.id), isWithAction: actionListData.has(option.id), partialChecked: isAllSelected ? false : selectedChild.length > 0, countSelectedChild: 0, blocked: isFlatList ? option.disabled || false : false }, (getTitle ? { title: getTitle(option.id) } : {}));
    }) : [];
    return preparedOptions;
}
const getSelectedChildren = (stateObj, currentId) => {
    const currentEl = stateObj.groupsMap[currentId];
    let selectedChild = [];
    const currentChild = currentEl ? getChildList(currentEl.children) : [];
    currentChild.forEach(el => {
        if (stateObj.selectedIds.includes(el.id)) {
            selectedChild = [...selectedChild, el.id];
        }
    });
    return selectedChild;
};
export const createOptions = (stateObj, translate, listItem, isAllSelected) => {
    const actionListData = new Set();
    listItem.forEach(el => {
        const elem = stateObj.groupsMap[el.id];
        if (elem && elem.children && elem.children.length) {
            actionListData.add(el.id);
        }
    });
    const preparedOptions = prepareComboOptionsForList(actionListData, translate, listItem, () => stateObj.selectedIds, (id) => getSelectedChildren(stateObj, id), !stateObj.isNestedList, (id) => stateObj.groupsMap[id] ? getGroupDescription(stateObj.groupsMap[id], translate) : "", isAllSelected);
    return preparedOptions;
};
export const sortDropdownItemArray = (arr, sortFn) => {
    if (sortFn) {
        return [...arr].sort(sortFn);
    }
    return arr;
};
export const createSingleListOptions = (stateObj, translate, listItem) => {
    const preparedOptions = listItem && listItem.length ? listItem.map(option => ({
        id: option.id,
        children: getGroupDescription(stateObj.groupsMap[option.id] || option, translate),
        disabled: option.disabled,
        multiLevel: Object.keys(stateObj.groupsMap).length === 0 ? false : option.children && option.children.length > 0 && stateObj.groupsMap[option.id] !== undefined
    })) : [];
    return preparedOptions;
};
export const createListDataOptions = (stateObj, translate, listItem, isMultiselect, isAllSelected) => isMultiselect ? createOptions(stateObj, translate, listItem, isAllSelected) : createSingleListOptions(stateObj, translate, listItem);
export const compareStringsArrays = (arr1, arr2) => {
    if (arr1.length !== arr2.length) {
        return false;
    }
    const set2 = new Set(arr2);
    return arr1.every(el => set2.has(el));
};
export const compareObjectsArrays = (arr1, arr2) => arr1.length === arr2.length && arr1.every((el, ind) => {
    const el2 = arr2[ind];
    const sameId = el.id === el2.id;
    const sameDisabled = (el.disabled === el2.disabled) || (el.disabled === undefined && el2.disabled === false) || (el2.disabled === undefined && el.disabled === false);
    return sameId && sameDisabled;
});
export const prepareSelectedIdsToItems = (selectedIds, groupsMap, groupsMapSelected) => {
    const selectedItems = selectedIds.map(id => {
        if (groupsMapSelected.has(id)) {
            return groupsMapSelected.get(id);
        }
        const item = groupsMap[id];
        return item ? Object.assign(Object.assign({ id }, (item.name ? { name: item.name } : {})), (item.color ? { color: item.color } : {})) : { id };
    });
    return selectedItems;
};
export const checkIsDataProblem = (selectedIds, groupsMap) => {
    const isHasDataProblem = selectedIds.some(id => !groupsMap[id]) && Object.keys(groupsMap).length > 1;
    return isHasDataProblem;
};
export const getStringFromSelected = (selectedIds, groupsMapSelected, translate) => {
    if (!selectedIds.length || (selectedIds.length === 1 && selectedIds[0] === ENTIRE_ORGANIZATION_GROUP_ID)) {
        return "";
    }
    const selectedItems = selectedIds.map(id => groupsMapSelected.has(id) ? getGroupDescription(groupsMapSelected.get(id), translate) : undefined).filter(el => el);
    return selectedItems.join(", ");
};
export const getStringFromAllSelected = (groupsMap, translate) => {
    var _a;
    if (!((_a = groupsMap[ENTIRE_ORGANIZATION_GROUP_ID]) === null || _a === void 0 ? void 0 : _a.children) || groupsMap[ENTIRE_ORGANIZATION_GROUP_ID].children.length === 0) {
        return "";
    }
    const selectedItems = groupsMap[ENTIRE_ORGANIZATION_GROUP_ID].children.map(el => groupsMap[el.id] ? getGroupDescription(groupsMap[el.id], translate) : undefined).filter(el => el);
    return selectedItems.join(", ");
};
export const getSelectedCount = (selectedIds) => selectedIds.length === 1 && selectedIds[0] === ENTIRE_ORGANIZATION_GROUP_ID ? 0 : selectedIds.length;
