import { useEffect } from "react";
import { getNotInitializedMessage } from "../utils/getNotInitializedMessage";
export const useWarning = ({ mode, name }) => {
    useEffect(() => {
        if (mode === null) {
            console.error(getNotInitializedMessage(name));
        }
    }, [name, mode]);
};
